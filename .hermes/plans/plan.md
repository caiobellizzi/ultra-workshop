{"goal": "Find all existing .md documentation files in the repo (README.md, docs/*.md, deploy/*.md, hermes-skills/*.md). For each file, read it and read the code it describes, then update the doc to match the current code — fix outdated info, remove dead references, add missing sections for things the code does. Edit existing files only, do not create new ones.", "steps": [{"id": "1", "description": "Search for .md files in the repo", "files": ["README.md", "docs/*.md", "deploy/*.md", "hermes-skills/*.md"]}, {"id": "2", "description": "Read each .md file and the code it describes", "files": ["README.md", "docs/*.md", "deploy/*.md", "hermes-skills/*.md"]}, {"id": "3", "description": "Update each .md file to match the current code", "files": ["README.md", "docs/*.md", "deploy/*.md", "hermes-skills/*.md"]}], "affected_files": ["README.md", "docs/*.md", "deploy/*.md", "hermes-skills/*.md"], "doc_refs": []}