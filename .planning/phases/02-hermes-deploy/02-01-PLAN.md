---
phase: 02-hermes-deploy
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - deploy/litellm/config.yaml
  - tests/phase-02/helpers.bash
  - tests/phase-02/pre-deploy.bats
autonomous: false
requirements: []

must_haves:
  truths:
    - "2GB swap file exists on VPS and is active (swapon --show confirms)"
    - "uab-telegram.service is masked (systemctl status shows 'masked')"
    - "Telegram bot token is rotated — old token revoked, new token saved in /etc/uws/env"
    - "LiteLLM private-worker timeout is 30s in deploy/litellm/config.yaml (not 300s)"
    - "Node.js 24 is installed on VPS via nvm and accessible at /home/uws/.nvm"
    - "pre-deploy.bats runs green against the live VPS"
  artifacts:
    - path: "deploy/litellm/config.yaml"
      provides: "LiteLLM config with private-worker timeout = 30s (L26)"
    - path: "tests/phase-02/helpers.bash"
      provides: "Shared SSH assertion helpers for bats test suite"
    - path: "tests/phase-02/pre-deploy.bats"
      provides: "Automated pre-deploy gate checks (swap, telegram mask, Node.js)"
  key_links:
    - from: "deploy/litellm/config.yaml"
      to: "VPS /opt/ultra-agents-brain/litellm/config.yaml (rsync target)"
      via: "rsync in install script"
      pattern: "timeout: 30"
---

<objective>
Clear all blocking pre-deploy gates and create the Wave 0 test harness. Nothing from Wave 1 onward can proceed until every gate is green.

Purpose: Prevent OOM kills, token collision, and config drift from blocking the deploy. L26 requires the LiteLLM timeout update to land before Hermes is wired up.
Output: Green pre-deploy.bats; 2GB swap on VPS; uab-telegram masked; Node.js 24 installed; LiteLLM config updated and rsynced; /etc/uws/env stub created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hermes-deploy/02-CONTEXT.md
@.planning/phases/02-hermes-deploy/02-RESEARCH.md
@.planning/phases/02-hermes-deploy/02-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Rotate Telegram bot token via BotFather (L7 gate)</name>
  <what-built>Nothing automated — BotFather is interactive Telegram-side. This is a mandatory blocking gate (L7). The old token in /opt/ultra-agents-brain/.env must be revoked before any Hermes deploy touches the Telegram API.</what-built>
  <how-to-verify>
    1. Open Telegram → search @BotFather → send /revoke
    2. Select the workshop bot (the one currently running uab-telegram.service)
    3. Confirm revocation — BotFather sends a new token
    4. Copy the new token (format: 1234567890:AAF...) — you will need it in Task 2
    5. Verify old token is dead: curl https://api.telegram.org/bot{OLD_TOKEN}/getMe → {"ok":false}
  </how-to-verify>
  <resume-signal>Type "token rotated" and paste the new token (it will go into /etc/uws/env in Task 2 — do NOT put it in chat if this is a shared context)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: VPS pre-deploy gates — swap + uab-telegram mask + Node.js 24 + /etc/uws/env stub</name>
  <files>tests/phase-02/helpers.bash, tests/phase-02/pre-deploy.bats</files>
  <read_first>
    Research §Verified: Add swap — fallocate + mkswap + /etc/fstab pattern
    Research §Verified: systemd pre-deploy gate commands — stop/disable/mask uab-telegram
    Research Pitfall 3 — Node.js not installed; nvm install path: curl nvm v0.40.3 + nvm install 24
    Research §Verified: /etc/uws/env permissions — mkdir /etc/uws, chown root:uws, chmod 0640
    VALIDATION.md Wave 0 Requirements — lists exactly what tests/phase-02/ files are needed
  </read_first>
  <action>
    SSH into VPS (root@31.97.130.253) and execute the following idempotent sequence. Each step is a gate — fail fast if any step errors.

    SWAP (Pitfall 5):
    - Check if /swapfile exists and is active: `swapon --show`. If not present: `fallocate -l 2G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile`. Append `/swapfile none swap sw 0 0` to /etc/fstab only if not already present (grep before appending).

    BRAIN TELEGRAM MASK (Pitfall 4, REQ-ws-013, D8):
    - `systemctl stop uab-telegram && systemctl disable uab-telegram && systemctl mask uab-telegram`
    - Verify: `systemctl status uab-telegram` must show `Loaded: masked`

    NODE.JS 24 via nvm (Pitfall 3):
    - Install nvm as the uws user will need it. Run as root first to confirm curl works, then install for uws:
      `sudo -u uws bash -c 'curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash'`
    - Source and install: `sudo -u uws bash -i -c 'nvm install 24 && nvm use 24 && nvm alias default 24'`
    - Verify: `sudo -u uws bash -i -c 'node --version'` → v24.x.x

    /etc/uws/env STUB (credentials placeholder, per D-discretion in CONTEXT.md):
    - `mkdir -p /etc/uws`
    - Create /etc/uws/env with the structure from Research §Pattern 3. Replace secret values with `PLACEHOLDER` — the human sets real values before Wave 1. Add the new Telegram token from Task 1 checkpoint.
    - `chown root:uws /etc/uws/env && chmod 0640 /etc/uws/env`
    - If uws user doesn't exist yet: `useradd --system --home-dir /home/uws --create-home --shell /bin/bash uws` (idempotent guard: check with `id uws` first). uws user creation is required here so /etc/uws/env ownership works; full uws provisioning happens in Plan 02.

    BATS TEST HARNESS (VALIDATION.md Wave 0):
    Create `tests/phase-02/helpers.bash` — shared helper that sets VPS_HOST, VPS_PORT, runs ssh_cmd() wrapper:
    ```
    VPS_HOST="31.97.130.253"
    VPS_USER="root"
    VPS_SSH="ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10"

    ssh_cmd() {
      $VPS_SSH ${VPS_USER}@${VPS_HOST} "$@"
    }

    assert_service_active() {
      local svc="$1"
      ssh_cmd "systemctl is-active $svc" | grep -q "^active$"
    }

    assert_service_masked() {
      local svc="$1"
      ssh_cmd "systemctl status $svc 2>&1" | grep -q "masked"
    }
    ```

    Create `tests/phase-02/pre-deploy.bats` covering:
    - swap: `swapon --show` on VPS returns non-empty output
    - uab-telegram masked: `systemctl status uab-telegram` contains "masked"
    - node: `sudo -u uws bash -i -c 'node --version'` returns "v24"
    - /etc/uws/env exists: stat returns 0
    - /etc/uws/env permissions: stat shows 640
    - LiteLLM timeout: `grep -v '^#' /opt/ultra-agents-brain/litellm/config.yaml | grep timeout` returns 30 (after Task 3 rsync)
  </action>
  <verify>
    <automated>bats tests/phase-02/pre-deploy.bats</automated>
  </verify>
  <done>bats tests/phase-02/pre-deploy.bats passes all assertions; VPS: swapon shows 2G, uab-telegram is masked, node -v returns v24.x, /etc/uws/env exists at 0640</done>
</task>

<task type="auto">
  <name>Task 3: Update LiteLLM private-worker timeout to 30s and rsync to VPS (L26)</name>
  <files>deploy/litellm/config.yaml</files>
  <read_first>
    Research §Verified: LiteLLM private-worker timeout update — yaml snippet showing timeout: 30 replacing 300
    CONTEXT.md L26 — "Update LiteLLM `private-worker` timeout to 30s and rsync to VPS during this phase"
    Research Pitfall 7 — must be done before Hermes is wired up
  </read_first>
  <action>
    1. Read the current `deploy/litellm/config.yaml`. Locate the `private-worker` model_name entry. Change `timeout: 300` (or whatever current value) to `timeout: 30`. Do not change any other fields.

    2. If deploy/litellm/config.yaml does not yet exist locally (it lives on VPS), fetch it first:
       `scp root@31.97.130.253:/opt/ultra-agents-brain/litellm/config.yaml deploy/litellm/config.yaml`
       Then apply the edit and rsync back.

    3. Rsync the updated config to VPS:
       `rsync -av deploy/litellm/config.yaml root@31.97.130.253:/opt/ultra-agents-brain/litellm/config.yaml`

    4. Restart LiteLLM on VPS to pick up the new timeout:
       `ssh root@31.97.130.253 "systemctl restart uab-litellm || systemctl restart litellm || true"`
       (use whatever service name Brain uses for LiteLLM — check with `systemctl list-units | grep litellm`)

    5. Commit the local change: `git add deploy/litellm/config.yaml && git commit -m "chore(litellm): reduce private-worker timeout 300→30s (L26)"`
  </action>
  <verify>
    <automated>grep -v '^#' deploy/litellm/config.yaml | grep -c 'timeout: 30'</automated>
  </verify>
  <done>deploy/litellm/config.yaml has `timeout: 30` under private-worker; file is rsynced to VPS; LiteLLM service restarted on VPS; pre-deploy.bats LiteLLM assertion passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→VPS (SSH) | All pre-deploy commands cross this boundary as root |
| /etc/uws/env | Secret file — root:uws 0640; contents must not appear in git or logs |
| Telegram bot token | Rotated in this plan; old token must be dead before Wave 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | Telegram bot (old token active) | mitigate | BotFather /revoke in Task 1 kills old token before any Hermes deploy |
| T-02-02 | Tampering | /etc/uws/env permissions | mitigate | chmod 0640, chown root:uws — uws can read, others cannot |
| T-02-03 | Denial of Service | VPS OOM (0B swap) | mitigate | 2GB swapfile added in Task 2 before any memory-consuming service |
| T-02-04 | Information Disclosure | New bot token in chat history | mitigate | checkpoint:human-action asks user NOT to paste token in shared context |
| T-02-SC | Tampering | npm/nvm install scripts | mitigate | nvm v0.40.3 pinned URL; no package install in this plan — Node only |
</threat_model>

<verification>
Run after all tasks complete:

```bash
bats tests/phase-02/pre-deploy.bats
ssh root@31.97.130.253 "swapon --show && systemctl status uab-telegram | grep masked && sudo -u uws bash -i -c 'node --version'"
grep 'timeout: 30' deploy/litellm/config.yaml
```

All must pass before proceeding to Plan 02.
</verification>

<success_criteria>
- bats tests/phase-02/pre-deploy.bats: all assertions green
- VPS: `swapon --show` shows 2G swapfile active
- VPS: `systemctl status uab-telegram` shows "masked"
- VPS: `sudo -u uws bash -i -c 'node --version'` returns v24.x.x
- Local: `deploy/litellm/config.yaml` has `timeout: 30` committed to git
- VPS: `/etc/uws/env` exists, permissions 0640, owner root:uws
- Telegram: old bot token revoked (curl to old token returns `{"ok":false}`)
</success_criteria>

<output>
Create `.planning/phases/02-hermes-deploy/02-01-SUMMARY.md` when done
</output>
