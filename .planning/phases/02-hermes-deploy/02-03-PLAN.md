---
phase: 02-hermes-deploy
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - hermes-config/config.yaml
  - tests/phase-02/telegram.bats
autonomous: false
requirements:
  - REQ-ws-002
  - REQ-ws-013

must_haves:
  truths:
    - "uab-telegram.service is inactive (dead) and masked — Brain's Telegram is permanently dead"
    - "Hermes Telegram gateway responds to /start from chat ID 7113965359 within 5s"
    - "Messages from any other chat ID are silently ignored by Hermes"
    - "TELEGRAM_BOT_TOKEN is set in /etc/uws/env (not in config.yaml)"
  artifacts:
    - path: "hermes-config/config.yaml"
      provides: "Telegram platform config — allow_from 7113965359; TELEGRAM_BOT_TOKEN via env var"
    - path: "tests/phase-02/telegram.bats"
      provides: "Automated checks: uab-telegram masked, Hermes Telegram accepts 7113965359"
  key_links:
    - from: "hermes-config/config.yaml platforms.telegram.extra.allow_from"
      to: "Hermes gateway allow_from filter"
      via: "config reload after restart"
      pattern: "7113965359"
    - from: "/etc/uws/env TELEGRAM_BOT_TOKEN"
      to: "hermes-config/config.yaml ${TELEGRAM_BOT_TOKEN}"
      via: "systemd EnvironmentFile substitution"
      pattern: "TELEGRAM_BOT_TOKEN"
---

<objective>
Wire the Telegram gateway into the running Hermes service and confirm the chat-ID gate. At the end of this plan, the user can send /start to the bot from chat 7113965359 and receive a reply within 5s.

Purpose: Validates REQ-ws-002 (reply latency + chat ID gate) and definitively verifies REQ-ws-013 (Brain's Telegram is dead). Must complete before HITL testing in Plan 05.
Output: Telegram working through Hermes; uab-telegram confirmed dead; telegram.bats green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hermes-deploy/02-CONTEXT.md
@.planning/phases/02-hermes-deploy/02-RESEARCH.md
@.planning/phases/02-hermes-deploy/02-02-SUMMARY.md
</context>

<interfaces>
<!-- From Research §Verified: Telegram allowed_users config -->
```yaml
platforms:
  telegram:
    extra:
      allow_from:
        - "7113965359"   # numeric user ID, not username
```

Or equivalently via env var (both work — config.yaml approach is preferred for auditing):
```bash
TELEGRAM_ALLOWED_USERS=7113965359
```

From Research §Pattern 3 — /etc/uws/env includes:
```bash
TELEGRAM_BOT_TOKEN=<new-rotated-token>   # from Plan 01 Task 1 checkpoint
```

From Research §Verified config.yaml — secret substitution pattern:
```yaml
# In config.yaml, reference env vars with ${} — Hermes substitutes at startup
# TELEGRAM_BOT_TOKEN is consumed by the gateway platform implicitly from EnvironmentFile
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update hermes-config/config.yaml Telegram section and inject bot token into /etc/uws/env</name>
  <files>hermes-config/config.yaml</files>
  <read_first>
    Research §Verified: Telegram allowed_users config — allow_from is a list of numeric user ID strings
    Research §Pattern 3 — /etc/uws/env layout; TELEGRAM_BOT_TOKEN must be the rotated token from Plan 01
    CONTEXT.md L4 — Hermes exclusively owns Telegram; L7 — token rotated pre-deploy
    Research §Standard Stack — Hermes reads TELEGRAM_BOT_TOKEN from env implicitly for the telegram platform
  </read_first>
  <action>
    Update `hermes-config/config.yaml` to ensure the Telegram platform section is complete:
    - `platforms.telegram.extra.allow_from: ["7113965359"]` — already in skeleton from Plan 02 but confirm it's present
    - Hermes reads `TELEGRAM_BOT_TOKEN` from the environment automatically when the `telegram` platform is active — no explicit token field in config.yaml (secrets stay in /etc/uws/env)

    On VPS, update `/etc/uws/env` to replace the TELEGRAM_BOT_TOKEN PLACEHOLDER with the real rotated token from Plan 01 Task 1. This is a VPS-side edit (not in git):
    ```
    ssh root@31.97.130.253 "nano /etc/uws/env"
    # OR: use sed to replace the placeholder:
    # ssh root@31.97.130.253 "sed -i 's/TELEGRAM_BOT_TOKEN=.*/TELEGRAM_BOT_TOKEN=<new_token>/' /etc/uws/env"
    ```
    After updating /etc/uws/env, restart uws-hermes to pick up the new token:
    `ssh root@31.97.130.253 "systemctl restart uws-hermes && sleep 20 && systemctl is-active uws-hermes"`

    Rsync updated config.yaml to VPS:
    `rsync -av hermes-config/ root@31.97.130.253:/opt/ultra-workshop/hermes-config/`

    Commit config.yaml change (no token in it):
    `git add hermes-config/config.yaml && git commit -m "feat(hermes): confirm telegram platform config with allow_from 7113965359"`

    Create `tests/phase-02/telegram.bats` covering:
    - uab-telegram.service is masked: `systemctl status uab-telegram` contains "masked"
    - uab-telegram.service is not running: `systemctl is-active uab-telegram` returns "inactive" or "failed" (not "active")
    - /etc/uws/env contains TELEGRAM_BOT_TOKEN line (value not checked — just that the key exists): `grep -c '^TELEGRAM_BOT_TOKEN=' /etc/uws/env`
    - uws-hermes service is active after token injection
  </action>
  <verify>
    <automated>bats tests/phase-02/telegram.bats</automated>
  </verify>
  <done>bats tests/phase-02/telegram.bats passes; uab-telegram confirmed masked on VPS; uws-hermes restarts cleanly with new TELEGRAM_BOT_TOKEN; hermes-config/config.yaml committed with allow_from</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Live Telegram /start gate verification (REQ-ws-002)</name>
  <what-built>Task 1 wired the bot token and chat-ID allowlist into the running Hermes service. This checkpoint verifies the user-facing behavior that cannot be asserted by bats alone: Hermes actually replies to /start from the allowed chat ID within 5 seconds, and silently ignores messages from other IDs.</what-built>
  <how-to-verify>
    1. Open Telegram on your phone/desktop
    2. Find the workshop bot (the one whose token you rotated via BotFather in Plan 01)
    3. Send: /start
    4. Expected: Hermes replies within 5 seconds (any acknowledgement message — "Hello" or similar)
    5. Check reply latency: note the time between send and receive
    6. OPTIONAL — unauthorized chat test: from a DIFFERENT Telegram account (or ask a contact), send a message to the bot. Expected: no reply (silent reject)
    7. On VPS, verify journal: `journalctl -u uws-hermes -n 30 --no-pager` — look for "Telegram" and the chat_id=7113965359 in the log
    8. Verify Brain's Telegram is completely dead: the old Brain bot should not respond to anything
  </how-to-verify>
  <resume-signal>Type "telegram-ok" if /start returns a reply within 5s from chat 7113965359, or describe the failure (e.g. "no reply", "wrong bot responded", "timeout")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram API → Hermes gateway | Long-polling; Hermes validates sender via allow_from before processing |
| /etc/uws/env TELEGRAM_BOT_TOKEN | Secret — root:uws 0640; not in git; injected via systemd EnvironmentFile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Spoofing | Telegram sender identity | mitigate | allow_from: ["7113965359"] numeric ID; user IDs cannot be spoofed on Telegram |
| T-02-10 | Information Disclosure | TELEGRAM_BOT_TOKEN in journal logs | mitigate | Hermes v0.7.0+ redacts secrets; verify with `journalctl -u uws-hermes | grep -i token` returns no token value |
| T-02-11 | Spoofing | Old Brain bot token still active | mitigate | Plan 01 Task 1 revoked old token via BotFather — double-check in Task 2 live test |
| T-02-SC | Tampering | npm/pip installs | accept | No package installs in this plan |
</threat_model>

<verification>
```bash
bats tests/phase-02/telegram.bats
ssh root@31.97.130.253 "systemctl status uab-telegram | grep -E 'masked|inactive'"
ssh root@31.97.130.253 "systemctl is-active uws-hermes"
# Human: send /start to bot from chat 7113965359, measure reply latency
```
</verification>

<success_criteria>
- REQ-ws-013: `systemctl status uab-telegram` shows "masked" AND "inactive (dead)" on VPS
- REQ-ws-002: /start from chat ID 7113965359 receives a reply within 5 seconds
- REQ-ws-002: Messages from other chat IDs receive no reply (silent reject)
- bats tests/phase-02/telegram.bats passes all assertions
- TELEGRAM_BOT_TOKEN is set in /etc/uws/env (verified by bats) and NOT in any git-tracked file
</success_criteria>

<output>
Create `.planning/phases/02-hermes-deploy/02-03-SUMMARY.md` when done
</output>
