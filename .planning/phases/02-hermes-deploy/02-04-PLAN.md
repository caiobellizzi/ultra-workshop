---
phase: 02-hermes-deploy
plan: 04
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - hermes-config/config.yaml
  - tests/phase-02/mcps.bats
autonomous: false
requirements:
  - REQ-ws-015

must_haves:
  truths:
    - "github MCP is registered — npx @modelcontextprotocol/server-github responds to tool list"
    - "context7 MCP is registered — npx @upstash/context7-mcp responds to tool list"
    - "crawl4ai MCP is registered — Hermes connects to SSE at localhost:11235 (no npm package)"
    - "hostinger-api MCP is registered — npx hostinger-api-mcp responds to tool list"
    - "google-workspace MCP is registered — uvx workspace-mcp responds to tool list via service account"
    - "All 5 MCPs appear in hermes mcp list output"
  artifacts:
    - path: "hermes-config/config.yaml"
      provides: "Full mcp_servers section with all 5 MCPs; secrets referenced as ${VAR}"
    - path: "tests/phase-02/mcps.bats"
      provides: "Automated MCP registration checks via hermes mcp list or gateway log"
  key_links:
    - from: "hermes-config/config.yaml mcp_servers.github.env.GITHUB_PERSONAL_ACCESS_TOKEN"
      to: "/etc/uws/env GITHUB_PAT"
      via: "systemd EnvironmentFile ${GITHUB_PAT} substitution"
      pattern: "\\$\\{GITHUB_PAT\\}"
    - from: "hermes-config/config.yaml mcp_servers.crawl4ai.url"
      to: "Crawl4AI Docker container on VPS at localhost:11235"
      via: "Hermes native SSE url: connection"
      pattern: "localhost:11235/mcp/sse"
    - from: "hermes-config/config.yaml mcp_servers.google-workspace.env.GOOGLE_SERVICE_ACCOUNT_KEY_FILE"
      to: "/etc/uws/gcp-service-account.json"
      via: "uvx subprocess env inheritance"
      pattern: "/etc/uws/gcp-service-account.json"
---

<objective>
Register all 5 MCP servers in hermes-config/config.yaml and confirm each is reachable from the running Hermes service. crawl4ai uses native SSE (no npm); google-workspace requires a one-time manual GCP service account setup before the automated step.

Purpose: REQ-ws-015 requires all 5 MCPs healthy. This plan delivers them. Wave 2 parallel to Plan 03 (no file conflict — Plan 03 edits platforms.telegram section; Plan 04 edits mcp_servers section).
Output: hermes-config/config.yaml with full mcp_servers; /etc/uws/env populated with 4 API credentials; /etc/uws/gcp-service-account.json on VPS; mcps.bats green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-hermes-deploy/02-CONTEXT.md
@.planning/phases/02-hermes-deploy/02-RESEARCH.md
@.planning/phases/02-hermes-deploy/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Full mcp_servers config from Research §Pattern 2 (verified schema) -->
```yaml
mcp_servers:
  github:
    command: "npx"
    args: ["-y", "@modelcontextprotocol/server-github"]
    env:
      GITHUB_PERSONAL_ACCESS_TOKEN: "${GITHUB_PAT}"
    timeout: 120
    connect_timeout: 60

  context7:
    command: "npx"
    args: ["-y", "@upstash/context7-mcp"]
    timeout: 60
    connect_timeout: 30

  crawl4ai:
    # Direct SSE — Crawl4AI Docker already on VPS at port 11235; no npm package needed
    url: "http://localhost:11235/mcp/sse"
    connect_timeout: 60
    timeout: 180

  hostinger-api:
    command: "npx"
    args: ["-y", "hostinger-api-mcp"]
    env:
      API_TOKEN: "${HOSTINGER_API_TOKEN}"
    timeout: 60
    connect_timeout: 30

  google-workspace:
    command: "uvx"
    args: ["workspace-mcp", "--tool-tier", "core"]
    env:
      GOOGLE_SERVICE_ACCOUNT_KEY_FILE: "/etc/uws/gcp-service-account.json"
      USER_GOOGLE_EMAIL: "${GOOGLE_USER_EMAIL}"
    timeout: 60
    connect_timeout: 30
```

Package legitimacy gate (from Research §Package Legitimacy Audit):
- github: Approved [CITED: modelcontextprotocol.io]
- context7: Approved [CITED: context7.com/docs]
- crawl4ai: SSE url: — no npm install needed (eliminates [ASSUMED] package)
- hostinger-api: Approved [CITED: hostinger.com/support/11079316]
- workspace-mcp: [ASSUMED] — requires checkpoint:human-verify before uvx install

/etc/uws/env additions needed:
```bash
GITHUB_PAT=<fine-grained-pat-scoped-to-test-workshop-sandbox>
HOSTINGER_API_TOKEN=<hostinger-api-token>
GOOGLE_USER_EMAIL=caiobellizzi@gmail.com
```
</interfaces>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: GCP service account setup + workspace-mcp legitimacy check (manual gates)</name>
  <what-built>Two blocking manual actions that cannot be automated: (1) GCP service account creation and key download, (2) human review of workspace-mcp PyPI package before uvx install.</what-built>
  <how-to-verify>
    === GCP SERVICE ACCOUNT SETUP (for google-workspace MCP) ===
    1. Go to console.cloud.google.com → create or select a project
    2. Enable APIs: Gmail API, Google Calendar API, Google Drive API
    3. IAM & Admin → Service Accounts → Create Service Account
       Name: "ultra-workshop-hermes", role: none (API-level scopes handle access)
    4. Keys tab → Add Key → JSON → download the JSON file
    5. Copy JSON to VPS: `scp <downloaded-file>.json root@31.97.130.253:/etc/uws/gcp-service-account.json`
    6. On VPS: `chown root:uws /etc/uws/gcp-service-account.json && chmod 0640 /etc/uws/gcp-service-account.json`
    7. For Gmail/Calendar domain-wide delegation: Admin Console → Security → API Controls → Domain-wide delegation → Add service account client ID with scopes:
       https://www.googleapis.com/auth/gmail.modify,https://www.googleapis.com/auth/calendar,https://www.googleapis.com/auth/drive

    === workspace-mcp PACKAGE REVIEW ===
    Review the package before uvx installs it in Task 3:
    1. Visit: https://pypi.org/project/workspace-mcp/ — confirm version 1.21.0 exists, author is taylorwilsdon
    2. Visit: https://github.com/taylorwilsdon/google_workspace_mcp — confirm repo exists, last commit recent, not archived
    3. Spot-check: no postinstall hooks that execute remote code; setup.py/pyproject.toml shows only google-auth and similar dependencies
    4. Confirm you're comfortable proceeding

    === API CREDENTIALS IN /etc/uws/env ===
    Add to /etc/uws/env on VPS (via ssh + nano or sed):
    - GITHUB_PAT=<your fine-grained PAT scoped to test-workshop-sandbox repo>
    - HOSTINGER_API_TOKEN=<your Hostinger API token from hpanel.hostinger.com → API>
    - GOOGLE_USER_EMAIL=caiobellizzi@gmail.com

    Sources:
    - GitHub PAT: github.com/settings/tokens → Fine-grained tokens → select test-workshop-sandbox repo → Contents read + Issues write
    - Hostinger token: hpanel.hostinger.com → Profile → API
  </how-to-verify>
  <resume-signal>Type "gcp-ready" when: (a) gcp-service-account.json is on VPS at /etc/uws/gcp-service-account.json with 0640 root:uws, (b) workspace-mcp reviewed and approved, (c) GITHUB_PAT + HOSTINGER_API_TOKEN + GOOGLE_USER_EMAIL added to /etc/uws/env</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update hermes-config/config.yaml with full mcp_servers section</name>
  <files>hermes-config/config.yaml</files>
  <read_first>
    Research §Pattern 2 — full mcp_servers schema with all 5 MCPs
    Research §Package Legitimacy Audit — crawl4ai uses url: (no npm); hostinger-api and github Approved; workspace-mcp [ASSUMED] but reviewed in Task 1
    Research Open Question 1 — if HERMES_CONFIG_PATH not honored, the symlink from Plan 02 makes /home/uws/.hermes/config.yaml the effective config; edit the source at /opt/ultra-workshop/hermes-config/config.yaml
    Research §Verified: Hermes config.yaml mcp_servers schema — verified schema for stdio vs SSE servers
  </read_first>
  <action>
    Edit `hermes-config/config.yaml` to replace the `mcp_servers: {}` stub with the full 5-server configuration from the interfaces block above. Key decisions per research:

    - github: `npx -y @modelcontextprotocol/server-github` with `GITHUB_PERSONAL_ACCESS_TOKEN: "${GITHUB_PAT}"` — approved package
    - context7: `npx -y @upstash/context7-mcp` — approved package, no env vars needed
    - crawl4ai: `url: "http://localhost:11235/mcp/sse"` — native SSE, no npm install — preferred over crawl4ai-mcp-sse-stdio (which is [ASSUMED]) per Research §Alternatives Considered
    - hostinger-api: `npx -y hostinger-api-mcp` with `API_TOKEN: "${HOSTINGER_API_TOKEN}"` — approved package
    - google-workspace: `uvx workspace-mcp --tool-tier core` with `GOOGLE_SERVICE_ACCOUNT_KEY_FILE: "/etc/uws/gcp-service-account.json"` and `USER_GOOGLE_EMAIL: "${GOOGLE_USER_EMAIL}"` — reviewed in Task 1

    All secret values use ${VAR} substitution — actual values come from /etc/uws/env via EnvironmentFile at runtime. Do NOT hardcode any credential in config.yaml.

    After editing, rsync to VPS:
    `rsync -av hermes-config/ root@31.97.130.253:/opt/ultra-workshop/hermes-config/`

    Restart Hermes to load the new MCP config:
    `ssh root@31.97.130.253 "systemctl restart uws-hermes && sleep 25 && systemctl is-active uws-hermes"`
    (25s wait: Hermes cold-start ~19s + MCP handshakes)

    Commit: `git add hermes-config/config.yaml && git commit -m "feat(hermes): register 5 MCP servers (github, context7, crawl4ai, hostinger, workspace)"`
  </action>
  <verify>
    <automated>grep -c 'github\|context7\|crawl4ai\|hostinger-api\|google-workspace' hermes-config/config.yaml</automated>
  </verify>
  <done>hermes-config/config.yaml committed with all 5 mcp_servers entries; no secrets in file; rsynced to VPS; Hermes restarted and active</done>
</task>

<task type="auto">
  <name>Task 3: Verify all 5 MCPs and write mcps.bats</name>
  <files>tests/phase-02/mcps.bats</files>
  <read_first>
    Research Open Question 3 — `hermes mcp list` command availability in v0.14.0; fallback is to check gateway logs for "Connected to MCP server"
    Research §Assumptions Log A1 — Crawl4AI Docker assumed running at localhost:11235; if not, fallback is to check docker ps on VPS
    Research §Assumptions Log A2 — `hermes mcp list` may use different syntax; adjust bats if needed
    VALIDATION.md Per-Task Verification Map — REQ-ws-015 test: `hermes mcp list | grep -c 'tools'`
  </read_first>
  <action>
    SSH into VPS and verify each MCP is reachable by running:
    `sudo -u uws bash -i -c '/opt/ultra-workshop/hermes/hermes-agent/.venv/bin/hermes mcp list'`

    If `hermes mcp list` is not available in v0.14.0, use the journal as the source of truth:
    `journalctl -u uws-hermes -n 100 --no-pager | grep -E "Connected|MCP|tools"`
    Count lines containing the 5 server names.

    Verify crawl4ai specifically: `curl -s http://localhost:11235/health` should return 200. If Crawl4AI Docker is down, start it: `docker compose -f ~/.agents/crawl4ai/docker-compose.yml up -d` (on LOCAL machine — the Docker runs locally, not on VPS). Wait, re-check architecture: the research says "Crawl4AI Docker container is running at localhost:11235 on VPS" (Assumption A1). Verify: `ssh root@31.97.130.253 "docker ps | grep crawl4ai"`. If not running, alert and document — the SSE url: will fail connection on startup but Hermes should retry.

    Create `tests/phase-02/mcps.bats` covering:
    - Hermes mcp list (or log grep) shows "github"
    - Hermes mcp list shows "context7"
    - Hermes mcp list shows "crawl4ai" OR `curl localhost:11235/health` returns 200 on VPS
    - Hermes mcp list shows "hostinger-api"
    - Hermes mcp list shows "google-workspace"
    - Total connected MCP count ≥ 5 (use log grep count or mcp list count)
    - /etc/uws/gcp-service-account.json exists on VPS with permissions 640

    Commit: `git add tests/phase-02/mcps.bats && git commit -m "test(hermes): bats coverage for 5 MCP registrations (REQ-ws-015)"`
  </action>
  <verify>
    <automated>bats tests/phase-02/mcps.bats</automated>
  </verify>
  <done>bats tests/phase-02/mcps.bats passes; all 5 MCPs confirmed registered on running Hermes instance; REQ-ws-015 acceptance criterion met</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /etc/uws/gcp-service-account.json | GCP service account key — root:uws 0640; not in git; grants access to Gmail/Calendar/Drive |
| Hermes → MCP subprocesses | npx/uvx subprocesses inherit /etc/uws/env; ProtectSystem=strict limits what they can write |
| crawl4ai SSE localhost:11235 | Loopback only — not exposed to network; Docker container on VPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Information Disclosure | GCP service account key | mitigate | /etc/uws/gcp-service-account.json chmod 0640 root:uws; NOT in git; bats asserts permissions |
| T-02-13 | Information Disclosure | GITHUB_PAT scope over-permission | mitigate | Fine-grained PAT scoped to test-workshop-sandbox only; not a classic PAT |
| T-02-14 | Tampering | workspace-mcp [ASSUMED] package | mitigate | Task 1 human checkpoint reviews pypi.org + GitHub before uvx install; uvx pins version |
| T-02-15 | Elevation of Privilege | MCP subprocesses via npx with network access | mitigate | NoNewPrivileges=true on parent; PrivateTmp=true; each MCP has minimal env vars |
| T-02-16 | Information Disclosure | Google service account over-permission | mitigate | Restrict DWD scopes to minimum APIs; document in Task 1 instructions |
| T-02-SC | Tampering | npm/pip/cargo installs (context7, github, hostinger-api npx) | mitigate | All three are approved packages in legitimacy audit; crawl4ai avoids npm entirely via SSE url: |
</threat_model>

<verification>
```bash
bats tests/phase-02/mcps.bats
ssh root@31.97.130.253 "sudo -u uws bash -i -c 'hermes mcp list 2>/dev/null || journalctl -u uws-hermes -n 100 | grep -E github\|context7\|crawl4ai\|hostinger\|workspace'"
ssh root@31.97.130.253 "stat /etc/uws/gcp-service-account.json"
grep -v 'ghp_\|sk-\|token\|password\|key' hermes-config/config.yaml | grep -c 'mcp_servers\|github\|context7\|crawl4ai\|hostinger\|google'
```
</verification>

<success_criteria>
- REQ-ws-015: All 5 MCPs registered and reachable — hermes mcp list or journal confirms all 5 server names connected
- bats tests/phase-02/mcps.bats passes all assertions
- /etc/uws/gcp-service-account.json on VPS: exists, permissions 0640, owner root:uws
- hermes-config/config.yaml in git: contains all 5 mcp_servers entries, zero plaintext secrets
- crawl4ai SSE connection: `curl http://localhost:11235/health` returns 200 on VPS
</success_criteria>

<output>
Create `.planning/phases/02-hermes-deploy/02-04-SUMMARY.md` when done
</output>
