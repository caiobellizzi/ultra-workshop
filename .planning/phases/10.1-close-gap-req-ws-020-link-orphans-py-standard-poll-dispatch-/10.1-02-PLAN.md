---
phase: "10.1"
plan: "02"
type: execute
wave: 2
depends_on:
  - "10.1-01"
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-ws-035
  - REQ-ws-036
  - REQ-ws-037
  - REQ-ws-038
  - REQ-ws-039
  - REQ-ws-040
  - REQ-ws-041
  - REQ-ws-042
  - REQ-ws-043
  - REQ-ws-044
  - REQ-ws-045
  - REQ-ws-046
  - REQ-ws-047
  - REQ-ws-048
  - REQ-ws-049
  - REQ-ws-050

must_haves:
  truths:
    - "REQUIREMENTS.md contains formal entries for REQ-ws-035 through REQ-ws-042 under a Phase 8 section header"
    - "REQUIREMENTS.md contains formal entries for REQ-ws-043 through REQ-ws-050 under a Phase 9 section header"
    - "REQ-ws-035..042 status in the traceability table is 'Complete'"
    - "REQ-ws-043..050 status in the traceability table is 'Implemented' (not 'Complete')"
    - "REQ-ws-050 entry contains the worktree caveat text"
    - "Coverage count updated from 40/40 to 56/56"
    - "Traceability table contains rows for REQ-ws-035..042 (Phase 8 | Complete) and REQ-ws-043..050 (Phase 9 | Implemented)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "16 new REQ entries + updated coverage count + updated traceability table"
      contains: "REQ-ws-035"
  key_links:
    - from: ".planning/REQUIREMENTS.md §Phase 8"
      to: ".planning/phases/08-specialist-quality-uplift/08-01-PLAN.md"
      via: "requirement IDs match plan frontmatter"
      pattern: "REQ-ws-03[5-9]|REQ-ws-04[012]"
    - from: ".planning/REQUIREMENTS.md §Phase 9"
      to: ".planning/phases/09-advanced-agent-architecture/09-CONTEXT.md"
      via: "requirement IDs match phase success criteria B1-B7"
      pattern: "REQ-ws-04[3-9]|REQ-ws-050"
---

<objective>
Back-fills 16 missing formal requirement entries in REQUIREMENTS.md — REQ-ws-035..042 (Phase 8: Specialist Quality Uplift) and REQ-ws-043..050 (Phase 9: Advanced Agent Architecture). These IDs were declared "to be minted during execution/planning" in both phase roadmap entries but were never written.

Purpose: Closes the traceability debt identified in the v1.0 milestone audit. After this plan, every shipped feature has a formal requirement entry with an acceptance criterion. The coverage count moves from 40/40 to 56/56.

Output:
- .planning/REQUIREMENTS.md (modified: 2 new phase sections + 16 entries + updated traceability table + updated coverage count)
</objective>

<execution_context>
@/Users/caiobellizzi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caiobellizzi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@.planning/phases/10.1-close-gap-req-ws-020-link-orphans-py-standard-poll-dispatch-/10.1-CONTEXT.md

<interfaces>
<!-- Extracted from codebase. Executor reads these directly — no exploration needed. -->

From .planning/REQUIREMENTS.md current state (lines 130-201):
- Current last named section: "Brain ↔ Workshop Integration" (line ~128)
- Section format: blank line, ---, blank line, "### Phase N: Title", blank line, entries
- Entry format: "**REQ-ws-XXX** — [name]\n[description]\nAcceptance: [criterion]"
- One blank line between entries
- Coverage line (line 201): "**Coverage:** 40/40 requirements mapped. No orphans."
- Traceability table (lines ~152-200): markdown table with columns "| Requirement | Phase | Status |"
- Last rows before coverage line:
    | REQ-ws-051 | Phase 10 | Complete |
    through
    | REQ-ws-056 | Phase 10 | Complete |
  (gap: REQ-ws-035..042 and REQ-ws-043..050 missing from table entirely)

From .planning/phases/08-specialist-quality-uplift/08-01-PLAN.md (requirement slot comments):
- REQ-ws-035 — Soul rewrite: lean/behavioral discipline across all 5 specialists
- REQ-ws-036 — Coder execute-and-verify: build+test result in Diff JSON
- REQ-ws-037 — Reviewer pass-1 build/test gate before static checks
- REQ-ws-038 — Structured failure contract: [{file, problem, required_fix}]
- REQ-ws-039 — Two-pass review (spec+build | quality+security)
- REQ-ws-040 — Escalate-to-HITL on 2nd-retry-exhaustion (exit code 2)
- REQ-ws-041 — Planner and reviewer standard brain reads (repo conventions + ADRs / review rules)
- REQ-ws-042 — workshop-fix: fix push path + add requirements stage

From .planning/ROADMAP.md Phase 8 success criteria (source of truth for acceptance criteria):
  SC-1: All five skills/*-specialist/SKILL.md files use lean/behavioral discipline
  SC-2: aider_runner.py runs build/test after diff; Diff JSON includes build_passed, test_passed, output_tail
  SC-3: workshop_reviewer.py evaluates build/test before static checks; two-pass review; FAIL output is [{file,problem,required_fix}]
  SC-4: workshop_build.py exits code 2 (HITL) after 2nd retry exhaustion
  SC-5: planner-specialist always calls brain-query for repo conventions + ADRs; reviewer-specialist queries brain before both review passes
  SC-6: workshop-fix/SKILL.md uses workshop_continue.py push path and documents requirements stage

From .planning/ROADMAP.md Phase 9 success criteria:
  SC-1: brainstorm/conception HITL loop until explicit owner approval; produces scoped goal statement
  SC-2: each specialist has job description, monthly token budget tracked in brain ledger, auto-pauses at 100%
  SC-3: every pipeline event appended to vault/_system/workshop-audit/{task_id}.jsonl via brain ingest
  SC-4: 8 parallel AgentTool-isolated reviewers in wave; merge agent deduplicates and auto-fixes severity:low
  SC-5: AgentTool/SkillTool isolation policy documented; all code+review agents use isolated context
  SC-6: requirements-specialist queries brain for prior clarifications before triggering HITL

From .planning/phases/09-advanced-agent-architecture/09-CONTEXT.md decisions (B1-B7, D-01/D-02/D-07/D-17/D-19/D-20/D-21):
  B5 worktree caveat (D-20): worktree.py created but not wired to production review wave; reviewers run via ThreadPoolExecutor only; worktree integration deferred
  REQ-ws-049: review-roster.yaml registry (D-02)
  REQ-ws-050: worktree isolation (B5 / D-20) — has caveat

Phase 8 verification status: pass_with_caveats (08-VERIFICATION.md) → REQ-ws-035..042 status: Complete
Phase 9 verification status: no VERIFICATION.md written → REQ-ws-043..050 status: Implemented
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append REQ-ws-035..042 (Phase 8) entries to REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — understand current end of file, section format, existing entry format, traceability table structure, coverage line exact text)
    - .planning/phases/08-specialist-quality-uplift/08-01-PLAN.md (requirement slot names from the "## Requirement IDs" comment section — REQ-ws-035..042 names)
    - .planning/phases/08-specialist-quality-uplift/08-VERIFICATION.md (acceptance criteria / what was actually verified — source for Acceptance lines; status is pass_with_caveats)
    - .planning/ROADMAP.md §Phase 8 (SC-1..SC-6 — acceptance criteria source)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md.

    Step 1 — Insert Phase 8 section. Find the current traceability section marker (the line starting "## Traceability"). Insert BEFORE it (so requirement entries come before the table) a new section block with this structure:

    blank line
    ---
    blank line
    ### Phase 8: Specialist Quality Uplift
    blank line
    8 entries, one blank line between each, using format:
    **REQ-ws-XXX** — [name]
    [description derived from SC + plan slot]
    Acceptance: [criterion]

    The 8 entries to write (derive description from SC and plan slot comment; write acceptance criterion from corresponding SC):

    REQ-ws-035 — Lean/behavioral soul rewrite across all five specialists
    Description: All five `skills/*-specialist/SKILL.md` files rewritten with lean/behavioral discipline: explicit decision-rules, escalation behavior, no persona flavor. Covers `planner-specialist`, `coder-specialist`, `reviewer-specialist`, `requirements-specialist`, and `triage-specialist`.
    Acceptance: All five `skills/*-specialist/SKILL.md` files contain explicit decision-rules and escalation behavior sections with no persona flavor text; `hermes skill run <name> --dry-run` exits 0 for each (SC-1)

    REQ-ws-036 — Coder execute-and-verify: build/test gate in Diff JSON
    Description: `hermes-skills/aider_runner.py` runs the repo's build/test command after applying the diff. The returned `Diff` JSON includes `build_passed`, `test_passed`, and `output_tail` fields.
    Acceptance: `grep "build_passed" hermes-skills/aider_runner.py` → ≥1 match; `python -m pytest tests/phase-08/test_quality_uplift.py -x -q` passes (SC-2)

    REQ-ws-037 — Reviewer pass-1 build/test gate before static checks
    Description: `hermes-skills/workshop_reviewer.py` evaluates build/test result before static checks. Two-pass review implemented: pass 1 = spec+build, pass 2 = quality+security.
    Acceptance: `grep "build_passed" hermes-skills/workshop_reviewer.py` → ≥1 match; `python -m pytest tests/phase-06/test_workshop_reviewer.py -x` passes (SC-3 partial)

    REQ-ws-038 — Structured failure contract: [{file, problem, required_fix}]
    Description: All FAIL output from `workshop_reviewer.py` is structured as a list of `{file, problem, required_fix}` dicts. This format is fed back to the coder retry loop.
    Acceptance: `grep "required_fix" hermes-skills/workshop_reviewer.py` → ≥1 match; reviewer output schema validated in `tests/phase-08/test_quality_uplift.py` (SC-3)

    REQ-ws-039 — Two-pass review: spec+build | quality+security
    Description: `workshop_reviewer.py` implements two distinct review passes: first pass evaluates spec compliance and build results; second pass evaluates quality and security. Both passes run before the HITL gate.
    Acceptance: Two distinct review-pass invocations evident in `workshop_reviewer.py`; `python -m pytest tests/phase-08/test_quality_uplift.py -x` passes (SC-3)

    REQ-ws-040 — Escalate to HITL on 2nd retry exhaustion (exit code 2)
    Description: `hermes-skills/workshop_build.py` exits with code 2 (HITL escalation) after the 2nd failed retry instead of emitting a broken diff. The HITL path provides step-level context in the payload.
    Acceptance: `grep "sys.exit(2)\|exit(2)\|returncode.*2\|StageTimeoutForHITL" hermes-skills/workshop_build.py` → ≥1 match; `python -m pytest tests/phase-04/test_workshop_build.py -x` passes (SC-4)

    REQ-ws-041 — Planner and reviewer brain reads for repo conventions and ADRs
    Description: `planner-specialist/SKILL.md` always calls `brain-query` for repo conventions and ADRs before generating the plan. `reviewer-specialist/SKILL.md` queries brain before both review passes. These reads prevent stale-convention false blocks.
    Acceptance: `grep -i "brain.query\|brain_query\|brain-query" skills/planner-specialist/SKILL.md skills/reviewer-specialist/SKILL.md` → ≥2 matches; `bats tests/phase-07/planner-smoke.bats` passes (SC-5)

    REQ-ws-042 — workshop-fix: correct push path + requirements stage documented
    Description: `workshop-fix/SKILL.md` uses `workshop_continue.py` push path (not the old direct-push path). The requirements stage is documented in the skill so the fix pipeline mirrors the build pipeline structure.
    Acceptance: `grep "workshop_continue\|continue" skills/workshop-fix/SKILL.md` → ≥1 match; `grep "requirements" skills/workshop-fix/SKILL.md` → ≥1 match (SC-6)

    Step 2 — Do NOT modify the traceability table yet (that is Task 2).
    Step 3 — Do NOT modify the Coverage line yet (that is Task 2).
  </action>
  <verify>
    <automated>grep -c "REQ-ws-03[5-9]\|REQ-ws-04[012]" /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\*\*REQ-ws-035\*\*" .planning/REQUIREMENTS.md` → 1 match
    - `grep -c "^\*\*REQ-ws-042\*\*" .planning/REQUIREMENTS.md` → 1 match
    - `grep "Phase 8: Specialist Quality Uplift" .planning/REQUIREMENTS.md` → 1 match (section header exists)
    - All 8 entries (REQ-ws-035..042) present: `grep -c "REQ-ws-03[5-9]\|REQ-ws-04[012]" .planning/REQUIREMENTS.md` → ≥8 matches (some may appear in traceability table too — that is fine)
    - Each entry has an "Acceptance:" line: `grep -c "^Acceptance:" .planning/REQUIREMENTS.md` increased by 8 compared to original
    - REQUIREMENTS.md is valid markdown (no broken heading levels, no truncation)
  </acceptance_criteria>
  <done>Phase 8 section with REQ-ws-035..042 entries inserted before the Traceability section; each entry has name, description, and Acceptance criterion</done>
</task>

<task type="auto">
  <name>Task 2: Append REQ-ws-043..050 (Phase 9) + update traceability table + coverage count</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (re-read after Task 1 changes — especially the traceability table current state and Coverage line)
    - .planning/phases/09-advanced-agent-architecture/09-CONTEXT.md (decisions D-01..D-21 — source for REQ content; especially B1-B7 and worktree caveat from D-20)
    - .planning/ROADMAP.md §Phase 9 (SC-1..SC-6 — acceptance criteria source)
    - .planning/phases/09-advanced-agent-architecture/09-01-PLAN.md (REQ-ws-044, REQ-ws-045 slots)
    - .planning/phases/09-advanced-agent-architecture/09-02-PLAN.md (REQ-ws-046, REQ-ws-047 slots)
    - .planning/phases/09-advanced-agent-architecture/09-03-PLAN.md (REQ-ws-043, REQ-ws-046, REQ-ws-048 slots)
    - .planning/phases/09-advanced-agent-architecture/09-04-PLAN.md (REQ-ws-044, REQ-ws-047, REQ-ws-048 slots)
  </read_first>
  <action>
    Edit .planning/REQUIREMENTS.md.

    Step 1 — Insert Phase 9 section. Insert BEFORE the "## Traceability" line (after the Phase 8 section added in Task 1) a new section block:

    blank line
    ---
    blank line
    ### Phase 9: Advanced Agent Architecture
    blank line
    8 entries, one blank line between each:

    REQ-ws-043 — Brainstorm/conception HITL stage (pre-triage)
    Description: A brainstorm/conception stage runs as a pre-triage HITL conversational loop in `workshop_build.py`. The loop continues until explicit owner approval (no turn cap — owner amendment B1-A). On approval it produces a scoped goal statement fed to triage. Triggered via `--brainstorm` flag; direct `/build` skips brainstorm.
    Acceptance: `grep "\-\-brainstorm\|brainstorm_approved" hermes-skills/workshop_build.py` → ≥2 matches; `python -m pytest tests/phase-09/test_brainstorm_hitl.py -x` passes (SC-1)

    REQ-ws-044 — Agent personas: monthly token budgets + auto-pause
    Description: Each specialist (planner, coder, reviewer, requirements, triage, merge-agent, brainstorm) has a monthly token budget tracked in the brain ledger (`brain/_system/cost-ledger.md`). Budgets configured in `hermes-config/review-roster.yaml`. Auto-pause fires at 100% utilization with a Telegram alert at 80%.
    Acceptance: `grep "monthly\|budget\|auto.pause" hermes-config/review-roster.yaml` → ≥3 matches; `python -m pytest tests/phase-09/test_cost_budget.py -x` passes (SC-2)

    REQ-ws-045 — Immutable per-task audit log in the brain
    Description: Every pipeline event (wave start, specialist completion with token/cost + finding counts, merge completion with block decision) is appended to an append-only `vault/_system/workshop-audit/{task_id}.jsonl` via `brain_http.call_agent("ingest", ...)`.
    Acceptance: `python -m pytest tests/phase-09/test_audit_log.py -x` passes; `grep "workshop-audit\|append_audit" hermes-skills/workshop_build.py hermes-skills/workshop_reviewer.py` → ≥2 matches (SC-3)

    REQ-ws-046 — Parallel 8-scope review wave with dedup+autofix merge agent
    Description: `workshop_reviewer.py` dispatches 8 reviewers in parallel via `ThreadPoolExecutor`: `correctness`, `security`, `python`, `typescript`, `reactjs`, `qa`, `docs`, `config`. A merge agent (wave 3) deduplicates findings by `(file, line)`, auto-fixes `severity: low` items, and produces a `MergeReport`. `hermes-config/review-roster.yaml` governs routing rules and model aliases.
    Acceptance: `python -m pytest tests/phase-09/test_review_wave.py tests/phase-09/test_merge_agent.py -x` passes; `grep "ThreadPoolExecutor" hermes-skills/workshop_reviewer.py` → ≥1 match (SC-4)

    REQ-ws-047 — AgentTool/SkillTool isolation policy: documented and registry-enforced
    Description: `hermes-config/agent-isolation-policy.md` documents which roles run as isolated AgentTool dispatches (security, correctness, merge-agent) vs. shared-context SkillTool passes. Per-specialist isolation flag encoded in `review-roster.yaml`. Policy document serves as the single source of truth.
    Acceptance: `ls hermes-config/agent-isolation-policy.md` → file exists; `grep "isolated" hermes-config/review-roster.yaml` → ≥1 match; `python -m pytest tests/phase-09/ -k isolation -x 2>/dev/null || grep "isolation" hermes-config/agent-isolation-policy.md | wc -l` → ≥1 (SC-5)

    REQ-ws-048 — requirements-specialist brain pre-query before HITL
    Description: `requirements-specialist/SKILL.md` (or `workshop_build.py` requirements stage) queries the brain for `"prior clarifications for {repo_full_name}"` before triggering HITL. Prevents re-asking resolved ambiguities.
    Acceptance: `grep "brain.query\|brain_query\|prior.clarification" skills/requirements-specialist/SKILL.md` → ≥1 match; `python -m pytest tests/phase-09/test_requirements_brain.py -x` passes (SC-6)

    REQ-ws-049 — Review roster registry: hermes-config/review-roster.yaml
    Description: `hermes-config/review-roster.yaml` defines all 8 reviewer roles with routing rules (file-extension → specialist map), model aliases, isolation flag, and monthly budget cap. Adding a new reviewer requires one soul file and one registry line — no Python changes.
    Acceptance: `ls hermes-config/review-roster.yaml` → file exists; `grep -c "correctness\|security\|python\|typescript\|reactjs\|qa\|docs\|config" hermes-config/review-roster.yaml` → ≥8 matches

    REQ-ws-050 — git-worktree isolation for file-editing agent (deferred production wiring)
    Description: `workshop/worktree.py` created implementing git-worktree lifecycle (create, prune on success, retain on failure with max-age sweep). Designed for the merge agent's auto-fix step. **Caveat: `worktree.py` is not wired to the production review wave — reviewers run via `ThreadPoolExecutor` only. Worktree integration is deferred to a future phase.**
    Acceptance: `ls workshop/worktree.py` → file exists; `python -m py_compile workshop/worktree.py` exits 0; `python -m pytest tests/phase-09/test_worktree.py -x` passes (SC-4 partial — creation/pruning lifecycle tested)

    Step 2 — Update the traceability table. Find the traceability table rows. Insert new rows for Phase 8 AFTER the `| REQ-ws-034 | Phase 7 | Complete |` row and BEFORE the blank line that separates Phase 7 from Phase 10 rows (the existing gap between Phase 7 and Phase 10 in the table). If no natural insertion point exists, insert them before the Phase 10 block. Insert two groups:

    Group A (Phase 8, Complete):
    | REQ-ws-035 | Phase 8 | Complete |
    | REQ-ws-036 | Phase 8 | Complete |
    | REQ-ws-037 | Phase 8 | Complete |
    | REQ-ws-038 | Phase 8 | Complete |
    | REQ-ws-039 | Phase 8 | Complete |
    | REQ-ws-040 | Phase 8 | Complete |
    | REQ-ws-041 | Phase 8 | Complete |
    | REQ-ws-042 | Phase 8 | Complete |

    Group B (Phase 9, Implemented):
    | REQ-ws-043 | Phase 9 | Implemented |
    | REQ-ws-044 | Phase 9 | Implemented |
    | REQ-ws-045 | Phase 9 | Implemented |
    | REQ-ws-046 | Phase 9 | Implemented |
    | REQ-ws-047 | Phase 9 | Implemented |
    | REQ-ws-048 | Phase 9 | Implemented |
    | REQ-ws-049 | Phase 9 | Implemented |
    | REQ-ws-050 | Phase 9 | Implemented |

    Step 3 — Update the Coverage line. Replace:
    **Coverage:** 40/40 requirements mapped. No orphans.
    with:
    **Coverage:** 56/56 requirements mapped. No orphans.

    Step 4 — Do NOT modify any existing requirement entries (REQ-ws-001 through REQ-ws-056). Only insertions and the coverage count update.
  </action>
  <verify>
    <automated>grep -c "REQ-ws-04[3-9]\|REQ-ws-050" /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/REQUIREMENTS.md && grep "Coverage:" /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Phase 9: Advanced Agent Architecture" .planning/REQUIREMENTS.md` → 1 match
    - `grep -c "^\*\*REQ-ws-043\*\*" .planning/REQUIREMENTS.md` → 1 match
    - `grep -c "^\*\*REQ-ws-050\*\*" .planning/REQUIREMENTS.md` → 1 match
    - `grep "worktree.py.*not wired\|not wired.*worktree\|worktree.*deferred\|deferred" .planning/REQUIREMENTS.md` → ≥1 match (REQ-ws-050 caveat present)
    - Traceability table has REQ-ws-035..042 rows with "Complete": `grep "REQ-ws-035.*Phase 8.*Complete" .planning/REQUIREMENTS.md` → 1 match
    - Traceability table has REQ-ws-043..050 rows with "Implemented": `grep "REQ-ws-043.*Phase 9.*Implemented" .planning/REQUIREMENTS.md` → 1 match
    - `grep "REQ-ws-050.*Phase 9.*Implemented" .planning/REQUIREMENTS.md` → 1 match (NOT "Complete")
    - Coverage count updated: `grep "56/56" .planning/REQUIREMENTS.md` → 1 match
    - Old coverage count gone: `grep "40/40" .planning/REQUIREMENTS.md` → 0 matches
    - All 16 new entries have Acceptance lines: `grep -c "^Acceptance:" .planning/REQUIREMENTS.md` ≥ previously existing count + 16
  </acceptance_criteria>
  <done>All 16 REQ entries written; traceability table has Phase 8 (Complete) and Phase 9 (Implemented) rows; coverage updated to 56/56; REQ-ws-050 worktree caveat present; REQ-ws-043..050 status is Implemented not Complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → .planning/REQUIREMENTS.md | Pure documentation edit; no untrusted input, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-P2-01 | Tampering | .planning/REQUIREMENTS.md edit | accept | Documentation-only change; no executable code modified; git history provides audit trail |
| T-10.1-P2-02 | Information Disclosure | REQ acceptance criteria | accept | All acceptance criteria reference test commands or grep assertions already present in the public repo; no secrets disclosed |
| T-10.1-P2-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — documentation edit only |
</threat_model>

<verification>
Full verification after Plan 02 execution:

1. `grep -c "^\*\*REQ-ws-03[5-9]\*\*\|^\*\*REQ-ws-04[012]\*\*" .planning/REQUIREMENTS.md` → 8 matches
2. `grep -c "^\*\*REQ-ws-04[3-9]\*\*\|^\*\*REQ-ws-050\*\*" .planning/REQUIREMENTS.md` → 8 matches
3. `grep "56/56" .planning/REQUIREMENTS.md` → 1 match
4. `grep "40/40" .planning/REQUIREMENTS.md` → 0 matches
5. `grep "REQ-ws-035.*Phase 8.*Complete" .planning/REQUIREMENTS.md` → 1 match
6. `grep "REQ-ws-043.*Phase 9.*Implemented" .planning/REQUIREMENTS.md` → 1 match
7. `grep "REQ-ws-050.*Phase 9.*Implemented" .planning/REQUIREMENTS.md` → 1 match (NOT Complete)
8. `grep -i "worktree.*deferred\|not wired" .planning/REQUIREMENTS.md` → ≥1 match (REQ-ws-050 caveat)
9. `grep -c "^Acceptance:" .planning/REQUIREMENTS.md` → ≥32 (24 original + 16 new — exact count depends on originals; value must be ≥ old_count + 16)
10. `python -m pytest tests/phase-10.1/ -q` → all tests still pass (regression check for Plan 01 work)
</verification>

<success_criteria>
- REQUIREMENTS.md contains 16 new formal REQ entries (REQ-ws-035..050)
- Phase 8 entries (REQ-ws-035..042) have status Complete in traceability table
- Phase 9 entries (REQ-ws-043..050) have status Implemented in traceability table
- REQ-ws-050 contains the worktree caveat ("not wired to production review wave" or equivalent)
- Coverage count is 56/56 (was 40/40)
- No existing REQ entries modified (grep on any existing REQ-ws-001..034 and REQ-ws-051..056 content unchanged)
</success_criteria>

<output>
Create `.planning/phases/10.1-close-gap-req-ws-020-link-orphans-py-standard-poll-dispatch-/10.1-02-SUMMARY.md` when done
</output>
