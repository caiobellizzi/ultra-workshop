# Phase 10.1: Close gap: REQ-ws-020 — link_orphans.py + standard-poll dispatch fix + mint REQ-ws-035..050 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-31
**Phase:** 10.1-close-gap-req-ws-020-link-orphans-py-standard-poll-dispatch
**Areas discussed:** link_orphans.py scope, Dispatch interface fix, REQ-ws-035..050 minting

---

## link_orphans.py scope

| Option | Description | Selected |
|--------|-------------|----------|
| Dispatch stub + Telegram post | Reads queue entry, extracts orphan list from payload, posts plain-text list to Telegram. Zero vault writes. | ✓ |
| Full vault-link updater | Actively scans vault for unlinked notes and updates frontmatter/wikilinks. New capability. | |
| HITL-gated confirm flow | Brain flags orphans → Telegram confirm button → workshop writes links on approval. | |

**User's choice:** Dispatch stub + Telegram post

| Option | Description | Selected |
|--------|-------------|----------|
| From queue entry payload | Orphan list is in the stdin JSON entry itself — zero extra brain_http calls. | ✓ |
| Via brain_http query | Call brain_http to query vault for unlinked notes at dispatch time. | |

**User's choice:** From the queue entry payload itself

| Option | Description | Selected |
|--------|-------------|----------|
| Simple list post | Formatted text message, no buttons, no HITL confirm. | ✓ |
| HITL approval prompt | Inline Telegram keyboard (Acknowledge / Ignore) before ACKing the queue entry. | |

**User's choice:** Simple list post

**Notes:** Owner wants minimal implementation that satisfies V19 acceptance criterion. Full vault manipulation is deferred to a future phase.

---

## Dispatch interface fix

| Option | Description | Selected |
|--------|-------------|----------|
| Fix dispatch in cron_standard_poll | Change _dispatch_entry to pass CLI flags for build/fix verbs. workshop_build.py and workshop_fix.py untouched. | ✓ |
| Add stdin support to workshop_build/fix | Add a stdin-reading code path to workshop_build.py and workshop_fix.py when no args provided. | |
| Thin adapter scripts | Create workshop_build_dispatch.py and workshop_fix_dispatch.py as translators. | |

**User's choice:** Fix dispatch in cron_standard_poll

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, add main() to telegram_alert | Add main() that reads stdin JSON and calls send_alert(). Closes REQ-ws-021 standard-poll path. | ✓ |
| No, leave fast-poll as only path | Fast-poll already satisfies 30s SLA; adding main() is non-essential. | |

**User's choice:** Yes, add main() to telegram_alert

**Notes:** Owner prefers minimal-blast-radius fix: change only the dispatch caller, not the called scripts. telegram_alert.py main() is net-positive (closes a partial gap) with minimal risk.

---

## REQ-ws-035..050 minting

| Option | Description | Selected |
|--------|-------------|----------|
| Acceptance-criteria level | Each requirement gets name, description, verifiable acceptance criterion. Same format as existing. | ✓ |
| One-liner placeholders | REQ-ID + phase + one-sentence description + status = Complete. Fast but not verifiable. | |
| Full SPEC-quality | Detailed spec with edge cases and cross-refs. Overcomplicated for back-fill. | |

**User's choice:** Acceptance-criteria level

| Option | Description | Selected |
|--------|-------------|----------|
| Implemented (no VERIFICATION.md) | Mark Phase 9 reqs as Implemented — code ships, not formally verified. | ✓ |
| Complete (same as Phase 10) | Mark Complete — overstates Phase 9's unverified status. | |
| Pending verification | Mark Pending — defers resolution. | |

**User's choice:** Implemented (code ships, no VERIFICATION.md)

| Option | Description | Selected |
|--------|-------------|----------|
| Note the gap in the requirement | worktree.py B5 req minted as Implemented with explicit caveat: not wired to production. | ✓ |
| Leave worktree gap for future phase | Mint all Phase 9 reqs as Implemented without flagging the gap. | |
| Block minting on fixing the gap | Don't mint Phase 9 reqs until worktree.py is wired. | |

**User's choice:** Note the gap in the requirement itself

**Notes:** Owner wants honest traceability — Phase 9 is implemented but not verified, and the worktree gap is a real known limitation.

---

## Claude's Discretion

- Exact content of each of the 16 minted requirements (derived from Phase 8/9 ROADMAP success criteria and implemented code)
- Field name for orphan list in queue entry payload (`orphans` — to be confirmed in link_orphans.py docstring if not already defined)
- Exact slot assignment of REQ-ws-035..042 to Phase 8 success criteria
- Exact slot assignment of REQ-ws-043..050 to Phase 9 success criteria

## Deferred Ideas

- Full vault-link updater (link_orphans.py writing to vault) — Phase 11+ scope
- Wiring worktree.py to production review wave (Phase 9 B5 gap) — separate phase
- Phase 6 live Telegram acceptance smoke test — pending verification, separate effort
- REQ-ws-015 MCP registrations closure — user-deferred since Phase 2
- REQ-ws-022 trust_shared zero-consumers gap — partial, separate closure
