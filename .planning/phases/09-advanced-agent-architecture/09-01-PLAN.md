---
phase: "09-advanced-agent-architecture"
plan: "09-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - workshop/types.py
  - workshop/cost.py
  - workshop/ledger.py
  - hermes-config/review-roster.yaml
  - tests/phase-09/__init__.py
  - tests/phase-09/test_cost_budget.py
  - tests/phase-09/test_audit_log.py
autonomous: true
requirements:
  - REQ-ws-044
  - REQ-ws-045

must_haves:
  truths:
    - "ReviewFinding, WaveReport, MergeReport Pydantic models exist in workshop/types.py with severity Literal['Critical','Important','Minor'] and case-normalizing field_validator"
    - "workshop/cost.py has ROLE_MONTHLY_CAPS, RoleBudgetExhausted, RoleBudgetWarning, get_role_monthly_spend, record_role_cost, check_role_budget"
    - "workshop/ledger.py has append_audit() that posts to brain ingest in a daemon thread (non-blocking fire-and-forget)"
    - "hermes-config/review-roster.yaml lists 8 reviewers with role, model_alias, isolation, file_patterns, monthly_budget_cents, fallback_model_alias"
    - "pytest tests/phase-09/ exits 0 for SC-2 and SC-3 stub tests"
  artifacts:
    - path: "workshop/types.py"
      provides: "ReviewFinding, WaveReport, MergeReport models"
      contains: "severity.*Literal.*Critical.*Important.*Minor"
    - path: "workshop/cost.py"
      provides: "per-role budget tracking layer"
      exports: ["get_role_monthly_spend", "record_role_cost", "check_role_budget", "RoleBudgetExhausted", "RoleBudgetWarning", "ROLE_MONTHLY_CAPS"]
    - path: "workshop/ledger.py"
      provides: "append_audit non-blocking brain write"
      exports: ["append_audit"]
    - path: "hermes-config/review-roster.yaml"
      provides: "review roster config with 8 entries"
      contains: "correctness"
    - path: "tests/phase-09/test_cost_budget.py"
      provides: "SC-2 pytest tests"
    - path: "tests/phase-09/test_audit_log.py"
      provides: "SC-3 pytest tests"
  key_links:
    - from: "workshop/cost.py"
      to: "hermes-skills/brain_http.py"
      via: "importlib dynamic load at BRAIN_HTTP_FILE path"
      pattern: "_brain_http.call_agent.*curator.*role="
    - from: "workshop/ledger.py"
      to: "hermes-skills/brain_http.py"
      via: "threading.Thread daemon"
      pattern: "append_audit.*ingest.*workshop-audit"
---

<objective>
Lay the data-model and infrastructure foundation that all Wave 2 plans depend on. Creates the new Pydantic types for the review wave, the per-role budget layer on top of the existing global circuit breaker, the audit-log append function, and the review-roster config file.

Purpose: Wave 2 plans (09-02 parallel reviewer wave, 09-03 brainstorm stage) import from these foundations — they must exist first with stable interfaces.
Output: Extended workshop/types.py, workshop/cost.py, workshop/ledger.py, hermes-config/review-roster.yaml, tests/phase-09/ stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-advanced-agent-architecture/09-CONTEXT.md
@.planning/phases/09-advanced-agent-architecture/09-RESEARCH.md
</context>

<interfaces>
From workshop/types.py (all existing models must remain intact):
  class PlanStep(BaseModel)
  class Plan(BaseModel)
  class FileChange(BaseModel)
  class Diff(BaseModel)
  class ReviewIssue(BaseModel)  — has file, problem, required_fix fields
  class Review(BaseModel)       — has passed, feedback, blocking_issues fields
  class ClarificationQuestion(BaseModel)
  class ClarificationRequest(BaseModel)
  class Issue(BaseModel)
  class IngestResult(BaseModel)
  # imports at top: from pydantic import AliasChoices, BaseModel, Field, field_validator
  # imports at top: from typing import Literal

From workshop/cost.py (all existing exports must stay unchanged):
  LEDGER_PATH = Path("/srv/second-brain/_system/cost-ledger.md")
  WARN_THRESHOLD = 18.0   # daily USD
  HARD_THRESHOLD = 20.0   # daily USD
  class BudgetExhausted(Exception)
  class BudgetWarning(Exception)
  def get_daily_spend() -> float
  def record_cost(task_id: str, amount: float, model: str) -> None
  def check_circuit_breaker(mode: str = "interactive") -> None
  # brain_http loaded via importlib: _BRAIN_HTTP_FILE = Path("/opt/ultra-workshop/hermes-skills/brain_http.py")

From workshop/ledger.py (all existing exports must stay unchanged):
  def validate_task_id(task_id: str) -> None
  def task_dir(task_id: str) -> Path
  def append_progress(task_id: str, event: str, data: dict) -> None
  def write_task_ledger(task_id: str, goal: str, status: str, pr_url: str = "") -> None

From hermes-skills/brain_http.py:
  def call_agent(agent_id: str, message: str, user_id: str = "workshop") -> dict
  # CRITICAL: uses data={} NOT json={} internally — callers only pass (agent_id, message)
  # DEFAULT_TIMEOUT = 60.0

From tests/phase-08/test_quality_uplift.py (_load_module pattern to replicate):
  def _load_module(module_name: str, path: str): ...
  workshop_build = _load_module("workshop_build_phase08", "hermes-skills/workshop_build.py")
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Create SC-2 and SC-3 test stubs (RED)</name>
  <files>tests/phase-09/__init__.py, tests/phase-09/test_cost_budget.py, tests/phase-09/test_audit_log.py</files>
  <read_first>
    tests/phase-08/test_quality_uplift.py
    workshop/cost.py
    workshop/ledger.py
    hermes-skills/brain_http.py
  </read_first>
  <behavior>
    SC-2 tests in test_cost_budget.py — these must FAIL (RED) on first run:
    - test_role_budget_caps_defined: from workshop.cost import ROLE_MONTHLY_CAPS; assert ROLE_MONTHLY_CAPS["security"] == 4000 and ROLE_MONTHLY_CAPS["correctness"] == 3000
    - test_check_role_budget_raises_exhausted: mock get_role_monthly_spend to return cap value; assert RoleBudgetExhausted raised
    - test_check_role_budget_raises_warning_at_80pct: mock spend at 80% of cap; assert RoleBudgetWarning raised
    - test_record_role_cost_calls_curator: mock _brain_http; call record_role_cost("security", 0.05, "reviewer-model"); assert call_agent called with "curator" and message containing "role=security"

    SC-3 tests in test_audit_log.py — these must FAIL (RED) on first run:
    - test_append_audit_calls_ingest: patch brain_http.call_agent; call append_audit("ws-abc123", "wave_start", {"role": "correctness"}); assert call_agent invoked with agent_id="ingest"
    - test_append_audit_message_contains_task_id: assert ingest message contains "workshop-audit/ws-abc123"
    - test_append_audit_is_nonblocking: use threading.Event + slow mock; assert append_audit returns before mock completes
  </behavior>
  <action>
    Create tests/phase-09/__init__.py as an empty file.

    Create tests/phase-09/test_cost_budget.py: copy the _load_module helper from tests/phase-08/test_quality_uplift.py. Import pytest, unittest.mock.patch, unittest.mock.MagicMock. Attempt to import ROLE_MONTHLY_CAPS, RoleBudgetExhausted, RoleBudgetWarning, check_role_budget, get_role_monthly_spend, record_role_cost from workshop.cost — these do not exist yet so the import itself will fail, making tests fail RED. Use try/except ImportError at module level to let the file load and mark tests as xfail or skip with reason "not yet implemented" — OR simply let the ImportError propagate (pytest will report collection errors as ERRORS, which counts as RED for TDD purposes).

    Create tests/phase-09/test_audit_log.py: import pytest, unittest.mock, threading, time. Attempt to import append_audit from workshop.ledger. Write the three tests listed above. Same RED-on-first-run approach.

    Do not create conftest.py. Do not alter existing test structure.
  </action>
  <verify>
    <automated>cd /opt/ultra-workshop && python -m pytest tests/phase-09/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>tests/phase-09/__init__.py, test_cost_budget.py, test_audit_log.py exist. pytest reports FAILED or ERROR (not a Python syntax error in the test file itself). Existing suite stays green: python -m pytest tests/ --ignore=tests/phase-09/ -x -q exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ReviewFinding/WaveReport/MergeReport to workshop/types.py + create review-roster.yaml</name>
  <files>workshop/types.py, hermes-config/review-roster.yaml</files>
  <read_first>
    workshop/types.py
    hermes-config/config.yaml
    .planning/phases/09-advanced-agent-architecture/09-CONTEXT.md
  </read_first>
  <behavior>
    ReviewFinding: file: str, line: int | None = None, problem: str, required_fix: str, severity: Literal["Critical", "Important", "Minor"]. field_validator on severity (mode="before") normalizes: "CRITICAL"/"HIGH" → "Critical", "IMPORTANT"/"MEDIUM" → "Important", "MINOR"/"LOW" → "Minor" (case-insensitive). Unrecognized values raise ValueError.

    WaveReport: role: str, passed: bool, findings: list[ReviewFinding] = [], tokens_used: int = 0, cost_cents: float = 0.0.

    MergeReport: block_push: bool, critical_findings: list[ReviewFinding] = [], important_findings: list[ReviewFinding] = [], auto_fixed: list[ReviewFinding] = [], summary: str = "".

    review-roster.yaml schema: top-level key "reviewers" containing a list. Each entry: role (str), model_alias (str), isolation (bool), file_patterns (list[str]), monthly_budget_cents (int), fallback_model_alias (str or null). Always-on roles (correctness, security) have file_patterns: [] and isolation: true. Stack reviewers have isolation: false and fallback_model_alias: "cheap-fast".
  </behavior>
  <action>
    Edit workshop/types.py: append three new model classes after the existing IngestResult class. Do not modify any existing classes. Import Literal is already at the top; no new imports needed for the models themselves.

    Create hermes-config/review-roster.yaml with 8 reviewer entries under top-level key "reviewers". Add a comment block at the very top of the file: "# Agent isolation policy enforcement: see hermes-config/agent-isolation-policy.md\n# This file is the single source of truth for reviewer isolation and monthly budget caps.\n# security + correctness are always-on (isolation: true); all others are diff-gated (isolation: false).\n# Never remove security or correctness from this file — check_role_budget for security raises BLOCK on exhaustion."

    Entries with their values:
    - correctness: model_alias=reviewer-model, isolation=true, file_patterns=[], monthly_budget_cents=3000, fallback_model_alias=null
    - security: model_alias=reviewer-model, isolation=true, file_patterns=[], monthly_budget_cents=4000, fallback_model_alias=null
    - python: model_alias=reviewer-model, isolation=false, file_patterns=[".py"], monthly_budget_cents=2000, fallback_model_alias=cheap-fast
    - typescript: model_alias=reviewer-model, isolation=false, file_patterns=[".ts", ".tsx"], monthly_budget_cents=2000, fallback_model_alias=cheap-fast
    - reactjs: model_alias=reviewer-model, isolation=false, file_patterns=[".tsx", ".jsx"], monthly_budget_cents=2000, fallback_model_alias=cheap-fast
    - qa: model_alias=reviewer-model, isolation=false, file_patterns=["test", "spec"], monthly_budget_cents=1000, fallback_model_alias=cheap-fast
    - docs: model_alias=reviewer-model, isolation=false, file_patterns=[".md", ".rst"], monthly_budget_cents=1000, fallback_model_alias=cheap-fast
    - config: model_alias=reviewer-model, isolation=false, file_patterns=["deploy", ".env", "secrets", "config"], monthly_budget_cents=1000, fallback_model_alias=cheap-fast
  </action>
  <verify>
    <automated>cd /opt/ultra-workshop && python -c "from workshop.types import ReviewFinding, WaveReport, MergeReport; r = ReviewFinding(file='f.py', line=1, problem='p', required_fix='fix', severity='HIGH'); assert r.severity == 'Critical', r.severity; print('types OK')" && python -c "import yaml; d = yaml.safe_load(open('hermes-config/review-roster.yaml')); roles = [e['role'] for e in d['reviewers']]; assert 'security' in roles and 'correctness' in roles and len(d['reviewers']) == 8; print('roster OK')"</automated>
  </verify>
  <done>ReviewFinding severity normalization: "HIGH"→"Critical", "low"→"Minor" confirmed. review-roster.yaml has exactly 8 entries. correctness and security have isolation: true. All existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend workshop/cost.py with per-role budget + workshop/ledger.py with append_audit (GREEN)</name>
  <files>workshop/cost.py, workshop/ledger.py</files>
  <read_first>
    workshop/cost.py
    workshop/ledger.py
    hermes-skills/brain_http.py
    tests/phase-09/test_cost_budget.py
    tests/phase-09/test_audit_log.py
  </read_first>
  <behavior>
    cost.py additions (all appended after existing check_circuit_breaker — nothing above modified):
    - ROLE_MONTHLY_CAPS dict with values from D-12 in USD-cents (security=4000, correctness=3000, python=2000, typescript=2000, reactjs=2000, qa=1000, docs=1000, config=1000, merge=1500, brainstorm=2000, pipeline_pool=3000)
    - RoleBudgetExhausted(Exception)
    - RoleBudgetWarning(Exception)
    - get_role_monthly_spend(role: str) -> float: parses LEDGER_PATH, filters lines containing both role and current month prefix (date.today().strftime("%Y-%m")), sums "amount:" values
    - record_role_cost(role: str, amount: float, model: str) -> None: calls _brain_http.call_agent("curator", f"record-cost&amount={amount:.6f}&role={role}&source=workshop&model={model}") inside try/except, non-blocking on failure
    - check_role_budget(role: str) -> None: if role not in ROLE_MONTHLY_CAPS: log and return. spend = get_role_monthly_spend(role). If spend >= cap: raise RoleBudgetExhausted. If spend >= cap * 0.8: raise RoleBudgetWarning.

    ledger.py additions (appended after write_task_ledger):
    - Load _brain_http via same importlib pattern as cost.py (path: Path("/opt/ultra-workshop/hermes-skills/brain_http.py"))
    - append_audit(task_id: str, event: str, data: dict) -> None: builds payload dict with ts=datetime.now(timezone.utc).isoformat(), task_id, event, plus all data keys. message = f"workshop-audit/{task_id}: " + json.dumps(payload). Launches threading.Thread(target=_brain_http.call_agent, args=("ingest", message), daemon=True).start() inside try/except that catches all exceptions silently (fail-open). Add import threading at top if not present.
  </behavior>
  <action>
    Edit workshop/cost.py: append ROLE_MONTHLY_CAPS and the five new symbols after the last line of check_circuit_breaker. Verify the module-level _brain_http object (loaded at import time) is reused — do not create a second importlib load block.

    Edit workshop/ledger.py: add "import threading" to the imports block at top if not already there (check first). Add the _brain_http importlib loading block after the existing imports. Append append_audit function after write_task_ledger.

    After both edits, run the phase-09 tests. They should transition from RED to GREEN. If any test is still failing, fix the implementation (not the test).
  </action>
  <verify>
    <automated>cd /opt/ultra-workshop && python -m pytest tests/phase-09/ -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>pytest tests/phase-09/ exits 0 (all SC-2 and SC-3 tests GREEN). python -m pytest tests/ -x -q exits 0 (no regressions). python -c "from workshop.cost import ROLE_MONTHLY_CAPS; assert ROLE_MONTHLY_CAPS['security'] == 4000" exits 0. python -c "from workshop.ledger import append_audit" exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workshop process → Brain HTTP | Cost and audit payloads sent to brain curator/ingest at 127.0.0.1:7000 — loopback only, no external exposure |
| review-roster.yaml → wave dispatcher | YAML config drives reviewer selection and budget — malformed file could silently disable security reviewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | hermes-config/review-roster.yaml | mitigate | Wave dispatcher (09-02) must raise if file missing or unparseable; never return empty roster; always-on fallback hardcoded for correctness+security |
| T-09-01-02 | Information Disclosure | append_audit via brain ingest | accept | Sent to loopback 127.0.0.1:7000 only; daemon thread means failure is silent but not a leak vector |
| T-09-01-03 | Denial of Service | RoleBudgetExhausted blocking security reviewer | mitigate | Per D-09: security exhausted → BLOCK to HITL, not skip-and-proceed; enforced in check_role_budget |
| T-09-01-04 | Spoofing | role tag injection in record_role_cost message | mitigate | check_role_budget validates role against ROLE_MONTHLY_CAPS keys; record_role_cost should also validate role before constructing the curator message string |
| T-09-01-SC | Tampering | dependency installs | mitigate | No new pip installs in this plan — pure Python stdlib + existing pydantic/yaml already in requirements |
</threat_model>

<verification>
Run from project root /opt/ultra-workshop:

1. python -m pytest tests/phase-09/ -x -q  (phase-09 tests must exit 0)
2. python -m pytest tests/ -x -q           (full suite must stay green)
3. python -c "from workshop.types import ReviewFinding, WaveReport, MergeReport; r = ReviewFinding(file='a.py', line=1, problem='p', required_fix='fix', severity='HIGH'); assert r.severity == 'Critical'"
4. python -c "from workshop.cost import ROLE_MONTHLY_CAPS, RoleBudgetExhausted, check_role_budget; assert ROLE_MONTHLY_CAPS['security'] == 4000"
5. python -c "from workshop.ledger import append_audit"
6. python -c "import yaml; d = yaml.safe_load(open('hermes-config/review-roster.yaml')); assert len(d['reviewers']) == 8"
</verification>

<success_criteria>
- ReviewFinding.severity normalizes "HIGH"→"Critical", "low"→"Minor" via Pydantic field_validator
- RoleBudgetExhausted raised when role spend >= monthly cap; RoleBudgetWarning at 80% of cap
- append_audit() fires-and-forgets in a daemon thread (returns before Brain call completes)
- review-roster.yaml has 8 entries; correctness + security have isolation: true and no fallback
- All 45+ existing pytest tests still pass
</success_criteria>

<output>
Create `.planning/phases/09-advanced-agent-architecture/09-01-SUMMARY.md` when done
</output>
