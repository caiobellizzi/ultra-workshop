---
phase: "09-advanced-agent-architecture"
plan: "09-04"
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - workshop/stage_policy.py
  - skills/brainstorm-specialist/SKILL.md
  - skills/requirements-specialist/SKILL.md
  - skills/planner-specialist/SKILL.md
autonomous: false
requirements:
  - REQ-ws-044
  - REQ-ws-047
  - REQ-ws-048

must_haves:
  truths:
    - "workshop/stage_policy.py has 'brainstorm' stage policy entry and MODEL_ALIASES for all 8 reviewer roles + merge"
    - "skills/brainstorm-specialist/SKILL.md exists with Socratic loop discipline and no-turn-cap behavior"
    - "skills/requirements-specialist/SKILL.md has brain pre-query behavior in its Discipline section"
    - "End-to-end smoke: python -m pytest tests/phase-09/ exits 0 and python -m pytest tests/ exits 0"
    - "Human verifies /brainstorm dry-run and /build --dry-run complete without Python errors"
  artifacts:
    - path: "workshop/stage_policy.py"
      provides: "stage policy for brainstorm + reviewer model aliases"
      contains: "brainstorm"
    - path: "skills/brainstorm-specialist/SKILL.md"
      provides: "brainstorm soul with Socratic loop + no-turn-cap"
      contains: "brainstorm_approved"
    - path: "skills/requirements-specialist/SKILL.md"
      provides: "updated requirements soul with brain pre-query behavior"
      contains: "prior clarifications"
  key_links:
    - from: "workshop/stage_policy.py"
      to: "hermes-skills/workshop_build.py"
      via: "stage_model_alias('brainstorm') lookup"
      pattern: "MODEL_ALIASES.*brainstorm"
    - from: "skills/brainstorm-specialist/SKILL.md"
      to: "hermes-skills/workshop_brainstorm.py"
      via: "hermes-skill-run.sh dispatch"
      pattern: "brainstorm-specialist"
---

<objective>
Final integration pass: wire the stage policy for the brainstorm stage, add model aliases for all reviewer roles, create the brainstorm soul, and update the requirements and planner souls with richer persona framing from B2. Ends with a human-verify smoke test to confirm the end-to-end pipeline wiring.

Purpose: After Wave 2 implements the mechanics, this plan ensures all policy configs are complete and the human verifies the new pipeline runs without errors.
Output: Updated stage_policy.py, brainstorm-specialist SKILL.md, updated requirements-specialist SKILL.md, human-verified smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-advanced-agent-architecture/09-CONTEXT.md
@.planning/phases/09-advanced-agent-architecture/09-RESEARCH.md
@.planning/phases/09-advanced-agent-architecture/09-01-SUMMARY.md
@.planning/phases/09-advanced-agent-architecture/09-02-SUMMARY.md
@.planning/phases/09-advanced-agent-architecture/09-03-SUMMARY.md
</context>

<interfaces>
From workshop/stage_policy.py (current structure):
  MODEL_ALIASES: dict[str, str]  — maps stage names to LiteLLM model aliases
  # Stage policy dict: per-stage timeout, auto_retries, hitl_on_timeout
  # stage_model_alias(stage: str) -> str  OR  MODEL_ALIASES.get(stage, default)
  # Read the actual structure before editing — may be a dict of dicts or flat dict

From hermes-skills/workshop_build.py (after 09-03 changes):
  _STAGE_INDEX = {"brainstorm": 0, "triage": 1, "requirements": 2, "planner": 3, "coder": 4, "reviewer": 5, "approval": 6}
  stage_model_alias(stage_name) — reads MODEL_ALIASES from stage_policy

From skills/reviewer-specialist/SKILL.md (soul format reference):
  # Lean/behavioral discipline; no prose personality for gate roles
  # B2: richer personas for requirements + planner only (judgment-heavy)

From skills/requirements-specialist/SKILL.md (current — to extend):
  # Current content: read before modifying to understand existing discipline

From hermes-config/review-roster.yaml (created in 09-01):
  # 8 reviewer model aliases are all "reviewer-model" — stage_policy MODEL_ALIASES must match
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend workshop/stage_policy.py for brainstorm stage + reviewer model aliases</name>
  <files>workshop/stage_policy.py</files>
  <read_first>
    workshop/stage_policy.py
    hermes-config/review-roster.yaml
    hermes-skills/workshop_build.py
  </read_first>
  <action>
    Read workshop/stage_policy.py in full to understand its current structure (dict-of-dicts vs flat, key names).

    Add "brainstorm" to the stage policy dict with: timeout=300 (5 minutes — brainstorm is conversational, needs more time than triage), auto_retries=0 (brainstorm never auto-retries — it is interactive), hitl_on_timeout=True (timeout during brainstorm → escalate to HITL, not fail hard).

    Add MODEL_ALIASES entries for all reviewer roles. Align with review-roster.yaml model_alias values: all 8 reviewers use "reviewer-model". Add entries for: "correctness-reviewer", "security-reviewer", "python-reviewer", "typescript-reviewer", "reactjs-reviewer", "qa-reviewer", "docs-reviewer", "config-reviewer". Also add "merge-agent" with alias "reviewer-model". Also add "brainstorm" (or "brainstorm-specialist") with alias "default-worker" (the brainstorm soul is conversational, not code-analysis heavy).

    If the stage policy has a separate per-stage timeout/retries dict from MODEL_ALIASES, add brainstorm to both structures. Do not modify any existing stage entries.
  </action>
  <verify>
    <automated>cd /opt/ultra-workshop && python -c "from workshop.stage_policy import MODEL_ALIASES; assert 'correctness-reviewer' in MODEL_ALIASES or 'correctness_reviewer' in MODEL_ALIASES; assert 'brainstorm' in MODEL_ALIASES or 'brainstorm-specialist' in MODEL_ALIASES; print('stage_policy OK')" && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>stage_policy.py imports cleanly. MODEL_ALIASES has entries for all 8 reviewer roles + merge + brainstorm. brainstorm stage has timeout=300, auto_retries=0, hitl_on_timeout=True. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create brainstorm-specialist SKILL.md + update requirements-specialist and planner-specialist souls</name>
  <files>skills/brainstorm-specialist/SKILL.md, skills/requirements-specialist/SKILL.md, skills/planner-specialist/SKILL.md</files>
  <read_first>
    skills/requirements-specialist/SKILL.md
    skills/planner-specialist/SKILL.md
    skills/reviewer-specialist/SKILL.md
    .planning/phases/09-advanced-agent-architecture/09-CONTEXT.md
  </read_first>
  <action>
    Create skills/brainstorm-specialist/SKILL.md: Richer persona framing (B2) for this judgment-heavy role. The soul IS the brainstorm agent — it conducts the Socratic loop.

    Frontmatter: name: brainstorm-specialist, description: "Socratic conception loop before pipeline triage — explores problem space and produces a scoped goal statement on owner approval", version: 1.0.0, tags: [workshop, brainstorm, hitl].

    Sections:
    - "## Brainstorm Specialist": role description. This agent acts as a Socratic interlocutor — its job is to help the owner arrive at a precise, scoped goal statement before any code is written. It does NOT produce code, plans, or implementation details. It produces clarity.
    - "## Persona": Job title: "Product Conception Analyst". Responsibilities: explore the problem space, surface assumptions, identify scope creep, propose alternatives, and produce a scoped goal statement. Reports to: owner (human). Monthly budget: 2000 USD-cents.
    - "## Discipline": Key rules — ask one focused question at a time. Never auto-proceed to triage; wait for explicit owner approval. No turn cap (B1-A — loop runs until owner says "approve" or equivalent signal). Questions must be concrete (not "tell me more") — explore: Is this the right thing to build? Does it conflict with existing decisions? What's the minimum scope that delivers value? Prohibited: never output code, plans, file paths, or implementation details during the brainstorm loop.
    - "## Behavior": Numbered steps: 1) Receive task description. 2) Ask the first scoping question. 3) Loop: receive owner response, determine if goal is sufficiently scoped (owner signals approval) or more clarification needed. 4) On approval: emit the scoped goal statement JSON. Never exit the loop without explicit approval.
    - "## Exit Signal": Describe what constitutes approval — owner says "approve", "looks good", "yes", or "go ahead" (case-insensitive). On approval, call workshop_brainstorm.py approval handler which sets state["brainstorm_approved"] = True and emits the goal_statement.
    - "## Output Schema": {"approved": true, "goal_statement": "string — one paragraph scoped goal statement"}
    - "## Dry-run Behavior": emit {"approved": true, "goal_statement": "dry-run goal: implement X with Y constraints"}

    Update skills/requirements-specialist/SKILL.md: Add to the existing "## Discipline" section (after the current last rule, do not remove existing rules):
    - "Before evaluating ambiguity, query Brain for prior clarifications: call brain_http.call_agent('query', f'prior clarifications for {repo_full_name}'). If the brain returns relevant prior clarifications for this repository, treat them as resolved context — do not re-ask questions that were already answered. Fail-open: if Brain is unreachable, log and continue."
    Also add a "## Persona" section before "## Discipline" (if one does not already exist): "Job title: Requirements Analyst. Responsibilities: extract unambiguous, plannable requirements from task descriptions. Reports to: owner (via HITL). Monthly budget: included in pipeline-pool (3000 USD-cents/month shared with triage and planner)."

    Update skills/planner-specialist/SKILL.md: Add a "## Persona" section (if one does not already exist): "Job title: Implementation Planner. Responsibilities: decompose approved requirements into deterministic, executable step sequences. Reports to: owner (via HITL for plan approval). Monthly budget: included in pipeline-pool."
    Do not modify existing planner discipline or behavior sections — persona addition only.
  </action>
  <verify>
    <automated>cd /opt/ultra-workshop && test -f skills/brainstorm-specialist/SKILL.md && grep -c "brainstorm_approved\|goal_statement" skills/brainstorm-specialist/SKILL.md && grep -c "prior clarifications" skills/requirements-specialist/SKILL.md && echo "souls OK"</automated>
  </verify>
  <done>brainstorm-specialist/SKILL.md exists, contains "brainstorm_approved" and "goal_statement". requirements-specialist/SKILL.md now contains "prior clarifications" in Discipline. planner-specialist/SKILL.md has Persona section. No existing soul content was removed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end smoke verification</name>
  <what-built>
    All Phase 9 changes are wired:
    - workshop_build.py has brainstorm stage (_STAGE_INDEX["brainstorm"] == 0)
    - Parallel review wave replaces single reviewer call
    - requirements_gate.py has brain pre-query
    - All 9 reviewer + merge-agent SKILL.md souls exist
    - workshop_brainstorm.py and workshop_merge_agent.py CLI shims exist
    - All phase-09 pytest tests pass
  </what-built>
  <how-to-verify>
    Run these commands and confirm each passes without Python tracebacks:

    1. Full test suite:
       cd /opt/ultra-workshop && python -m pytest tests/ -x -q
       Expected: all tests pass (no failures, no errors)

    2. Stage index check:
       python -c "exec(open('hermes-skills/workshop_build.py').read().split('def main')[0]); print(_STAGE_INDEX)"
       Expected: {'brainstorm': 0, 'triage': 1, 'requirements': 2, 'planner': 3, 'coder': 4, 'reviewer': 5, 'approval': 6}

    3. Brainstorm dry-run:
       python hermes-skills/workshop_brainstorm.py --dry-run --query '{"task": "add a login page"}'
       Expected: JSON output with approved: true and goal_statement field (no Python traceback)

    4. Merge agent dry-run:
       python hermes-skills/workshop_merge_agent.py --dry-run --query '{}'
       Expected: JSON output with block_push field (no Python traceback)

    5. Reviewer shim dry-run:
       python hermes-skills/workshop_reviewer.py --dry-run --query '{"task_id": "ws-test", "diff": {}, "plan": {}}'
       Expected: some JSON output (no traceback — exact schema may vary)

    6. Soul presence check:
       for soul in correctness-reviewer security-reviewer python-reviewer typescript-reviewer reactjs-reviewer qa-reviewer docs-reviewer config-reviewer merge-agent brainstorm-specialist; do test -f "skills/$soul/SKILL.md" && echo "OK $soul" || echo "MISSING $soul"; done

    7. Policy doc:
       test -f hermes-config/agent-isolation-policy.md && echo "policy OK"
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass without errors. Describe any failures with the command output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (human) → brainstorm loop | Owner input drives goal_statement — no sanitization needed (owner is trusted), but goal_statement must not contain injected shell commands (it is passed to triage as a string) |
| brainstorm soul → pipeline state | goal_statement written to task state and passed to triage — must be bounded in length to avoid state bloat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering | goal_statement injection into triage query | mitigate | goal_statement is passed as a JSON string value (not interpolated into shell command); Pydantic model bounds the type |
| T-09-04-02 | Denial of Service | brainstorm loop running indefinitely | accept | D-18 explicitly removes turn cap by owner decision (B1-A); owner controls exit via approval signal; HITL watchdog (existing startup-hitl-scan.py pattern) protects against complete hangs |
| T-09-04-03 | Tampering | stage_policy brainstorm timeout too short, cutting off deliberation | mitigate | timeout=300 (5 min) with hitl_on_timeout=True — timeout escalates to HITL, not hard-fail; owner can resume |
| T-09-04-SC | Tampering | dependency installs | mitigate | No new pip installs in this plan — all stdlib |
</threat_model>

<verification>
Run from project root /opt/ultra-workshop:

1. python -m pytest tests/ -x -q  (full suite must pass)
2. python -m pytest tests/phase-09/ -v  (all phase-09 tests detailed output)
3. grep -c "brainstorm" workshop/stage_policy.py  (should be >= 1)
4. grep -rn "prior clarifications" skills/requirements-specialist/SKILL.md
5. Human verification per Task 3 checkpoint above
</verification>

<success_criteria>
- python -m pytest tests/ exits 0 with no regressions from the full Phase 9 changes
- brainstorm-specialist soul has explicit no-turn-cap discipline (B1-A) and goal_statement output schema
- requirements-specialist soul has brain pre-query instruction in Discipline (B7)
- stage_policy.py has brainstorm stage with timeout=300, hitl_on_timeout=True
- Human verifies dry-run of brainstorm and merge agent completes without tracebacks
</success_criteria>

<output>
Create `.planning/phases/09-advanced-agent-architecture/09-04-SUMMARY.md` when done
</output>
