---
phase: 03-skill-toolkit
plan: "04"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - hermes-skills/brain_http.py
  - skills/brain-query/SKILL.md
  - skills/brain-ingest/SKILL.md
  - skills/brain-research/SKILL.md
  - tests/phase-03/brain-smoke.bats
autonomous: true
requirements: [REQ-ws-005]

must_haves:
  truths:
    - "hermes-skills/brain_http.py POSTs to Brain using multipart/form-data (data={...}), NOT json={...}"
    - "skills/brain-query/SKILL.md, skills/brain-ingest/SKILL.md, skills/brain-research/SKILL.md all pass hermes-skills/test_skill_frontmatter.py"
    - "V4 relaxation: brain-query smoke test asserts HTTP 200 + response body parses + message_id/run_id present — NOT vault-grounded answer (deferred)"
    - "brain_http.py surfaces status:ERROR from Brain clearly on stderr; does not silently return empty content"
    - "skills/brain-query/SKILL.md documents V4 relaxation: explains what IS and IS NOT asserted in smoke test (Brain Groq structured-output issue)"
    - "Follow-up backlog note added to SKILL.md: 'once Brain Groq structured-output issue is resolved, upgrade smoke to assert citation-grounded answer'"
  artifacts:
    - path: "hermes-skills/brain_http.py"
      provides: "Synchronous httpx helper for Brain Agno POST /agents/{id}/runs (form-data)"
      min_lines: 60
      contains: "data={\"message\""
    - path: "skills/brain-query/SKILL.md"
      provides: "Hermes skill wrapping brain_http.py for vault query"
      contains: "V4 relaxation"
    - path: "skills/brain-ingest/SKILL.md"
      provides: "Hermes skill wrapping brain_http.py for vault ingest (HITL-gated on Brain side)"
      contains: "run_id"
    - path: "skills/brain-research/SKILL.md"
      provides: "Hermes skill wrapping brain_http.py for research agent"
      contains: "metadata:\n  hermes:"
  key_links:
    - from: "skills/brain-query/SKILL.md"
      to: "hermes-skills/brain_http.py"
      via: "terminal python3 /opt/ultra-workshop/hermes-skills/brain_http.py query <question>"
      pattern: "brain_http.py.*query"
    - from: "hermes-skills/brain_http.py"
      to: "Brain API at 127.0.0.1:7000"
      via: "httpx.post with data={} (form-data)"
      pattern: "data=.*message"
---

<objective>
Wave 2: implement the Brain HTTP helper and 3 brain-bridge skills (REQ-ws-005).

Depends on Plan 01 (hermes-skill-run.sh deployed; bats helpers available).

CRITICAL context: The researcher confirmed Brain's query agent returns status:ERROR due to Groq + structured-output + tool-calling conflict. The user RELAXED the V4 acceptance to HTTP round-trip only. Plans must:
- Assert: HTTP 200, response body parses as JSON, run_id/message_id present in response
- NOT assert: vault-grounded answer with citations (deferred)
- Document the relaxation in brain-query/SKILL.md body

Purpose: brain_http.py is the single HTTP helper used by all 3 brain skills. It must handle form-data correctly, surface errors clearly, and be synchronous (no asyncio — skill bodies run synchronously via terminal tool).

Output:
- hermes-skills/brain_http.py (deployed to VPS /opt/ultra-workshop/hermes-skills/)
- skills/brain-query/SKILL.md, skills/brain-ingest/SKILL.md, skills/brain-research/SKILL.md
- tests/phase-03/brain-smoke.bats (HTTP round-trip verification)
</objective>

<execution_context>
@/Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-RESEARCH.md
@/Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-PATTERNS.md

Key facts:
- Brain API: POST http://127.0.0.1:7000/agents/{id}/runs
- CRITICAL: form-data not JSON. httpx data={} NOT data=json.dumps({}), NOT headers={"Content-Type": "application/json"}
- Confirmed agent IDs: query, ingest, research, curator
- brain_http.py must be SYNCHRONOUS (httpx.post not async) — skill bodies call via `terminal python3 brain_http.py ...`
- Response JSON: {run_id, agent_id, session_id, content, status, metrics}
- status: "ERROR" → surface to stderr; exit 1
- brain-ingest: HITL-gated on Brain side (smoke test verifies HTTP 200 + run_id, NOT vault write)
- V4 relaxation: smoke asserts HTTP 200 + response parses + run_id present; NOT citation-grounded answer
- PATTERNS.md has full brain_http.py implementation pattern with exact httpx call shape
- PATTERNS.md has brain-query/SKILL.md body pattern including dry-run section
- httpx may need install check: `pip install httpx` if not in venv
</context>

<interfaces>
From hermes-skills/startup-hitl-scan.py (structural/error pattern):
```python
from __future__ import annotations
import os, sqlite3
from pathlib import Path
from typing import Optional

def ensure_schema(db_path: Path = DB_PATH) -> None: ...
# CLI entrypoint pattern:
if __name__ == "__main__":
    if len(sys.argv) < 3:
        print("Usage: brain_http.py <agent_id> <message>", file=sys.stderr)
        sys.exit(1)
```

From PATTERNS.md (brain_http.py exact pattern):
```python
BRAIN_BASE_URL = "http://127.0.0.1:7000"
DEFAULT_TIMEOUT = 60.0

def call_agent(agent_id: str, message: str, user_id: str = "workshop") -> dict:
    """POST to /agents/{agent_id}/runs using multipart/form-data."""
    resp = httpx.post(
        f"{BRAIN_BASE_URL}/agents/{agent_id}/runs",
        data={"message": message, "stream": "false", "user_id": user_id},
        timeout=DEFAULT_TIMEOUT,
    )
    resp.raise_for_status()
    return resp.json()
```

Brain response schema:
```python
{
  "run_id": "<uuid>",
  "agent_id": "query",
  "agent_name": "query",
  "session_id": "<uuid>",
  "content": "<LLM response or error message>",
  "status": "success|ERROR",
  "metrics": {"duration": float},
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement hermes-skills/brain_http.py + verify on VPS</name>
  <files>hermes-skills/brain_http.py</files>
  <read_first>
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-PATTERNS.md — section "hermes-skills/brain_http.py": exact module docstring, imports, BRAIN_BASE_URL, call_agent() with data={} form-data pattern, error handling, CLI entrypoint
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/hermes-skills/startup-hitl-scan.py — module structure (docstring, from __future__ import annotations, constants, error handling, stderr pattern)
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-RESEARCH.md — "Brain HTTP API" section: confirmed form-data POST, response schema, pitfall 1 (status:ERROR handling), live VPS probe details
  </read_first>
  <action>
    Create hermes-skills/brain_http.py following PATTERNS.md exactly:

    Module docstring: explain that Brain uses form-data NOT JSON; include deploy location comment.

    Imports: from __future__ import annotations; import httpx, json, sys; from typing import Optional

    Constants: BRAIN_BASE_URL = "http://127.0.0.1:7000"; DEFAULT_TIMEOUT = 60.0

    call_agent(agent_id: str, message: str, user_id: str = "workshop") -> dict:
    - httpx.post(f"{BRAIN_BASE_URL}/agents/{agent_id}/runs", data={"message": message, "stream": "false", "user_id": user_id}, timeout=DEFAULT_TIMEOUT)
    - resp.raise_for_status()
    - data = resp.json()
    - if data.get("status") == "ERROR": print(f"Brain error: {data.get('content', 'unknown')}", file=sys.stderr); sys.exit(1)
    - return data

    CLI __main__ block:
    - if len(sys.argv) < 3: print usage to stderr; sys.exit(1)
    - agent_id = sys.argv[1]; message = " ".join(sys.argv[2:])
    - result = call_agent(agent_id, message)
    - print(json.dumps({"run_id": result.get("run_id"), "content": result.get("content"), "status": result.get("status")}))

    After writing, check if httpx is in the VPS venv:
    ssh root@31.97.130.253 "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/pip show httpx 2>/dev/null | head -1"

    If httpx is NOT installed, install it:
    ssh root@31.97.130.253 "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/pip install httpx"

    Deploy to VPS:
    rsync -avz hermes-skills/brain_http.py root@31.97.130.253:/opt/ultra-workshop/hermes-skills/brain_http.py

    Verify on VPS (V4 relaxation: HTTP 200 + run_id present):
    ssh root@31.97.130.253 "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/python3 /opt/ultra-workshop/hermes-skills/brain_http.py query 'what is PARA' 2>&1"

    The output must be valid JSON with run_id present. Status may be "ERROR" (Groq issue) — that is OK for this task. We verify HTTP connectivity, not answer quality.

    CRITICAL: do NOT use json={...} or Content-Type: application/json. Only data={...} (multipart/form-data).
    CRITICAL: brain_http.py must be SYNCHRONOUS (no async/await, no asyncio.run) — Hermes skill bodies call it via `terminal python3 brain_http.py ...` synchronously.
  </action>
  <verify>
    <automated>ssh root@31.97.130.253 "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/python3 /opt/ultra-workshop/hermes-skills/brain_http.py query 'ping' 2>&1 | python3 -c \"import sys, json; d=json.loads(sys.stdin.read()); print('run_id' in d or 'error' in str(d));\""</automated>
  </verify>
  <acceptance_criteria>
    - hermes-skills/brain_http.py exists locally
    - File contains `data={"message"` (not `json={"message"`)
    - File contains NO `async def`, NO `await`, NO `asyncio`
    - VPS: /opt/ultra-workshop/hermes-skills/brain_http.py deployed
    - VPS: `python3 brain_http.py query 'ping'` returns JSON with run_id key (HTTP 200 confirmed)
    - If Brain returns status:ERROR, the script prints it to stderr and exits 1 (not silently returns empty)
  </acceptance_criteria>
  <done>brain_http.py deployed to VPS; HTTP round-trip to Brain confirmed (V4 HTTP-only acceptance)</done>
</task>

<task type="auto">
  <name>Task 2: Create 3 brain-bridge SKILL.md files + deploy + bats smoke</name>
  <files>skills/brain-query/SKILL.md, skills/brain-ingest/SKILL.md, skills/brain-research/SKILL.md, tests/phase-03/brain-smoke.bats</files>
  <read_first>
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-PATTERNS.md — section "skills/brain-query/SKILL.md, skills/brain-ingest/SKILL.md, skills/brain-research/SKILL.md": frontmatter pattern, body delegation pattern, per-skill agent_id variants, dry-run body pattern
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-RESEARCH.md — Pitfall 7 (brain-ingest HITL on Brain side); confirmed agent IDs; brain-query note about V4 relaxation
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/tests/phase-03/helpers.bash — load pattern for brain-smoke.bats
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/tests/phase-02/hitl-restart.bats — @test structure with status + output assertions
  </read_first>
  <action>
    Create skills/brain-query/SKILL.md:
    - name: brain-query
    - description: "Query the vault: answer a question using Brain's knowledge base. Use for 'brain-query --question <q>', 'ask brain', 'vault search', or similar."
    - version: 1.0.0; author: ultra-workshop; license: MIT; platforms: [linux]; tags: [brain, vault, query, research]
    - Body sections:
      ## Brain Query
      Delegates to Brain's query agent via HTTP.

      ## Steps
      1. Extract the question from the user message (parse --question flag)
      2. Run: terminal python3 /opt/ultra-workshop/hermes-skills/brain_http.py query "<question>"
      3. Parse the JSON response; surface content field as the answer

      ## V4 Acceptance Note (FOLLOW-UP BACKLOG)
      The smoke test for this skill asserts HTTP 200 + run_id present only.
      Brain's query agent currently returns status:ERROR due to a Groq structured-output + tool-calling
      conflict in the LiteLLM configuration. The citation-grounded answer test is deferred until
      Brain's Groq issue is resolved. Once fixed: upgrade smoke test to assert content is non-empty
      and contains vault document references.

      ## Dry-run behavior
      If trigger contains --dry-run, print the command that would execute and the question extracted, then stop.

    Create skills/brain-ingest/SKILL.md:
    - name: brain-ingest; agent_id for call: ingest
    - description: "Ingest content into the vault via Brain. Use for 'brain-ingest --content <text>', 'add to vault', 'ingest note'. Note: Brain's ingest agent is HITL-gated — a human approval is required on Brain's side before vault write completes."
    - tags: [brain, vault, ingest, write]
    - Body: similar structure; notes that smoke test verifies run_id only (vault write may not be immediate due to Brain HITL)

    Create skills/brain-research/SKILL.md:
    - name: brain-research; agent_id for call: research
    - description: "Run a research task via Brain. Use for 'brain-research --topic <topic>', 'research with brain', or 'synthesize information about'. Invokes Brain's research agent."
    - tags: [brain, vault, research, synthesis]
    - Body: similar structure; notes research agent performs multi-step synthesis

    Deploy all 3 skills to VPS:
    rsync -avz skills/brain-query skills/brain-ingest skills/brain-research root@31.97.130.253:/home/uws/.hermes/skills/

    Create tests/phase-03/brain-smoke.bats:
    - load helpers
    - @test "brain-query dry-run exits 0":
        run ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh brain-query --dry-run --question 'what is PARA'"
        [ "$status" -eq 0 ]
    - @test "brain_http.py query returns HTTP 200 + run_id (V4 relaxed acceptance)":
        run ssh_cmd "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/python3 /opt/ultra-workshop/hermes-skills/brain_http.py query 'what is PARA' 2>/dev/null"
        [ "$status" -eq 0 ]
        echo "$output" | python3 -c "import sys, json; d=json.load(sys.stdin); assert 'run_id' in d, 'run_id missing'"
    - @test "brain-ingest dry-run exits 0":
        run ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh brain-ingest --dry-run --content 'test'"
        [ "$status" -eq 0 ]
    - @test "brain-research dry-run exits 0":
        run ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh brain-research --dry-run --topic 'PARA'"
        [ "$status" -eq 0 ]

    NOTE on @test 2 (HTTP live test): If Brain is down or returns connection error, this test will fail.
    Add a skip guard: if Brain is unreachable (curl --connect-timeout 3 http://127.0.0.1:7000/health returns non-0 on VPS), skip with "Brain unavailable" message rather than hard-fail.
    Skip pattern: run ssh_cmd "curl --connect-timeout 3 -sf http://127.0.0.1:7000/health"; skip_if_brain_down() { run ssh_cmd "curl ..."; [ "$status" -ne 0 ] && skip "Brain API unreachable"; }
  </action>
  <verify>
    <automated>bats /Users/caiobellizzi/Documents/Projects/ultra-workshop/tests/phase-03/brain-smoke.bats</automated>
  </verify>
  <acceptance_criteria>
    - skills/brain-query/SKILL.md contains "V4 Acceptance Note" and "FOLLOW-UP BACKLOG" text
    - skills/brain-query/SKILL.md contains agent_id "query" in the terminal invocation
    - skills/brain-ingest/SKILL.md contains "HITL-gated" warning text
    - `grep -r "data={.\"message\"" hermes-skills/brain_http.py` returns a match (form-data confirmed in source)
    - `pytest hermes-skills/test_skill_frontmatter.py -v` → passes for brain-query, brain-ingest, brain-research (3 new skills)
    - `bats tests/phase-03/brain-smoke.bats` → 3/4 pass minimum (dry-run tests); HTTP live test passes if Brain is up, skips if Brain is down
    - VPS: `ls /home/uws/.hermes/skills/` shows brain-query, brain-ingest, brain-research
  </acceptance_criteria>
  <done>Brain-bridge skills deployed; V4 relaxed acceptance documented in SKILL.md; HTTP smoke passes or skips gracefully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VPS Hermes skill body → brain_http.py → Brain API | HTTP call on loopback (127.0.0.1:7000); no auth; trusted network |
| brain_http.py CLI arg (message) → HTTP POST body | User-supplied message passed through form-data field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information Disclosure | brain_http.py logging vault paths in response | mitigate | brain_http.py only prints run_id + content + status; does NOT log raw response to stdout in structured form; full JSON only emitted when called from CLI directly |
| T-03-13 | Tampering | brain_http.py message arg injection via shell expansion | mitigate | SKILL.md body uses `terminal python3 brain_http.py query "$question"` — Hermes passes this via its tool executor, not via raw shell; brain_http.py uses sys.argv positional args (no shell=True) |
| T-03-14 | Spoofing | Brain API at 127.0.0.1:7000 returning crafted response | accept | Loopback only; same VPS; no external network access; trust established by Phase 2 |
| T-03-15 | Tampering | brain-ingest accidentally writing to vault without HITL | mitigate | Pitfall 7 documented: Brain's ingest agent is HITL-gated on Brain side; smoke test verifies run_id only (not vault file existence); additional SKILL.md warning documents HITL requirement |
| T-03-SC | Tampering | httpx install on VPS | mitigate | httpx is checked for existence before install; if [ASSUMED] tag applies, human checkpoint confirms before pip install |
</threat_model>

<verification>
After Plan 04 completes:
1. `bats tests/phase-03/brain-smoke.bats` → 3+ pass (dry-runs all pass; HTTP live test passes or skips)
2. VPS: `python3 /opt/ultra-workshop/hermes-skills/brain_http.py query 'ping'` returns JSON with run_id
3. `grep "data={" hermes-skills/brain_http.py` contains `"message"` (form-data confirmed)
4. `grep "json=" hermes-skills/brain_http.py` returns empty (no JSON Content-Type leak)
5. skills/brain-query/SKILL.md contains "FOLLOW-UP BACKLOG" note about citation test deferral
6. `pytest hermes-skills/test_skill_frontmatter.py -v` → includes brain-query, brain-ingest, brain-research in passing tests
</verification>

<success_criteria>
- brain_http.py deployed to VPS with correct form-data pattern
- 3 brain-bridge skills deployed to VPS ~/.hermes/skills/
- V4 relaxation documented in brain-query/SKILL.md
- bats brain-smoke.bats passes (dry-runs all pass; HTTP test passes or gracefully skips)
- REQ-ws-005 satisfied at relaxed acceptance level per user decision
</success_criteria>

<output>
Create .planning/phases/03-skill-toolkit/03-04-SUMMARY.md when done
</output>
