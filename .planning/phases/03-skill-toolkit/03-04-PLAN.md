---
phase: 03-skill-toolkit
plan: "04"
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - hermes-skills/brain_http.py
  - skills/brain-query/SKILL.md
  - skills/brain-ingest/SKILL.md
  - skills/brain-research/SKILL.md
  - tests/phase-03/brain-smoke.bats
autonomous: true
requirements: [REQ-ws-005]

must_haves:
  truths:
    - "hermes-skills/brain_http.py POSTs to Brain using multipart/form-data (httpx data={...}), NOT json={...}"
    - "brain_http.py is synchronous — no async def, no asyncio; skill bodies invoke it via terminal tool"
    - "skills/brain-query/SKILL.md, skills/brain-ingest/SKILL.md, skills/brain-research/SKILL.md pass hermes-skills/test_skill_frontmatter.py (23+ total tests)"
    - "V4 relaxation: brain-query smoke asserts HTTP 200 + run_id present in response — NOT citation-grounded vault answer"
    - "skills/brain-query/SKILL.md documents V4 relaxation and contains FOLLOW-UP BACKLOG note about citation test deferral"
    - "skills/brain-ingest/SKILL.md contains HITL warning: vault write is gated on Brain-side approval"
    - "All 3 brain skills deployed to VPS /home/uws/.hermes/skills/ via direct cp"
    - "brain_http.py deployed to VPS /opt/ultra-workshop/hermes-skills/"
    - "bats tests/phase-03/brain-smoke.bats: dry-run tests (3) pass; HTTP live test passes or skips gracefully if Brain down"
  artifacts:
    - path: "hermes-skills/brain_http.py"
      provides: "Synchronous httpx helper — POST /agents/{id}/runs with form-data"
      min_lines: 60
      contains: "data={\"message\""
    - path: "skills/brain-query/SKILL.md"
      provides: "Hermes skill wrapping brain_http.py for vault query; V4 note and FOLLOW-UP BACKLOG"
      contains: "FOLLOW-UP BACKLOG"
    - path: "skills/brain-ingest/SKILL.md"
      provides: "Hermes skill for vault ingest with HITL warning"
      contains: "HITL"
    - path: "skills/brain-research/SKILL.md"
      provides: "Hermes skill wrapping brain_http.py for research synthesis"
      contains: "metadata:\n  hermes:"
    - path: "tests/phase-03/brain-smoke.bats"
      provides: "Brain smoke tests: 3 dry-run assertions + 1 HTTP live test with skip guard"
  key_links:
    - from: "skills/brain-query/SKILL.md"
      to: "hermes-skills/brain_http.py"
      via: "terminal python3 /opt/ultra-workshop/hermes-skills/brain_http.py query <question>"
      pattern: "brain_http.py.*query"
    - from: "hermes-skills/brain_http.py"
      to: "Brain API at 127.0.0.1:7000"
      via: "httpx.post with data={} form-data"
      pattern: "data=.*message"
---

<objective>
Wave 2: implement brain_http.py and 3 brain-bridge skills (REQ-ws-005).

Depends on Plan 01 (hermes-skill-run.sh + bats helpers deployed) and Plan 03 (frontmatter validator with 20 passing tests — adding 3 more here is additive).

CRITICAL — Brain API facts confirmed by live VPS probe (RESEARCH.md):
- Brain runs at http://127.0.0.1:7000 (Agno 2.6.7, FastAPI/uvicorn)
- POST /agents/{id}/runs uses multipart/form-data — NOT application/json
- Confirmed agent IDs: query, ingest, research, curator
- httpx 0.28.1 already in Hermes venv — no install needed
- Brain query agent returns status:ERROR (Groq + tool-calling conflict) — HTTP 200 is correct; content may be an error message
- V4 relaxation: smoke test asserts HTTP 200 + run_id present only; citation-grounded answer deferred

Output:
- hermes-skills/brain_http.py (deployed to VPS /opt/ultra-workshop/hermes-skills/)
- skills/brain-query/SKILL.md, skills/brain-ingest/SKILL.md, skills/brain-research/SKILL.md (deployed to VPS)
- tests/phase-03/brain-smoke.bats
</objective>

<execution_context>
@/Users/caiobellizzi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caiobellizzi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-skill-toolkit/03-RESEARCH.md
@.planning/phases/03-skill-toolkit/03-PATTERNS.md
@.planning/phases/03-skill-toolkit/03-01-SUMMARY.md
@.planning/phases/03-skill-toolkit/03-03-SUMMARY.md

Key facts:
- Brain API: POST http://127.0.0.1:7000/agents/{id}/runs
- CRITICAL: use data={} (form-data) NOT json={} — JSON POST returns 422
- httpx 0.28.1 ALREADY in Hermes venv — do NOT pip install httpx
- brain_http.py must be SYNCHRONOUS — skill bodies call it via `terminal python3 brain_http.py ...`
- Response JSON: {run_id, agent_id, session_id, content, status, metrics}
- status: "ERROR" is an application error from Brain — surface to stderr + exit 1; HTTP 200 is still returned
- brain-ingest: HITL-gated on Brain side (smoke test checks run_id only, NOT vault write)
- Brain skill deploy: cp to /home/uws/.hermes/skills/ (same as Plan 03 — NOT hermes skills install)
- PATTERNS.md has exact brain_http.py implementation pattern with httpx call shape
</context>

<interfaces>
From hermes-skills/startup-hitl-scan.py (module structure + error pattern):
```python
"""Module docstring"""
from __future__ import annotations
import httpx, json, sys
from typing import Optional

BRAIN_BASE_URL = "http://127.0.0.1:7000"
DEFAULT_TIMEOUT = 60.0

# Error output: print to stderr, exit 1
if data.get("status") == "ERROR":
    print(f"Brain error: {data.get('content', 'unknown')}", file=sys.stderr)
    sys.exit(1)
```

Brain response schema (RESEARCH.md live probe):
```json
{
  "run_id": "<uuid>",
  "agent_id": "query",
  "session_id": "<uuid>",
  "content": "<LLM response or error message>",
  "status": "success|ERROR",
  "metrics": {"duration": 1.23}
}
```

Brain skill body pattern (PATTERNS.md, adapted per agent_id):
```markdown
## Steps
1. Extract the --question/--content/--topic argument from the trigger
2. Run: terminal python3 /opt/ultra-workshop/hermes-skills/brain_http.py <agent_id> "<argument>"
3. Parse the JSON response; surface the content field
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement hermes-skills/brain_http.py and deploy to VPS</name>
  <files>hermes-skills/brain_http.py</files>
  <read_first>
    - .planning/phases/03-skill-toolkit/03-PATTERNS.md — section "hermes-skills/brain_http.py": exact module docstring, imports, BRAIN_BASE_URL, call_agent() with data={} form-data pattern, ERROR status handling, CLI __main__ block
    - hermes-skills/startup-hitl-scan.py — module structure (from __future__ import annotations, module docstring style, print to stderr pattern, if __name__ == "__main__" entrypoint)
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — "Brain HTTP API" section: confirmed form-data POST, response schema, status:ERROR handling (Pitfall 4), live probe details; "Environment Availability" confirming httpx already installed
  </read_first>
  <action>
    Create hermes-skills/brain_http.py following PATTERNS.md exactly:

    Module docstring: state that Brain uses multipart/form-data NOT JSON; include deploy location "/opt/ultra-workshop/hermes-skills/brain_http.py"; note synchronous (no asyncio).

    Imports: from __future__ import annotations; import httpx, json, sys; from typing import Optional

    Constants: BRAIN_BASE_URL = "http://127.0.0.1:7000"; DEFAULT_TIMEOUT = 60.0

    def call_agent(agent_id: str, message: str, user_id: str = "workshop") -> dict:
    Docstring noting form-data requirement.
    Body: httpx.post(f"{BRAIN_BASE_URL}/agents/{agent_id}/runs", data={"message": message, "stream": "false", "user_id": user_id}, timeout=DEFAULT_TIMEOUT); resp.raise_for_status(); data = resp.json(); if data.get("status") == "ERROR": print(f"Brain error: {data.get('content', 'unknown')}", file=sys.stderr); sys.exit(1); return data

    __main__ block: if len(sys.argv) < 3: print usage to stderr + exit 1; agent_id = sys.argv[1]; message = " ".join(sys.argv[2:]); result = call_agent(agent_id, message); print(json.dumps({"run_id": result.get("run_id"), "content": result.get("content"), "status": result.get("status")}))

    MUST NOT use: json={...}, Content-Type: application/json headers, async def, await, asyncio.

    Verify brain_http.py locally has no async: grep -c "async def" hermes-skills/brain_http.py must return 0.

    Check httpx is present in venv (do NOT install — already at 0.28.1):
    ssh root@31.97.130.253 "/opt/ultra-workshop/hermes/venv/bin/python3 -c 'import httpx; print(httpx.__version__)'"
    Must print 0.28.1 — if not, something is wrong; do NOT pip install without investigating.

    Deploy to VPS:
    rsync -avz hermes-skills/brain_http.py root@31.97.130.253:/opt/ultra-workshop/hermes-skills/brain_http.py

    Verify on VPS (V4 HTTP round-trip only — status:ERROR is OK at application level):
    ssh root@31.97.130.253 "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/python3 /opt/ultra-workshop/hermes-skills/brain_http.py query 'ping' 2>/dev/null"
    The output must be valid JSON. run_id key must be present. status may be "ERROR" — that is acceptable.
  </action>
  <verify>
    <automated>ssh root@31.97.130.253 "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/python3 /opt/ultra-workshop/hermes-skills/brain_http.py query 'ping' 2>/dev/null | python3 -c \"import sys, json; d=json.load(sys.stdin); print('ok' if 'run_id' in d else 'missing-run_id')\""</automated>
  </verify>
  <acceptance_criteria>
    - hermes-skills/brain_http.py exists locally
    - `grep "data={\"message\"" hermes-skills/brain_http.py` returns a match (form-data confirmed — NOT json=)
    - `grep "json=" hermes-skills/brain_http.py` returns empty (no JSON Content-Type leak)
    - `grep -c "async def" hermes-skills/brain_http.py` returns 0 (no async — synchronous only)
    - VPS: /opt/ultra-workshop/hermes-skills/brain_http.py deployed
    - VPS: `python3 brain_http.py query 'ping'` returns JSON with run_id key (HTTP 200 confirmed regardless of status field value)
    - VPS: httpx version remains 0.28.1 (unchanged from baseline)
  </acceptance_criteria>
  <done>brain_http.py deployed to VPS; HTTP round-trip to Brain API confirmed; V4 HTTP-only acceptance satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Create 3 brain-bridge SKILL.md files, deploy all, create bats brain-smoke</name>
  <files>skills/brain-query/SKILL.md, skills/brain-ingest/SKILL.md, skills/brain-research/SKILL.md, tests/phase-03/brain-smoke.bats</files>
  <read_first>
    - .planning/phases/03-skill-toolkit/03-PATTERNS.md — section "skills/brain-query/SKILL.md, skills/brain-ingest/SKILL.md, skills/brain-research/SKILL.md": frontmatter pattern, body delegation pattern, per-skill agent_id variants, dry-run body pattern
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — "Brain HTTP API" section (agent IDs: query, ingest, research); Pitfall 4 (brain-query status:ERROR caveat); Pitfall 3 (brain-ingest HITL-gated); V4 relaxation decision (HTTP 200 + run_id only)
    - tests/phase-03/helpers.bash — ssh_cmd() for brain-smoke.bats `load helpers`
    - tests/phase-02/hitl-restart.bats — @test structure with setup/teardown, skip patterns, status + output assertions
  </read_first>
  <action>
    Create skills/brain-query/SKILL.md:
    - name: brain-query
    - description: "Query the vault: answer a question using Brain's knowledge base. Use for 'brain-query --question <q>', 'ask brain', 'vault search', or similar."
    - version: 1.0.0; author: ultra-workshop; license: MIT; platforms: [linux]; tags: [brain, vault, query, research]
    - Body:
      ## Brain Query
      Extract --question argument from trigger. Run: terminal python3 /opt/ultra-workshop/hermes-skills/brain_http.py query "<question>". Parse the JSON response; surface the content field as the answer.

      ## V4 Acceptance Note
      Brain's query agent currently returns status:ERROR due to a Groq structured-output + tool-calling conflict in the LiteLLM configuration. HTTP 200 and run_id are returned correctly.

      ## FOLLOW-UP BACKLOG
      Once Brain's Groq structured-output issue is resolved: upgrade the brain-smoke.bats HTTP live test to assert content is non-empty and contains vault document references. The smoke test currently asserts HTTP 200 + run_id only (V4 relaxation, decided 2026-05-21).

      ## Dry-run behavior
      If trigger contains --dry-run, print the command that would execute and the question extracted, then stop without calling terminal.

    Create skills/brain-ingest/SKILL.md:
    - name: brain-ingest
    - description: "Ingest content into the vault via Brain. Use for 'brain-ingest --content <text>', 'add to vault', 'ingest note'. Warning: Brain's ingest agent is HITL-gated — human approval required on Brain side before vault write completes."
    - platforms: [linux]; tags: [brain, vault, ingest, write]
    - Body: Extract --content argument; run terminal python3 /opt/ultra-workshop/hermes-skills/brain_http.py ingest "<content>"; surface run_id.
    - Include HITL warning: "Note: Brain's ingest agent requires human approval (HITL) before the vault write is committed. The smoke test verifies HTTP 200 and run_id only — it does not verify that content was written to the vault."
    - Include Dry-run behavior section.

    Create skills/brain-research/SKILL.md:
    - name: brain-research
    - description: "Run a multi-step research task via Brain. Use for 'brain-research --topic <topic>', 'research with brain', 'synthesize information about'. Invokes Brain's research agent for deep synthesis."
    - platforms: [linux]; tags: [brain, vault, research, synthesis]
    - Body: Extract --topic argument; run terminal python3 /opt/ultra-workshop/hermes-skills/brain_http.py research "<topic>"; surface content.
    - Include Dry-run behavior section.

    Deploy all 3 brain skills to VPS via direct cp:
    rsync -avz skills/brain-query skills/brain-ingest skills/brain-research root@31.97.130.253:/home/uws/.hermes/skills/
    Verify: ssh root@31.97.130.253 "ls /home/uws/.hermes/skills/ | grep brain"

    Create tests/phase-03/brain-smoke.bats:
    #!/usr/bin/env bats + `load helpers`

    Skip guard helper function:
    skip_if_brain_down() {
      run ssh_cmd "curl --connect-timeout 3 -sf http://127.0.0.1:7000/health"
      [ "$status" -ne 0 ] && skip "Brain API unreachable"
    }

    @test "brain-query dry-run exits 0":
      run ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh brain-query --dry-run --question 'what is PARA'"
      [ "$status" -eq 0 ]

    @test "brain-ingest dry-run exits 0":
      run ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh brain-ingest --dry-run --content 'test note'"
      [ "$status" -eq 0 ]

    @test "brain-research dry-run exits 0":
      run ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh brain-research --dry-run --topic 'PARA'"
      [ "$status" -eq 0 ]

    @test "brain_http.py query returns HTTP 200 + run_id (V4 relaxed acceptance)":
      skip_if_brain_down
      run ssh_cmd "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/python3 /opt/ultra-workshop/hermes-skills/brain_http.py query 'what is PARA' 2>/dev/null"
      [ "$status" -eq 0 ]
      echo "$output" | python3 -c "import sys, json; d=json.load(sys.stdin); assert 'run_id' in d, 'run_id missing from Brain response'"

    Run: bats tests/phase-03/brain-smoke.bats — 3 dry-run tests must pass; HTTP live test passes if Brain up or skips if down.
  </action>
  <verify>
    <automated>bats /Users/caiobellizzi/Documents/Projects/ultra-workshop/tests/phase-03/brain-smoke.bats</automated>
  </verify>
  <acceptance_criteria>
    - skills/brain-query/SKILL.md contains both "FOLLOW-UP BACKLOG" and "V4 Acceptance Note" text
    - skills/brain-query/SKILL.md agent invocation uses agent_id "query" in terminal command
    - skills/brain-ingest/SKILL.md contains "HITL" text warning about Brain-side approval
    - `grep "json=" hermes-skills/brain_http.py` returns empty (form-data confirmed; no json= leak)
    - `pytest hermes-skills/test_skill_frontmatter.py -v` → 23+ tests pass (2 per skill × 10 from Plan 03 + 3 brain skills = 13 skills × 2 = 26 total, or >= 23 if some are combined)
    - `bats tests/phase-03/brain-smoke.bats` → 3 dry-run @tests pass; HTTP live @test either passes or shows "SKIP" (not FAIL)
    - VPS: `ssh root@31.97.130.253 "ls /home/uws/.hermes/skills/ | grep -c brain"` returns 3
  </acceptance_criteria>
  <done>3 brain-bridge skills deployed to VPS; V4 relaxation documented in brain-query/SKILL.md; bats brain-smoke passes with graceful skip on Brain outage; REQ-ws-005 satisfied at V4 relaxed level</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VPS Hermes skill body → brain_http.py → Brain API | HTTP on loopback 127.0.0.1:7000; no auth; trusted network (same VPS) |
| brain_http.py CLI args (agent_id, message) → HTTP POST body | User-supplied message passed through form-data field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Information Disclosure | brain_http.py stdout could leak vault content | mitigate | __main__ block only prints run_id + content + status (structured JSON); does not log full response or request headers |
| T-03-14 | Tampering | brain_http.py message arg via skill body | mitigate | Hermes passes terminal args via its tool executor (not raw shell); brain_http.py uses sys.argv positional args with no shell=True |
| T-03-15 | Spoofing | Brain API at 127.0.0.1:7000 response spoofing | accept | Loopback only; same VPS host as caller; no external network; trust established by Phase 2 service topology |
| T-03-16 | Tampering | brain-ingest accidentally writing vault without HITL | mitigate | Documented in skill body as HITL-gated; smoke test verifies run_id only (not vault file existence); Brain-side HITL enforcement is independent of this skill |
| T-03-SC | Tampering | No package installs (httpx already present) | accept | httpx 0.28.1 confirmed in venv; no pip install in this plan |
</threat_model>

<verification>
After Plan 04 completes:
1. `bats tests/phase-03/brain-smoke.bats` → 3+ pass (all dry-run pass; HTTP test passes or skips)
2. VPS: `python3 /opt/ultra-workshop/hermes-skills/brain_http.py query 'ping' 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print('ok' if 'run_id' in d else 'fail')"` prints "ok"
3. `grep "data={" hermes-skills/brain_http.py` returns match containing "message" (form-data confirmed)
4. `grep "json=" hermes-skills/brain_http.py` returns empty
5. skills/brain-query/SKILL.md: `grep "FOLLOW-UP BACKLOG" skills/brain-query/SKILL.md` returns a match
6. `pytest hermes-skills/test_skill_frontmatter.py -v` → all tests pass (now 26+ with brain skills)
</verification>

<success_criteria>
- brain_http.py deployed with correct form-data pattern (no json= path)
- 3 brain-bridge skills deployed to VPS /home/uws/.hermes/skills/
- V4 relaxation documented; FOLLOW-UP BACKLOG note present in brain-query/SKILL.md
- bats brain-smoke passes (dry-runs all pass; HTTP live test passes or gracefully skips)
- REQ-ws-005 satisfied at V4 relaxed acceptance level
</success_criteria>

<output>
Create .planning/phases/03-skill-toolkit/03-04-SUMMARY.md when done
</output>
