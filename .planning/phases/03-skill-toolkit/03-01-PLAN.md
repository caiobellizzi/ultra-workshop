---
phase: 03-skill-toolkit
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/hermes-skill-run.sh
  - tests/phase-03/helpers.bash
  - tests/phase-03/scaffold.bats
  - hermes-skills/test_skill_frontmatter.py
  - pyproject.toml
autonomous: true
requirements: [REQ-ws-003, REQ-ws-004, REQ-ws-005, REQ-ws-006]

must_haves:
  truths:
    - "scripts/hermes-skill-run.sh exists locally, is executable, and exits 0 for any skill name with --dry-run"
    - "tests/phase-03/helpers.bash provides ssh_cmd() with VPS_HOST=31.97.130.253"
    - "tests/phase-03/scaffold.bats passes: 2/2 bats assertions green"
    - "hermes-skills/test_skill_frontmatter.py exists and pytest --collect-only exits 0 (0 items is OK at Wave 0)"
    - "VPS: /opt/ultra-workshop/scripts/ directory exists"
    - "VPS: hermes-skill-run.sh deployed and chmod +x at /opt/ultra-workshop/scripts/"
    - "VPS: bats binary on PATH (which bats returns 0)"
    - "VPS: aider-chat importable in Hermes venv (python3 -c 'import aider' exits 0)"
  artifacts:
    - path: "scripts/hermes-skill-run.sh"
      provides: "Shell wrapper translating <skill-name> [flags] to hermes chat --skills <name> --query <q> -Q --max-turns 3 --yolo"
      min_lines: 25
    - path: "tests/phase-03/helpers.bash"
      provides: "Shared bats helpers for Phase 3 SSH smoke tests"
      contains: "ssh_cmd"
    - path: "tests/phase-03/scaffold.bats"
      provides: "Baseline bats smoke validating wrapper deployed correctly on VPS"
    - path: "hermes-skills/test_skill_frontmatter.py"
      provides: "Pytest parametrized test validating required frontmatter fields on all skills/*/SKILL.md"
  key_links:
    - from: "tests/phase-03/*.bats"
      to: "scripts/hermes-skill-run.sh"
      via: "ssh_cmd invocation"
      pattern: "hermes-skill-run.sh.*--dry-run"
    - from: "hermes-skills/test_skill_frontmatter.py"
      to: "skills/*/SKILL.md"
      via: "Path.glob('*/SKILL.md') + yaml.safe_load"
      pattern: "glob.*SKILL.md"
---

<objective>
Wave 0: install missing VPS packages, create /opt/ultra-workshop/scripts/, deploy hermes-skill-run.sh, and build the test infrastructure all later plans depend on.

Purpose: This plan gates everything. Plans 02–05 all require: (a) bats helpers for SSH smoke tests, (b) the wrapper script deployed on VPS, (c) the frontmatter validator to auto-check every SKILL.md, and (d) aider-chat installed before Plan 05 executes. Resolving all prerequisites here prevents mid-phase installation failures.

RESEARCH.md live VPS probe (2026-05-21) confirmed these gaps — do not skip:
- /opt/ultra-workshop/scripts/ does NOT exist → must mkdir -p before rsync
- bats is NOT on VPS PATH → must apt-get install -y bats
- aider-chat is NOT in Hermes venv → must python3 -m pip install aider-chat
- httpx 0.28.1 and PyYAML 6.0.3 ARE already installed → do NOT touch these

Output:
- scripts/hermes-skill-run.sh (shell wrapper)
- tests/phase-03/helpers.bash (SSH helpers)
- tests/phase-03/scaffold.bats (baseline bats smoke)
- hermes-skills/test_skill_frontmatter.py (pytest frontmatter validator)
- VPS: /opt/ultra-workshop/scripts/ created, bats and aider-chat installed
</objective>

<execution_context>
@/Users/caiobellizzi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caiobellizzi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-skill-toolkit/03-RESEARCH.md
@.planning/phases/03-skill-toolkit/03-PATTERNS.md

Key facts (RESEARCH.md live probe 2026-05-21):
- VPS: root@31.97.130.253; Hermes binary: /opt/ultra-workshop/hermes/venv/bin/hermes
- Hermes CLI has NO `hermes skill run` subcommand — use `hermes chat --skills <name>`
- /opt/ultra-workshop/ contains ONLY: deploy/, hermes/, hermes-config/, hermes-skills/ — no scripts/
- MISSING on VPS: bats (find / found no bats binary), aider-chat (import aider fails)
- PRESENT on VPS: httpx 0.28.1, PyYAML 6.0.3 in Hermes venv — do not reinstall
- No `pip` binary in Hermes venv — always use `python3 -m pip`
- --dry-run is handled at SKILL.md body level, not by hermes-skill-run.sh itself
- --yolo flag required on `hermes chat` to prevent interactive approval hangs (Pitfall 3)
- Phase 2 helpers.bash: tests/phase-02/helpers.bash — VPS_HOST, VPS_USER, ssh_cmd(), assert_service_active()
</context>

<interfaces>
From tests/phase-02/helpers.bash (copy verbatim, update header comment only):
```bash
VPS_HOST="31.97.130.253"
VPS_USER="root"
VPS_SSH="ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10"
ssh_cmd() { $VPS_SSH ${VPS_USER}@${VPS_HOST} "$@"; }
assert_service_active() { local svc="$1"; ssh_cmd "systemctl is-active $svc" | grep -q "^active$"; }
```

From tests/phase-02/hitl-restart.bats (test structure pattern):
```bash
#!/usr/bin/env bats
load helpers
@test "description" {
  run ssh_cmd "some command"
  [ "$status" -eq 0 ]
  echo "$output" | grep -q "expected string"
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: VPS prerequisites — mkdir scripts/, install bats, install aider-chat</name>
  <files></files>
  <read_first>
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — "VPS State After Phase 2" table; "Installation (VPS)" code block; Common Pitfalls section (Pitfall 1: no pip binary; Pitfall 2: scripts/ dir missing)
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — "Package Legitimacy Audit" table confirming aider-chat as [ASSUMED] Approved (50k+ GitHub stars, ~1M/mo downloads)
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — "Environment Availability" table confirming httpx and PyYAML are already installed (no action)
  </read_first>
  <action>
    Execute the following VPS operations in order. All must succeed before Task 2.

    Step 1 — Create scripts directory (RESEARCH Pitfall 2 — directory does not exist):
    `ssh root@31.97.130.253 "mkdir -p /opt/ultra-workshop/scripts"`
    Verify: `ssh root@31.97.130.253 "ls -la /opt/ultra-workshop/ | grep scripts"` shows the directory.

    Step 2 — Install bats system package:
    `ssh root@31.97.130.253 "apt-get install -y bats 2>&1 | tail -3"`
    Verify: `ssh root@31.97.130.253 "which bats && bats --version"` — must return a path and version like "Bats 1.x.x".
    If already installed, apt-get will say "already newest version" — that is a pass.

    Step 3 — Install aider-chat in Hermes venv (RESEARCH Pitfall 1 — no pip binary):
    `ssh root@31.97.130.253 "/opt/ultra-workshop/hermes/venv/bin/python3 -m pip install aider-chat 2>&1 | tail -5"`
    CRITICAL: Use python3 -m pip, NOT pip install — no standalone pip binary exists in the venv.
    Verify: `ssh root@31.97.130.253 "/opt/ultra-workshop/hermes/venv/bin/python3 -c 'import aider; print(aider.__version__)'"` — must exit 0 and print a version.

    Step 4 — Confirm httpx and PyYAML are undisturbed (DO NOT reinstall):
    `ssh root@31.97.130.253 "/opt/ultra-workshop/hermes/venv/bin/python3 -c 'import httpx; print(httpx.__version__)'"`
    Must print 0.28.1. If it does not, something went wrong in Step 3 — investigate before continuing.
    `ssh root@31.97.130.253 "/opt/ultra-workshop/hermes/venv/bin/python3 -c 'import yaml; print(yaml.__version__)'"` must print 6.0.3.
  </action>
  <verify>
    <automated>ssh root@31.97.130.253 "ls /opt/ultra-workshop/scripts && which bats && /opt/ultra-workshop/hermes/venv/bin/python3 -c 'import aider; print(\"aider-ok\")'" && echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `ssh root@31.97.130.253 "ls /opt/ultra-workshop/scripts"` exits 0 (directory exists, even if empty)
    - `ssh root@31.97.130.253 "which bats"` exits 0 and prints a path (e.g. /usr/bin/bats)
    - `ssh root@31.97.130.253 "/opt/ultra-workshop/hermes/venv/bin/python3 -c 'import aider; print(aider.__version__)'"` exits 0 and prints a version string (e.g. 0.82.x)
    - `ssh root@31.97.130.253 "/opt/ultra-workshop/hermes/venv/bin/python3 -c 'import httpx; print(httpx.__version__)'"` prints 0.28.1 (unchanged)
    - No package install output contains "ERROR" or "Failed to install"
  </acceptance_criteria>
  <done>VPS prerequisites satisfied: scripts/ dir exists, bats installed, aider-chat importable, httpx/PyYAML undisturbed</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/hermes-skill-run.sh and deploy to VPS</name>
  <files>scripts/hermes-skill-run.sh</files>
  <read_first>
    - scripts/install.sh — dry-run guard pattern (lines 13–26: DRY_RUN loop over args, rsh() function); set -euo pipefail header
    - .planning/phases/03-skill-toolkit/03-PATTERNS.md — section "scripts/hermes-skill-run.sh": header pattern, --dry-run guard, core delegation with exec, SSH invocation note
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — "hermes skill run Wrapper" section: confirmed hermes chat --skills interface; --yolo required; VERIFIED no hermes skill run subcommand
  </read_first>
  <action>
    Create scripts/hermes-skill-run.sh following the PATTERNS.md template:

    Line 1: #!/usr/bin/env bash
    Lines 2–6: comments — file path, usage line, description that this wraps `hermes chat --skills`, note that --dry-run is handled at SKILL body level (not here).
    Line 7: set -euo pipefail

    Argument validation: if [ $# -eq 0 ] || [ -z "${1:-}" ]; print "Usage: hermes-skill-run.sh <skill-name> [--dry-run] [--key value ...]" to stderr and exit 1.

    Capture SKILL="$1"; shift. Then QUERY="$*" captures remaining args.

    Dry-run short-circuit: loop over the original argv (before shift) checking for "--dry-run". If found: echo "[dry-run] would run: hermes chat --skills ${SKILL} --query '${QUERY}' -Q --max-turns 3 --yolo"; exit 0.
    Implementation note: check if echo "$QUERY" | grep -q -- "--dry-run" rather than iterating, which is simpler.

    Production path (no --dry-run): exec sudo -u uws /opt/ultra-workshop/hermes/venv/bin/hermes chat --skills "$SKILL" --query "$QUERY" -Q --max-turns 3 --yolo

    MUST NOT use: eval, sh -c, bash -c. Must use exec so the process replaces itself.

    After writing the file locally:
    chmod +x scripts/hermes-skill-run.sh

    Deploy to VPS (Task 1 must complete first — directory now exists):
    rsync -avz scripts/hermes-skill-run.sh root@31.97.130.253:/opt/ultra-workshop/scripts/hermes-skill-run.sh
    ssh root@31.97.130.253 "chmod +x /opt/ultra-workshop/scripts/hermes-skill-run.sh"
  </action>
  <verify>
    <automated>bash /Users/caiobellizzi/Documents/Projects/ultra-workshop/scripts/hermes-skill-run.sh some-skill --dry-run; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/hermes-skill-run.sh exists and is executable (ls -la shows -rwxr-xr-x or -rwxrwxr-x)
    - `bash scripts/hermes-skill-run.sh some-skill --dry-run` prints a line containing "[dry-run] would run: hermes chat --skills some-skill" and exits 0
    - `bash scripts/hermes-skill-run.sh` with no args prints "Usage:" to stderr and exits 1
    - `grep "exec sudo -u uws" scripts/hermes-skill-run.sh` returns a match
    - `grep "eval" scripts/hermes-skill-run.sh` returns empty (no eval)
    - VPS: `ssh root@31.97.130.253 "ls -la /opt/ultra-workshop/scripts/hermes-skill-run.sh"` shows -rwxr-xr-x
  </acceptance_criteria>
  <done>hermes-skill-run.sh written locally and deployed to /opt/ultra-workshop/scripts/ on VPS with +x permissions</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 3 bats helpers + scaffold smoke + pytest frontmatter validator</name>
  <files>tests/phase-03/helpers.bash, tests/phase-03/scaffold.bats, hermes-skills/test_skill_frontmatter.py, pyproject.toml</files>
  <read_first>
    - tests/phase-02/helpers.bash — copy verbatim as Phase 3 helpers (VPS_HOST, VPS_USER, ssh_cmd, assert_service_active) — update header comment only
    - tests/phase-02/hitl-restart.bats — @test structure with `load helpers`, `run ssh_cmd`, `[ "$status" -eq 0 ]`, `echo "$output" | grep -q` assertions
    - hermes-skills/test_startup_hitl_scan.py — importlib hyphenated-filename pattern (lines 26–30), @pytest.fixture with tmp_path, plain assert statements (no unittest.TestCase)
    - .planning/phases/03-skill-toolkit/03-PATTERNS.md — "hermes-skills/test_skill_frontmatter.py" section (exact parametrize pattern); "tests/phase-03/*.bats" section (helpers template, @test structure)
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — "Minimum required fields rule": name + description mandatory; tools:/mcpServers:/hooks: must be ABSENT in Hermes SKILL.md
  </read_first>
  <action>
    Create tests/phase-03/helpers.bash:
    Copy tests/phase-02/helpers.bash verbatim. Change only the header comment to reference "tests/phase-03/helpers.bash — Phase 3 shared SSH helpers". VPS_HOST="31.97.130.253", VPS_USER="root", VPS_SSH, ssh_cmd(), assert_service_active() all identical to Phase 2.

    Create tests/phase-03/scaffold.bats:
    #!/usr/bin/env bats header comment referencing Phase 3 Wave 0 scaffold validation.
    `load helpers` (bats resolves relative to the test file's directory — tests/phase-03/helpers.bash will be found).
    Two @test blocks:
      @test "hermes-skill-run.sh dry-run exits 0 for dummy skill":
        run ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh dummy-skill --dry-run"
        [ "$status" -eq 0 ]
        echo "$output" | grep -q "\[dry-run\]"
      @test "hermes binary is reachable as uws user":
        run ssh_cmd "sudo -u uws /opt/ultra-workshop/hermes/venv/bin/hermes --version"
        [ "$status" -eq 0 ]

    Create hermes-skills/test_skill_frontmatter.py:
    Imports: import yaml, pytest; from pathlib import Path
    SKILL_DIRS = list((Path(__file__).parent.parent / "skills").glob("*/SKILL.md"))
    Note: skills/ is at the repo root; test file is in hermes-skills/ — two parents up reaches repo root.

    Two parametrized test functions:
    @pytest.mark.parametrize("skill_path", SKILL_DIRS, ids=lambda p: p.parent.name)
    def test_frontmatter_has_required_fields(skill_path):
        raw = skill_path.read_text()
        parts = raw.split("---")
        assert len(parts) >= 3, f"{skill_path}: no frontmatter block found"
        fm = yaml.safe_load(parts[1])
        assert "name" in fm, f"{skill_path}: missing 'name'"
        assert "description" in fm, f"{skill_path}: missing 'description'"
        for forbidden in ("tools", "mcpServers", "hooks"):
            assert forbidden not in fm, f"{skill_path}: forbidden Hermes key '{forbidden}' present"

    @pytest.mark.parametrize("skill_path", SKILL_DIRS, ids=lambda p: p.parent.name)
    def test_name_matches_directory(skill_path):
        raw = skill_path.read_text()
        parts = raw.split("---")
        assert len(parts) >= 3
        fm = yaml.safe_load(parts[1])
        assert fm.get("name") == skill_path.parent.name, \
            f"name '{fm.get('name')}' does not match directory '{skill_path.parent.name}'"

    When SKILL_DIRS is empty (Wave 0 — no skills/ entries yet), pytest collects 0 tests. This is acceptable — NOT a failure.

    Update pyproject.toml:
    Read existing pyproject.toml if present. Add or update [tool.pytest.ini_options] with testpaths = ["hermes-skills", "scripts"]. Do not modify any existing [tool.poetry], [project], or other sections. If pyproject.toml does not exist, create a minimal file containing only:
    [tool.pytest.ini_options]
    testpaths = ["hermes-skills", "scripts"]
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-workshop && python -m pytest hermes-skills/test_skill_frontmatter.py --collect-only -q 2>&1 | tail -5 && bats tests/phase-03/scaffold.bats</automated>
  </verify>
  <acceptance_criteria>
    - tests/phase-03/helpers.bash exists with VPS_HOST="31.97.130.253" and ssh_cmd() function body
    - tests/phase-03/scaffold.bats exists with `load helpers` on its own line and 2 @test blocks
    - `bats tests/phase-03/scaffold.bats` exits 0 (both tests pass)
    - hermes-skills/test_skill_frontmatter.py exists and is syntactically valid Python
    - `pytest hermes-skills/test_skill_frontmatter.py --collect-only` exits 0 (may show "no tests ran" — that is OK)
    - test_skill_frontmatter.py: `grep "mcpServers" hermes-skills/test_skill_frontmatter.py` returns a match (forbidden-key assertion is present)
    - pyproject.toml has [tool.pytest.ini_options] section with testpaths containing "hermes-skills"
  </acceptance_criteria>
  <done>Phase 3 bats + pytest infrastructure complete; all downstream plans can use `load helpers`, ssh_cmd, and the frontmatter validator auto-validates every new SKILL.md</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mac shell → ssh root@VPS | Root SSH used for apt-get and pip installs; trust is the existing Phase 2 SSH keypair |
| VPS shell → hermes-skill-run.sh → hermes chat | SKILL and QUERY passed as exec argv — no shell string expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | hermes-skill-run.sh — SKILL arg injection | mitigate | $1 captured positionally and passed as quoted "$SKILL" in exec argv; no word-splitting |
| T-03-02 | Tampering | hermes-skill-run.sh — QUERY string | mitigate | QUERY="$*" passed as "$QUERY" to exec; exec (not eval, not sh -c) prevents shell injection |
| T-03-03 | Elevation | sudo -u uws in wrapper | accept | Root drops to uws before invoking Hermes; uws is unprivileged; sudo policy in place since Phase 2 |
| T-03-04 | Tampering | aider-chat PyPI package legitimacy | mitigate | Listed as [ASSUMED] Approved in RESEARCH.md (50k+ GitHub stars, ~1M/mo PyPI downloads, github.com/paul-gauthier/aider); verify at pypi.org/project/aider-chat before install if uncertain |
| T-03-SC | Tampering | bats via apt-get system package | accept | Ubuntu archive system package; no PyPI/npm slopcheck needed; apt verifies package signatures |
</threat_model>

<verification>
After Wave 0 completes:
1. `bash scripts/hermes-skill-run.sh skill-name --dry-run` exits 0 and line contains "[dry-run] would run:"
2. `bats tests/phase-03/scaffold.bats` → 2/2 pass
3. `pytest hermes-skills/test_skill_frontmatter.py --collect-only` → exits 0
4. VPS: `ssh root@31.97.130.253 "ls -la /opt/ultra-workshop/scripts/hermes-skill-run.sh"` shows -rwxr-xr-x
5. VPS: `ssh root@31.97.130.253 "which bats"` returns path
6. VPS: `ssh root@31.97.130.253 "/opt/ultra-workshop/hermes/venv/bin/python3 -c 'import aider; print(aider.__version__)'"` exits 0
</verification>

<success_criteria>
- hermes-skill-run.sh deployed to VPS and executable
- bats scaffold.bats: 2/2 pass
- pytest frontmatter validator collects cleanly
- VPS: bats + aider-chat installed; /opt/ultra-workshop/scripts/ exists
- All Plans 02–05 can execute without additional VPS prerequisite installs
</success_criteria>

<output>
Create .planning/phases/03-skill-toolkit/03-01-SUMMARY.md when done
</output>
