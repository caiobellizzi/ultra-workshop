---
phase: 03-skill-toolkit
plan: "03"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - skills/caveman/SKILL.md
  - skills/commit/SKILL.md
  - skills/diagnose/SKILL.md
  - skills/triage-issue/SKILL.md
  - skills/qa/SKILL.md
  - skills/knowledge/SKILL.md
  - skills/write-a-prd/SKILL.md
  - skills/zoom-out/SKILL.md
  - skills/grill-me/SKILL.md
  - skills/triage/SKILL.md
  - tests/phase-03/skills-smoke.bats
autonomous: true
requirements: [REQ-ws-004]

must_haves:
  truths:
    - "10 skills exist under skills/ with valid Hermes frontmatter (name, description, version, author, license, platforms, metadata.hermes.tags)"
    - "No skill has tools:, mcpServers:, or hooks: frontmatter keys"
    - "pytest hermes-skills/test_skill_frontmatter.py passes 20+ tests (2 per skill x 10 skills)"
    - "bats tests/phase-03/skills-smoke.bats: 10/10 dry-run tests pass"
    - "skills/triage-issue/SKILL.md and skills/qa/SKILL.md contain TRANSLATION NOTE comment replacing Agent tool reference"
    - "skills/commit/SKILL.md body does NOT contain references to readme-sync.md or ~/.claude/rules"
    - "All 10 skills deployed to VPS at /home/uws/.hermes/skills/<name>/SKILL.md via direct cp (NOT hermes skills install)"
  artifacts:
    - path: "skills/caveman/SKILL.md"
      provides: "Hermes-formatted caveman communication skill"
      contains: "metadata:\n  hermes:"
    - path: "skills/commit/SKILL.md"
      provides: "Hermes git commit workflow skill (Claude README-sync checkpoint removed)"
      contains: "metadata:\n  hermes:"
    - path: "skills/triage-issue/SKILL.md"
      provides: "Hermes triage skill with TRANSLATION NOTE replacing Agent/Explore"
      contains: "TRANSLATION NOTE"
    - path: "skills/qa/SKILL.md"
      provides: "Hermes QA skill with TRANSLATION NOTE replacing Agent/Explore"
      contains: "TRANSLATION NOTE"
    - path: "tests/phase-03/skills-smoke.bats"
      provides: "10 dry-run smoke assertions via hermes-skill-run.sh"
  key_links:
    - from: "hermes-skills/test_skill_frontmatter.py"
      to: "skills/*/SKILL.md"
      via: "glob parametrize — auto-picks up all 10 skills"
      pattern: "glob.*SKILL.md"
    - from: "tests/phase-03/skills-smoke.bats"
      to: "scripts/hermes-skill-run.sh (deployed to VPS)"
      via: "ssh_cmd + --dry-run invocation"
      pattern: "hermes-skill-run.sh.*--dry-run"
---

<objective>
Wave 1 (parallel with Plan 02): port ~10 agent-agnostic and auto-translatable skills to Hermes format under skills/ directory and deploy to VPS (REQ-ws-004).

Purpose: These skills are the Tier 1 toolkit Phase 4's pipeline will call. They must exist on VPS under /home/uws/.hermes/skills/ with correct Hermes frontmatter before the pipeline is built.

Deployment method: direct cp to /home/uws/.hermes/skills/<name>/SKILL.md via rsync — NOT hermes skills install (which has no local-path mode as confirmed by live VPS RESEARCH.md).

Skills selected (from RESEARCH.md confirmed agent-agnostic or auto_translated candidates):
1. caveman — agent_agnostic, pure prose
2. commit — auto_translated (Bash→terminal); remove Claude README-sync step
3. diagnose — agent_agnostic, pure debug methodology
4. triage-issue — requires_manual_port (Agent/Explore → TRANSLATION NOTE)
5. qa — requires_manual_port (Agent/Explore → TRANSLATION NOTE)
6. knowledge — agent_agnostic, pure consultation hierarchy prose
7. write-a-prd — agent_agnostic or auto_translated writing instructions
8. zoom-out — agent_agnostic, reflection/metacognition (remove disable-model-invocation key)
9. grill-me — agent_agnostic, interview methodology
10. triage — auto_translated, lightweight issue triage

Output:
- 10 skills/*/SKILL.md files
- tests/phase-03/skills-smoke.bats (10 dry-run smoke assertions)
- VPS: all 10 skills deployed to /home/uws/.hermes/skills/
</objective>

<execution_context>
@/Users/caiobellizzi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caiobellizzi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-skill-toolkit/03-RESEARCH.md
@.planning/phases/03-skill-toolkit/03-PATTERNS.md
@.planning/phases/03-skill-toolkit/03-01-SUMMARY.md

Key facts:
- Source skills: ~/.claude/skills/<name>/SKILL.md (Mac-local)
- Target (local repo): skills/<name>/SKILL.md
- Target (VPS): /home/uws/.hermes/skills/<name>/SKILL.md — use cp/rsync NOT hermes skills install
- Hermes frontmatter required: name, description, version, author, license, platforms, metadata.hermes.tags
- NEVER add: tools:, mcpServers:, hooks: keys in frontmatter (Hermes ignores/errors on Claude Code tool allowlists)
- zoom-out: remove disable-model-invocation: true (Claude Code-specific key — Hermes ignores it but keep file clean)
- commit: remove reference to "README sync check" / readme-sync.md step (Claude ecosystem only)
- triage-issue + qa: replace "Agent tool with subagent_type=Explore" with TRANSLATION NOTE
- DIRECT_TOOL_MAP for body: Bash→terminal, Read→read_file, Write→write_file, Edit→edit_file, Grep→search, Glob→find_files
- Every skill body must include "## Dry-run behavior" section
- Skill deploy: rsync skills/ to VPS; mkdir -p per skill dir using cp pattern
- Plan 01 must be complete: hermes-skill-run.sh deployed; bats helpers exist
</context>

<interfaces>
Canonical Hermes frontmatter (from PATTERNS.md Shared Patterns, RESEARCH.md live VPS verification):
```yaml
---
name: <slug-matching-directory-name>
description: "<trigger phrase for skill matching>"
version: 1.0.0
author: ultra-workshop (ported from Claude Code skill)
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [tag1, tag2]
---
```

TRANSLATION NOTE format (for requires_manual_port body substitutions):
```
# [TRANSLATION NOTE: Agent/Explore → Hermes has no direct subagent tool.
#  Use search and read_file tools to investigate directly.
#  Original: "Use the Agent tool with subagent_type=Explore to deeply investigate..."]
Use search and read_file tools to investigate the codebase directly...
```

Dry-run body section (add to every skill):
```markdown
## Dry-run behavior
If the trigger contains `--dry-run`, print the steps that would execute and the arguments extracted, then stop without taking any action.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port agent-agnostic skills (caveman, diagnose, knowledge, write-a-prd, zoom-out, grill-me, triage)</name>
  <files>skills/caveman/SKILL.md, skills/diagnose/SKILL.md, skills/knowledge/SKILL.md, skills/write-a-prd/SKILL.md, skills/zoom-out/SKILL.md, skills/grill-me/SKILL.md, skills/triage/SKILL.md</files>
  <read_first>
    - /Users/caiobellizzi/.claude/skills/caveman/SKILL.md — source for caveman port
    - /Users/caiobellizzi/.claude/skills/diagnose/SKILL.md — source for diagnose port
    - /Users/caiobellizzi/.claude/skills/knowledge/SKILL.md — source for knowledge port (if exists)
    - /Users/caiobellizzi/.claude/skills/write-a-prd/SKILL.md — source for write-a-prd (if exists; create minimal if absent)
    - /Users/caiobellizzi/.claude/skills/zoom-out/SKILL.md — source for zoom-out (if exists; create minimal if absent)
    - /Users/caiobellizzi/.claude/skills/grill-me/SKILL.md — source for grill-me (if exists; create minimal if absent)
    - /Users/caiobellizzi/.claude/skills/triage/SKILL.md — source for triage (if exists; create minimal if absent)
    - .planning/phases/03-skill-toolkit/03-PATTERNS.md — section "skills/caveman/SKILL.md": exact frontmatter delta showing what to ADD; section "Shared Patterns > Hermes SKILL.md frontmatter (canonical)"
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — zoom-out note: remove "disable-model-invocation: true" (Claude Code-specific)
  </read_first>
  <action>
    For each of the 7 skills, read the source at ~/.claude/skills/<name>/SKILL.md then create skills/<name>/SKILL.md.

    Frontmatter transformation for all 7:
    - KEEP: name, description (as-is from source, or set if absent)
    - ADD: version: 1.0.0; author: ultra-workshop (ported from Claude Code skill); license: MIT; platforms: [linux, macos, windows]; metadata.hermes.tags (use appropriate tags per skill — see list below)
    - REMOVE: any of tools:, mcpServers:, hooks:, disable-model-invocation: (all Claude Code-specific)

    Tags per skill:
    - caveman: [communication, brevity, tokens]
    - diagnose: [debugging, diagnosis, bug-fix]
    - knowledge: [knowledge, research, lookup]
    - write-a-prd: [prd, product, requirements, planning]
    - zoom-out: [reflection, metacognition, planning]
    - grill-me: [interview, requirements, clarification]
    - triage: [triage, issue, bug]

    Body transformation:
    - Copy body verbatim for pure prose skills (caveman, diagnose, knowledge, zoom-out, grill-me)
    - For write-a-prd and triage: apply DIRECT_TOOL_MAP to any inline tool references in the body prose (Bash→terminal etc.)
    - Add "## Dry-run behavior" section at the end of every skill body

    If source skill does NOT exist at ~/.claude/skills/<name>/SKILL.md: create a minimal but complete Hermes skill body describing what the skill does based on the name. Do NOT leave the body empty.

    After creating all 7 files, run `pytest hermes-skills/test_skill_frontmatter.py -v` to verify the auto-parametrize picks them up and all pass.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-workshop && python -m pytest hermes-skills/test_skill_frontmatter.py -v -k "caveman or diagnose or knowledge or write-a-prd or zoom-out or grill-me or triage" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 7 skills/*/SKILL.md files exist (ls skills/ shows caveman, diagnose, knowledge, write-a-prd, zoom-out, grill-me, triage)
    - Each file's frontmatter has: name, description, version, author, license, platforms, metadata.hermes.tags
    - `grep -rl "tools:" skills/` returns empty (no forbidden frontmatter key)
    - `grep -rl "disable-model-invocation" skills/` returns empty (zoom-out key removed)
    - `pytest hermes-skills/test_skill_frontmatter.py -v -k "caveman or diagnose or knowledge"` → all matching tests pass
    - Each file ends with a "## Dry-run behavior" section
    - skills/caveman/SKILL.md frontmatter name field = "caveman" (matches directory)
  </acceptance_criteria>
  <done>7 agent-agnostic skills created with valid Hermes frontmatter; pytest frontmatter validator confirms them all</done>
</task>

<task type="auto">
  <name>Task 2: Port commit + requires-manual skills, deploy all 10, create bats smoke</name>
  <files>skills/commit/SKILL.md, skills/triage-issue/SKILL.md, skills/qa/SKILL.md, tests/phase-03/skills-smoke.bats</files>
  <read_first>
    - /Users/caiobellizzi/.claude/skills/commit/SKILL.md — source; note any README-sync checkpoint lines to remove
    - /Users/caiobellizzi/.claude/skills/triage-issue/SKILL.md — source; find "Agent tool with subagent_type=Explore" line to replace with TRANSLATION NOTE
    - /Users/caiobellizzi/.claude/skills/qa/SKILL.md — source; find "Agent (subagent_type=Explore)" line to replace with TRANSLATION NOTE
    - .planning/phases/03-skill-toolkit/03-PATTERNS.md — sections for commit, triage-issue, qa translation deltas; section "tests/phase-03/*.bats" for skills-smoke.bats @test structure
    - tests/phase-03/helpers.bash — ssh_cmd() function used in smoke tests
    - tests/phase-02/hitl-restart.bats — @test structure with load helpers, run, status, output assertions
    - .planning/phases/03-skill-toolkit/03-RESEARCH.md — "Hermes Skill Install Mechanism" section: confirmed `hermes skills install` does not accept local paths; use direct cp instead
  </read_first>
  <action>
    Create skills/commit/SKILL.md:
    - Source: ~/.claude/skills/commit/SKILL.md
    - ADD frontmatter: version: 1.0.0, author: ultra-workshop (ported), license: MIT, platforms: [linux, macos, windows], metadata.hermes.tags: [git, commit, workflow]
    - Body: copy mostly verbatim; REMOVE lines referencing "README sync check", "readme-sync.md", or "~/.claude/rules/readme-sync.md" (these are Claude ecosystem steps with no Hermes equivalent); replace any `Bash` tool name references in body prose with `terminal`
    - Add "## Dry-run behavior" section at end

    Create skills/triage-issue/SKILL.md:
    - Source: ~/.claude/skills/triage-issue/SKILL.md
    - ADD frontmatter: version, author, license, platforms, tags: [triage, bug, github, issue]
    - Body: locate line containing "Agent tool with subagent_type=Explore" or similar; replace with:
      # [TRANSLATION NOTE: Agent/Explore subagent has no direct Hermes equivalent.
      #  Use search and read_file tools to investigate the codebase directly.
      #  Original Claude Code instruction: "Use the Agent tool with subagent_type=Explore to deeply investigate..."]
      Use search and read_file tools to deeply investigate the codebase...
    - Apply DIRECT_TOOL_MAP to remaining tool references in body
    - Add "## Dry-run behavior" section at end

    Create skills/qa/SKILL.md:
    - Source: ~/.claude/skills/qa/SKILL.md
    - ADD frontmatter: tags: [qa, bug-report, github, issue]
    - Body: replace "kick off an Agent (subagent_type=Explore)" with same TRANSLATION NOTE pattern
    - Apply DIRECT_TOOL_MAP, add "## Dry-run behavior"

    Deploy all 10 skills to VPS using direct cp (NOT hermes skills install — no local path mode exists):
    For each skill <name>:
      ssh root@31.97.130.253 "mkdir -p /home/uws/.hermes/skills/<name>"
      rsync -avz skills/<name>/SKILL.md root@31.97.130.253:/home/uws/.hermes/skills/<name>/SKILL.md

    Or batch with rsync:
    rsync -avz skills/ root@31.97.130.253:/home/uws/.hermes/skills/

    Verify deployment: ssh root@31.97.130.253 "ls /home/uws/.hermes/skills/ | grep -E 'caveman|commit|diagnose|triage'"

    Create tests/phase-03/skills-smoke.bats:
    #!/usr/bin/env bats header + `load helpers` (resolves to tests/phase-03/helpers.bash).
    10 @test blocks, one per skill, each: `run ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh <name> --dry-run"` followed by `[ "$status" -eq 0 ]`.
    Skills: caveman, commit, diagnose, triage-issue, qa, knowledge, write-a-prd, zoom-out, grill-me, triage.

    CRITICAL: --dry-run is passed as part of the QUERY to hermes chat. Each skill body's "## Dry-run behavior" section tells Hermes to respond to "--dry-run" without tool calls. hermes-skill-run.sh has --yolo to prevent interactive approval hangs (Pitfall 3 in RESEARCH.md).

    Run: bats tests/phase-03/skills-smoke.bats — all 10 must pass.
  </action>
  <verify>
    <automated>bats /Users/caiobellizzi/Documents/Projects/ultra-workshop/tests/phase-03/skills-smoke.bats</automated>
  </verify>
  <acceptance_criteria>
    - skills/commit/SKILL.md exists; `grep -i "readme-sync" skills/commit/SKILL.md` returns empty
    - skills/triage-issue/SKILL.md contains "TRANSLATION NOTE" comment; `grep "subagent_type=Explore" skills/triage-issue/SKILL.md` returns empty
    - skills/qa/SKILL.md contains "TRANSLATION NOTE" comment; `grep "subagent_type=Explore" skills/qa/SKILL.md` returns empty
    - `grep -rl "tools:" skills/` returns empty (no forbidden frontmatter key in any of the 10 skills)
    - `pytest hermes-skills/test_skill_frontmatter.py -v` → 20+ tests pass (2 per skill × 10 skills)
    - `bats tests/phase-03/skills-smoke.bats` → 10/10 pass
    - VPS: `ssh root@31.97.130.253 "ls /home/uws/.hermes/skills/"` shows caveman, commit, diagnose, triage-issue, qa, knowledge, write-a-prd, zoom-out, grill-me, triage
  </acceptance_criteria>
  <done>All 10 Tier 1 skills ported, deployed to VPS via direct cp, and passing dry-run smoke tests; REQ-ws-004 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mac skills/ → VPS /home/uws/.hermes/skills/ | rsync as root deploys SKILL.md files; Hermes reads them at runtime as uws |
| bats SSH → VPS hermes-skill-run.sh → hermes chat | Smoke test invocation; --dry-run prevents real LLM calls in skill body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | TRANSLATION NOTE inline comment | accept | Notes are prose read by Hermes agent as instructions; no code execution of NOTE content |
| T-03-10 | Denial of Service | hermes chat --yolo with dry-run query hangs | mitigate | --yolo in hermes-skill-run.sh bypasses interactive approval prompts; --max-turns 3 caps iterations |
| T-03-11 | Elevation | rsync as root to /home/uws/.hermes/skills/ | accept | Root SSH required for VPS operations (Phase 2 pattern); only SKILL.md text files deployed; path is in Hermes ReadWritePaths |
| T-03-12 | Tampering | commit skill body referencing sensitive git patterns | mitigate | Commit skill body reviewed before deploy; describes behavior in prose; does not execute autonomously without Hermes orchestration |
| T-03-SC | Tampering | No package installs in this plan | accept | All 10 skills are SKILL.md markdown files; no pip/npm installs |
</threat_model>

<verification>
After Plan 03 completes:
1. `pytest hermes-skills/test_skill_frontmatter.py -v` → 20+ tests pass
2. `bats tests/phase-03/skills-smoke.bats` → 10/10 pass
3. `grep -rl "tools:" skills/` returns empty
4. VPS: `ssh root@31.97.130.253 "ls /home/uws/.hermes/skills/"` shows all 10 skill directories
5. `grep "TRANSLATION NOTE" skills/triage-issue/SKILL.md skills/qa/SKILL.md` returns matches
6. `grep "readme-sync" skills/commit/SKILL.md` returns empty
</verification>

<success_criteria>
- 10 skills under skills/ with valid Hermes frontmatter
- pytest frontmatter validator: 20+ tests pass
- bats skills-smoke.bats: 10/10 dry-run tests pass
- All 10 deployed to VPS /home/uws/.hermes/skills/ via direct cp
- REQ-ws-004 satisfied
</success_criteria>

<output>
Create .planning/phases/03-skill-toolkit/03-03-SUMMARY.md when done
</output>
