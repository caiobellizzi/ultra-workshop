---
phase: 03-skill-toolkit
plan: "03"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - skills/caveman/SKILL.md
  - skills/commit/SKILL.md
  - skills/diagnose/SKILL.md
  - skills/triage-issue/SKILL.md
  - skills/qa/SKILL.md
  - skills/knowledge/SKILL.md
  - skills/write-a-prd/SKILL.md
  - skills/zoom-out/SKILL.md
  - skills/grill-me/SKILL.md
  - skills/triage/SKILL.md
  - tests/phase-03/skills-smoke.bats
autonomous: true
requirements: [REQ-ws-004]

must_haves:
  truths:
    - "10 skills exist under skills/ with valid Hermes frontmatter (name, description, version, author, license, platforms, metadata.hermes.tags)"
    - "No skill has tools:, mcpServers:, or hooks: frontmatter keys"
    - "hermes-skills/test_skill_frontmatter.py passes 100% (20+ tests across 10 skills)"
    - "tests/phase-03/skills-smoke.bats: all 10 dry-run smoke tests pass (each skill exits 0 on --dry-run)"
    - "requires_manual_port skills (triage-issue, qa) have TRANSLATION NOTE comments in body where Agent tool was replaced"
  artifacts:
    - path: "skills/caveman/SKILL.md"
      provides: "Hermes-formatted caveman communication mode skill"
      contains: "metadata:\n  hermes:"
    - path: "skills/commit/SKILL.md"
      provides: "Hermes-formatted git commit workflow skill (no Claude README-sync checkpoint)"
      contains: "metadata:\n  hermes:"
    - path: "skills/diagnose/SKILL.md"
      provides: "Hermes-formatted diagnosis loop skill"
      contains: "metadata:\n  hermes:"
    - path: "skills/triage-issue/SKILL.md"
      provides: "Hermes-formatted triage skill with TRANSLATION NOTE replacing Agent tool"
      contains: "TRANSLATION NOTE"
    - path: "skills/qa/SKILL.md"
      provides: "Hermes-formatted QA session skill with TRANSLATION NOTE replacing Agent tool"
      contains: "TRANSLATION NOTE"
    - path: "skills/knowledge/SKILL.md"
      provides: "Hermes knowledge hierarchy skill (agent-agnostic — no Claude-specific tools)"
      contains: "metadata:\n  hermes:"
    - path: "skills/write-a-prd/SKILL.md"
      provides: "Hermes write-a-prd skill"
      contains: "metadata:\n  hermes:"
    - path: "skills/zoom-out/SKILL.md"
      provides: "Hermes zoom-out reflection skill"
      contains: "metadata:\n  hermes:"
    - path: "skills/grill-me/SKILL.md"
      provides: "Hermes grill-me interview skill"
      contains: "metadata:\n  hermes:"
    - path: "skills/triage/SKILL.md"
      provides: "Hermes triage skill (lightweight issue triage — different from triage-issue)"
      contains: "metadata:\n  hermes:"
  key_links:
    - from: "hermes-skills/test_skill_frontmatter.py"
      to: "skills/*/SKILL.md"
      via: "glob parametrize — auto-picks up all 10 skills"
      pattern: "glob.*SKILL.md"
    - from: "tests/phase-03/skills-smoke.bats"
      to: "scripts/hermes-skill-run.sh"
      via: "ssh_cmd + --dry-run"
      pattern: "hermes-skill-run.sh.*--dry-run"
---

<objective>
Wave 1 (parallel with Plan 02): port ~10 agent-agnostic and auto-translatable skills to Hermes format under skills/ directory (REQ-ws-004).

Purpose: These skills are the Tier 1 toolkit that the Phase 4 pipeline will call. They must exist on the VPS under ~/.hermes/skills/ with correct frontmatter before the pipeline is built.

Skills selected (from Phase 2 patterns + researcher discretion — all agent-agnostic or auto_translated category):
1. caveman — agent_agnostic, no tool refs
2. commit — auto_translated (Bash → terminal); remove Claude README-sync checkpoint
3. diagnose — agent_agnostic, pure prose
4. triage-issue — requires_manual_port (Agent tool replaced with TRANSLATION NOTE)
5. qa — requires_manual_port (Agent tool replaced with TRANSLATION NOTE)
6. knowledge — agent_agnostic, pure prose (consultation hierarchy)
7. write-a-prd — agent_agnostic, pure writing instructions
8. zoom-out — agent_agnostic, reflection/metacognition
9. grill-me — agent_agnostic, interview methodology
10. triage — agent_agnostic, lightweight issue triage

Output:
- 10 skills/*/SKILL.md files (deploy target: VPS ~/.hermes/skills/)
- tests/phase-03/skills-smoke.bats (10 dry-run smoke assertions)
- Updated 03-VALIDATION.md Per-Task Verification Map
</objective>

<execution_context>
@/Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-RESEARCH.md
@/Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-PATTERNS.md

Key facts:
- Source skills at ~/.claude/skills/<name>/SKILL.md on Mac
- Target: skills/<name>/SKILL.md in repo (deployed to VPS ~/.hermes/skills/<name>/SKILL.md via rsync)
- Hermes frontmatter required: name, description, version, author, license, platforms, metadata.hermes.tags
- NEVER add: tools:, mcpServers:, hooks: keys in frontmatter
- Body: copy as-is for agent_agnostic; apply DIRECT_TOOL_MAP for auto_translated; add TRANSLATION NOTE for requires_manual_port
- DIRECT_TOOL_MAP: Read→read_file, Write→write_file, Edit→edit_file, Bash→terminal, Grep→search, Glob→find_files, WebFetch→http_request, WebSearch→web_search
- commit skill: remove the "README sync check (mandatory)" step (Claude Code ecosystem only)
- triage-issue + qa: replace "Use the Agent tool with subagent_type=Explore" with a TRANSLATION NOTE comment pointing to search + read_file tools
- hermes-skill-run.sh --dry-run (from Plan 01): skill body must detect "--dry-run" in the query and respond without executing real actions
- Plan 01 must be complete (hermes-skill-run.sh deployed to VPS) for bats smoke to pass
</context>

<interfaces>
From PATTERNS.md — canonical Hermes frontmatter (Shared Patterns section):
```yaml
---
name: <slug>
description: "<trigger phrase>"
version: 1.0.0
author: ultra-workshop (ported from Claude Code skill)
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [tag1, tag2]
---
```

TRANSLATION NOTE format (for requires_manual_port body replacements):
```
# [TRANSLATION NOTE: Agent/Explore → Hermes has no direct subagent tool.
#  Use search + read_file tools to investigate directly.
#  Original: "Use the Agent tool with subagent_type=Explore to deeply investigate..."]
Use search and read_file tools to investigate the codebase directly...
```

Dry-run body pattern (add to all skills):
```markdown
## Dry-run behavior
If the trigger contains `--dry-run`, print the steps that would execute and the arguments extracted, then stop without taking any action.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Port agent-agnostic skills (caveman, diagnose, knowledge, write-a-prd, zoom-out, grill-me, triage)</name>
  <files>skills/caveman/SKILL.md, skills/diagnose/SKILL.md, skills/knowledge/SKILL.md, skills/write-a-prd/SKILL.md, skills/zoom-out/SKILL.md, skills/grill-me/SKILL.md, skills/triage/SKILL.md</files>
  <read_first>
    - /Users/caiobellizzi/.claude/skills/caveman/SKILL.md — source for caveman port
    - /Users/caiobellizzi/.claude/skills/diagnose/SKILL.md — source for diagnose port
    - /Users/caiobellizzi/.claude/skills/knowledge/SKILL.md — source for knowledge port
    - /Users/caiobellizzi/.claude/skills/write-a-prd/SKILL.md — source for write-a-prd port (if exists; else create from description)
    - /Users/caiobellizzi/.claude/skills/zoom-out/SKILL.md — source for zoom-out port (if exists; else create from description)
    - /Users/caiobellizzi/.claude/skills/grill-me/SKILL.md — source for grill-me port (if exists; else create from description)
    - /Users/caiobellizzi/.claude/skills/triage/SKILL.md — source for triage port (if exists; else create from description)
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-PATTERNS.md — section "skills/caveman/SKILL.md" showing exact frontmatter delta; Shared Patterns "Hermes SKILL.md frontmatter (canonical)"
  </read_first>
  <action>
    For each skill, read the source ~/.claude/skills/<name>/SKILL.md then create skills/<name>/SKILL.md:

    1. skills/caveman/SKILL.md:
       - Keep existing name + description (they are already clean)
       - ADD: version: 1.0.0, author: ultra-workshop (ported from Claude Code skill), license: MIT, platforms: [linux, macos, windows], metadata.hermes.tags: [communication, brevity, tokens]
       - Body: copy verbatim (no tool references — pure prose)
       - Add "## Dry-run behavior" section at end

    2. skills/diagnose/SKILL.md:
       - Body: pure prose methodology — copy verbatim
       - Tags: [debugging, diagnosis, bug-fix]
       - Add "## Dry-run behavior" section at end

    3. skills/knowledge/SKILL.md:
       - Body: copy verbatim (no Claude-specific tool references in the knowledge hierarchy description)
       - Tags: [knowledge, research, lookup]
       - Add "## Dry-run behavior" section at end

    4. skills/write-a-prd/SKILL.md:
       - Body: copy verbatim if no Claude-specific tools; replace any Bash references with terminal
       - Tags: [prd, product, requirements, planning]
       - Add "## Dry-run behavior" section at end

    5. skills/zoom-out/SKILL.md:
       - Body: copy verbatim (reflection skill — pure prose)
       - Tags: [reflection, metacognition, planning]
       - Add "## Dry-run behavior" section at end

    6. skills/grill-me/SKILL.md:
       - Body: copy verbatim (interview methodology — pure prose)
       - Tags: [interview, requirements, clarification]
       - Add "## Dry-run behavior" section at end

    7. skills/triage/SKILL.md:
       - Body: copy verbatim; if source uses any tool refs, apply DIRECT_TOOL_MAP
       - Tags: [triage, issue, bug]
       - Add "## Dry-run behavior" section at end

    If any source skill does not exist at ~/.claude/skills/<name>/SKILL.md, create a minimal but complete Hermes skill body describing what the skill should do based on the name and description — do NOT leave it empty.

    After creating all 7 files, run `pytest hermes-skills/test_skill_frontmatter.py -v` — it must pick up the new skills and pass for all 7.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-workshop && python -m pytest hermes-skills/test_skill_frontmatter.py -v -k "caveman or diagnose or knowledge or write-a-prd or zoom-out or grill-me or triage" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All 7 skills/*/SKILL.md files exist
    - Each file has frontmatter with name, description, version, author, license, platforms, metadata.hermes.tags
    - `grep -l "tools:" skills/*/SKILL.md` returns empty (no Claude tools frontmatter)
    - `pytest hermes-skills/test_skill_frontmatter.py -v` passes for all 7 skills (name + description present; no forbidden keys)
    - Each file has "## Dry-run behavior" section
    - skills/caveman/SKILL.md name: field = "caveman" (matches directory)
  </acceptance_criteria>
  <done>7 agent-agnostic skills created with valid Hermes frontmatter; pytest frontmatter validator confirms them all</done>
</task>

<task type="auto">
  <name>Task 2: Port auto-translated + requires-manual skills (commit, triage-issue, qa) + deploy all 10 + bats smoke</name>
  <files>skills/commit/SKILL.md, skills/triage-issue/SKILL.md, skills/qa/SKILL.md, tests/phase-03/skills-smoke.bats</files>
  <read_first>
    - /Users/caiobellizzi/.claude/skills/commit/SKILL.md — source for commit port; note README-sync checkpoint reference to remove
    - /Users/caiobellizzi/.claude/skills/triage-issue/SKILL.md — source; note "Agent tool with subagent_type=Explore" to replace with TRANSLATION NOTE
    - /Users/caiobellizzi/.claude/skills/qa/SKILL.md — source; note "Agent (subagent_type=Explore)" to replace with TRANSLATION NOTE
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/.planning/phases/03-skill-toolkit/03-PATTERNS.md — sections for commit, triage-issue, qa translation deltas
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/tests/phase-02/helpers.bash — helpers structure to reuse in skills-smoke.bats
    - /Users/caiobellizzi/Documents/Projects/ultra-workshop/tests/phase-02/hitl-restart.bats — @test structure, `load helpers`, run/status/output pattern
  </read_first>
  <action>
    Create skills/commit/SKILL.md:
    - Source: ~/.claude/skills/commit/SKILL.md
    - Frontmatter ADD: version, author, license, platforms, tags: [git, commit, workflow]
    - Body: copy mostly verbatim; REMOVE the "README sync check (mandatory)" step (lines referencing ~/.claude/rules/readme-sync.md — Claude ecosystem only); replace any implicit Bash references with terminal in prose descriptions
    - Add "## Dry-run behavior" section
    - name: "commit"

    Create skills/triage-issue/SKILL.md:
    - Source: ~/.claude/skills/triage-issue/SKILL.md
    - Frontmatter ADD: version, author, license, platforms, tags: [triage, bug, github, issue]
    - Body: replace "Use the Agent tool with subagent_type=Explore to deeply investigate..." with:
      "# [TRANSLATION NOTE: Agent/Explore subagent has no direct Hermes equivalent.\n#  Use search and read_file tools to investigate directly.]\nUse search and read_file tools to deeply investigate the codebase..."
    - Add "## Dry-run behavior" section

    Create skills/qa/SKILL.md:
    - Source: ~/.claude/skills/qa/SKILL.md
    - Frontmatter ADD: version, author, license, platforms, tags: [qa, bug-report, github, issue]
    - Body: replace "kick off an Agent (subagent_type=Explore)" with TRANSLATION NOTE (same pattern as triage-issue)
    - Add "## Dry-run behavior" section

    Deploy all 10 skills to VPS:
    rsync -avz skills/ root@31.97.130.253:/home/uws/.hermes/skills/
    (This deploys skills/caveman/, skills/commit/, etc. to /home/uws/.hermes/skills/caveman/, etc.)

    Verify deployment:
    ssh root@31.97.130.253 "ls /home/uws/.hermes/skills/" | grep -c "caveman"

    Create tests/phase-03/skills-smoke.bats:
    - Header: reference REQ-ws-004 skills smoke test
    - `load helpers`
    - 10 @test blocks, one per skill: each runs `ssh_cmd "bash /opt/ultra-workshop/scripts/hermes-skill-run.sh <skill-name> --dry-run"` and asserts `[ "$status" -eq 0 ]`
    - Skills: caveman, commit, diagnose, triage-issue, qa, knowledge, write-a-prd, zoom-out, grill-me, triage

    Run bats tests/phase-03/skills-smoke.bats — all 10 must pass.

    CRITICAL: the smoke uses --dry-run which is handled at the skill body level. The hermes-skill-run.sh wrapper passes "--dry-run" as part of the QUERY to `hermes chat`. Each skill's "## Dry-run behavior" section instructs Hermes to respond to "--dry-run" in the trigger without executing real actions. If any skill's dry-run hangs (Pitfall 2: --yolo required for non-interactive), confirm hermes-skill-run.sh has --yolo flag.
  </action>
  <verify>
    <automated>bats /Users/caiobellizzi/Documents/Projects/ultra-workshop/tests/phase-03/skills-smoke.bats</automated>
  </verify>
  <acceptance_criteria>
    - skills/commit/SKILL.md exists with tags: [git, commit, workflow]; body does NOT contain "readme-sync.md" or "~/.claude/rules"
    - skills/triage-issue/SKILL.md exists with TRANSLATION NOTE comment; does NOT contain "Agent tool with subagent_type=Explore"
    - skills/qa/SKILL.md exists with TRANSLATION NOTE comment; does NOT contain "subagent_type=Explore"
    - `pytest hermes-skills/test_skill_frontmatter.py -v` → 20+ tests pass (2 per skill × 10 skills)
    - `bats tests/phase-03/skills-smoke.bats` → 10/10 pass
    - VPS: `ssh root@31.97.130.253 "ls /home/uws/.hermes/skills/ | wc -l"` → at least 10 (plus pre-existing Hermes builtins)
    - `grep -r "tools:" skills/*/SKILL.md` returns empty
  </acceptance_criteria>
  <done>All 10 Tier 1 skills are deployed to VPS Hermes and pass dry-run smoke tests; REQ-ws-004 satisfied</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mac skills/ dir → VPS ~/.hermes/skills/ | rsync deploys skill files; VPS Hermes reads SKILL.md at runtime |
| bats SSH → VPS hermes-skill-run.sh → hermes chat | Smoke test flow; --dry-run prevents real LLM calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | SKILL.md body with TRANSLATION NOTE | accept | Notes are inline comments; Hermes agent reads them as instructions; no execution of NOTE content |
| T-03-09 | Denial of Service | hermes chat --yolo with dry-run query | accept | --dry-run in query causes skill to respond without tool calls; max-turns 3 prevents runaway |
| T-03-10 | Elevation | rsync to VPS ~/.hermes/skills/ | accept | rsync as root to VPS; VPS access already required for Phase 2 ops; only skill definition files are written |
| T-03-11 | Tampering | commit skill body could contain secret references | mitigate | Commit skill body is prose instructions; reviewed before deploy; does not execute autonomously |
| T-03-SC | Tampering | No package installs in this plan | accept | All 10 skills are SKILL.md markdown files + pytest tests — no package installs |
</threat_model>

<verification>
After Plan 03 completes:
1. `pytest hermes-skills/test_skill_frontmatter.py -v` → 20+ tests pass
2. `bats tests/phase-03/skills-smoke.bats` → 10/10 pass
3. `grep -r "tools:" skills/*/SKILL.md` returns empty (no forbidden frontmatter keys)
4. VPS: `ssh root@31.97.130.253 "ls /home/uws/.hermes/skills/"` shows caveman, commit, diagnose, triage-issue, qa, knowledge, write-a-prd, zoom-out, grill-me, triage
5. `grep -r "subagent_type=Explore" skills/triage-issue/SKILL.md` returns empty (TRANSLATION NOTE replaced it)
</verification>

<success_criteria>
- 10 skills under skills/ with valid Hermes frontmatter
- pytest frontmatter validator: 20+ tests pass
- bats skills-smoke.bats: 10/10 dry-run tests pass
- REQ-ws-004 satisfied: ~10 agent-agnostic skills live in ~/.hermes/skills/ passing dry-run
</success_criteria>

<output>
Create .planning/phases/03-skill-toolkit/03-03-SUMMARY.md when done
</output>
