---
phase: 04-build-fix-pipeline
plan: "02"
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - workshop/nodes/__init__.py
  - workshop/nodes/triage.py
  - workshop/nodes/planner.py
  - workshop/nodes/coder.py
  - workshop/nodes/reviewer.py
  - workshop/nodes/pr_open.py
autonomous: true
requirements:
  - REQ-ws-007
  - REQ-ws-008
  - REQ-ws-010
  - REQ-ws-011

must_haves:
  truths:
    - "Each node module is importable from the Hermes venv"
    - "run_triage() returns a dict with 'task_type' key (BUILD or FIX)"
    - "run_coder() clones test-workshop-sandbox to a tmpdir and invokes aider_runner.py subprocess with shell=False"
    - "run_pr_opener() writes pending_hitl.db row BEFORE calling clarify_gateway.register"
    - "run_pr_opener() uses gh pr create with GH_TOKEN env injection (shell=False)"
    - "run_pr_opener() calls brain_http ingest for ADR write-back (non-blocking try/except)"
  artifacts:
    - path: "workshop/nodes/__init__.py"
      provides: "Package marker"
    - path: "workshop/nodes/triage.py"
      provides: "run_triage(task_id, goal, context) -> dict"
      contains: "def run_triage"
    - path: "workshop/nodes/planner.py"
      provides: "run_planner(task_id, goal, triage_result, context) -> Plan"
      contains: "def run_planner"
    - path: "workshop/nodes/coder.py"
      provides: "run_coder(task_id, plan, workspace_dir) -> Diff"
      contains: "def run_coder"
    - path: "workshop/nodes/reviewer.py"
      provides: "run_reviewer(task_id, plan, diff, context) -> Review"
      contains: "def run_reviewer"
    - path: "workshop/nodes/pr_open.py"
      provides: "run_pr_opener(task_id, branch, plan, diff, session_id, chat_id) -> str (PR URL or 'REJECTED')"
      contains: "def run_pr_opener"
  key_links:
    - from: "workshop/nodes/coder.py"
      to: "hermes-skills/aider_runner.py"
      via: "subprocess.run(shell=False)"
      pattern: "aider_runner\\.py"
    - from: "workshop/nodes/pr_open.py"
      to: "hermes-skills/startup-hitl-scan.py record_hitl_pause"
      via: "importlib.util.spec_from_file_location"
      pattern: "record_hitl_pause"
    - from: "workshop/nodes/pr_open.py"
      to: "gh pr create"
      via: "subprocess.run(shell=False, env=GH_TOKEN)"
      pattern: "gh.*pr.*create"
---

<objective>
Create the five pipeline node modules (workshop/nodes/): triage, planner, coder, reviewer, pr_open. These are thin wrappers that each call delegate_typed() for their specialist role, except coder (invokes aider_runner.py subprocess) and pr_open (HITL gate + gh CLI subprocess).

Purpose: Nodes consume the types and orchestrator from Plan 01. The workshop_build.py entry point (Plan 03) imports and calls these nodes sequentially.
Output: Five node modules with clear function signatures, HITL-durable write ordering, and shell=False subprocess calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-build-fix-pipeline/04-RESEARCH.md
@.planning/phases/04-build-fix-pipeline/04-PATTERNS.md
@.planning/phases/04-build-fix-pipeline/04-00-probe-results.md
@hermes-skills/startup-hitl-scan.py
@hermes-skills/startup-hitl-scan-hook/handler.py
@hermes-skills/aider_runner.py
</context>

<interfaces>
<!-- Contracts produced by Plan 01 that these nodes consume -->

From workshop/types.py:
```python
class Plan(BaseModel):
    goal: str
    steps: list[PlanStep]
    affected_files: list[str] = Field(default_factory=list)

class Diff(BaseModel):
    summary: str
    changes: list[FileChange]
    branch: str

class Review(BaseModel):
    passed: bool
    feedback: str
    blocking_issues: list[str] = Field(default_factory=list)

class Issue(BaseModel):
    url: str; title: str; body: str; number: int

class IngestResult(BaseModel):
    run_id: str; status: str; adr_path: str
```

From workshop/orchestrator.py:
```python
def delegate_typed(goal, context, output_schema, toolsets, max_retries=2) -> T: ...
```

From workshop/ledger.py:
```python
def append_progress(task_id: str, event: str, data: dict) -> None: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: triage.py, planner.py, reviewer.py (delegate_typed wrappers)</name>
  <files>workshop/nodes/__init__.py, workshop/nodes/triage.py, workshop/nodes/planner.py, workshop/nodes/reviewer.py</files>
  <action>
Create workshop/nodes/__init__.py as an empty file with deploy comment.

Create workshop/nodes/triage.py:
- Header: "# Deploy location: /opt/ultra-workshop/workshop/nodes/triage.py"
- from __future__ import annotations
- Imports: workshop.orchestrator.delegate_typed; workshop.ledger.append_progress
- Also import from workshop.types: (triage returns a dict, not a typed schema — keep simple)
- TRIAGE_PROMPT template string: given goal and optional issue context, classify task_type as "BUILD" or "FIX", return JSON {"task_type": "BUILD"|"FIX", "summary": "...", "complexity": "low"|"medium"|"high"}
- run_triage(task_id: str, goal: str, context: str = "") -> dict:
  - prompt = TRIAGE_PROMPT.format(goal=goal, context=context)
  - raw = delegate_typed(goal=prompt, context=context, output_schema=TriageResult, toolsets=["brain-query"])
  - append_progress(task_id, "triage_complete", {"task_type": raw.task_type, "complexity": raw.complexity})
  - return raw.model_dump()
- Define TriageResult(BaseModel) locally (not exported): task_type: str, summary: str, complexity: str

Create workshop/nodes/planner.py:
- Header: "# Deploy location: /opt/ultra-workshop/workshop/nodes/planner.py"
- run_planner(task_id: str, goal: str, triage_result: dict, context: str = "") -> Plan:
  - PLANNER_PROMPT: instruct subagent to query Brain for related code patterns, produce a JSON Plan
  - planner_context = context + f"\n\nTriage result: {json.dumps(triage_result)}"
  - result = delegate_typed(goal=PLANNER_PROMPT.format(goal=goal), context=planner_context, output_schema=Plan, toolsets=["brain-query"])
  - append_progress(task_id, "plan_complete", {"steps": len(result.steps), "affected_files": result.affected_files})
  - return result

Create workshop/nodes/reviewer.py:
- Header: "# Deploy location: /opt/ultra-workshop/workshop/nodes/reviewer.py"
- run_reviewer(task_id: str, plan: Plan, diff: Diff, context: str = "") -> Review:
  - REVIEWER_PROMPT: instruct to review diff against plan, return JSON Review with passed/feedback/blocking_issues
  - reviewer_context = context + f"\n\nPlan: {plan.model_dump_json()}\n\nDiff: {diff.model_dump_json()}"
  - result = delegate_typed(goal=REVIEWER_PROMPT, context=reviewer_context, output_schema=Review, toolsets=[])
  - append_progress(task_id, "review_complete", {"passed": result.passed, "blocking_count": len(result.blocking_issues)})
  - return result

Imports across all three node files: always use from __future__ import annotations; always flush prints.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-workshop && python -c "from workshop.nodes.triage import run_triage; from workshop.nodes.planner import run_planner; from workshop.nodes.reviewer import run_reviewer; print('ok')"</automated>
  </verify>
  <done>
    - All three node modules importable
    - run_triage, run_planner, run_reviewer have correct signatures per interfaces block
    - grep -r "shell=True" workshop/nodes/ → no matches
  </done>
</task>

<task type="auto">
  <name>Task 2: coder.py and pr_open.py (subprocess + HITL nodes)</name>
  <files>workshop/nodes/coder.py, workshop/nodes/pr_open.py</files>
  <action>
Create workshop/nodes/coder.py following PATTERNS.md §workshop/nodes/coder.py exactly:
- Header: "# Deploy location: /opt/ultra-workshop/workshop/nodes/coder.py"
- from __future__ import annotations
- Imports: subprocess, sys, os, json, tempfile, pathlib Path; workshop.types Plan, Diff, FileChange; workshop.ledger append_progress
- SANDBOX_REPO = "https://github.com/caiobellizzi/test-workshop-sandbox.git"
- AIDER_RUNNER = Path("/opt/ultra-workshop/hermes-skills/aider_runner.py")
- run_coder(task_id: str, plan: Plan, workspace_dir: str | None = None) -> Diff:
  - If workspace_dir is None: create tmpdir with tempfile.mkdtemp(prefix=f"uws-sandbox-{task_id}-")
  - Clone SANDBOX_REPO into workspace_dir if .git not present:
    subprocess.run(["git", "clone", SANDBOX_REPO, workspace_dir], check=True, shell=False)
  - Create branch: subprocess.run(["git", "checkout", "-b", f"workshop/{task_id}"], cwd=workspace_dir, check=True, shell=False)
  - Invoke aider_runner.py:
    result = subprocess.run(
        [sys.executable, str(AIDER_RUNNER), "--task", plan.goal, "--workspace-file", workspace_dir + "/README.md"],
        capture_output=True, text=True, shell=False, cwd=workspace_dir,
        env={**os.environ},
    )
  - If result.returncode != 0: raise RuntimeError(f"aider_runner failed (exit {result.returncode}): {result.stderr[:500]}")
  - Parse stdout for diff summary (use first 500 chars of stdout as summary if no structured output)
  - branch = f"workshop/{task_id}"
  - diff = Diff(summary=result.stdout[:500], changes=[], branch=branch)
  - append_progress(task_id, "coder_complete", {"branch": branch, "exit_code": result.returncode})
  - return diff

Create workshop/nodes/pr_open.py following PATTERNS.md §workshop/nodes/pr_open.py exactly:
- Header: "# Deploy location: /opt/ultra-workshop/workshop/nodes/pr_open.py"
- from __future__ import annotations
- Imports: os, subprocess, sys, uuid, importlib.util, json; pathlib Path; workshop.types Plan, Diff, IngestResult; workshop.ledger append_progress
- Load startup-hitl-scan.py via importlib:
  _SCAN_FILE = Path("/opt/ultra-workshop/hermes-skills/startup-hitl-scan.py")
  Use try/except so import errors don't crash on non-VPS environments; store as _scan_mod (None if unavailable)
- Load brain_http.py via importlib (same pattern)
- REPO = "caiobellizzi/test-workshop-sandbox"
- ALLOWED_CHAT_ID = "7113965359"
- run_pr_opener(task_id: str, branch: str, plan: Plan, diff: Diff, session_id: str, chat_id: str = ALLOWED_CHAT_ID) -> str:
  CRITICAL ordering per PATTERNS.md HITL durable-first pattern:
  1. Write DB row FIRST:
     record_hitl_pause = getattr(_scan_mod, "record_hitl_pause", None)
     if record_hitl_pause:
         row_id = record_hitl_pause(session_id=session_id, task_description=f"[{task_id}] Push {branch!r} and open PR?", chat_id=chat_id)
     append_progress(task_id, "hitl_pause_recorded", {"branch": branch, "row_id": str(row_id if record_hitl_pause else "N/A")})
  2. Register with clarify_gateway (import from tools.clarify_gateway inside the function):
     try:
         from tools.clarify_gateway import register as cg_register  # type: ignore[import]
         clarify_id = f"hitl-pr-{task_id}-{uuid.uuid4().hex[:8]}"
         entry = cg_register(clarify_id=clarify_id, session_key=f"hitl-{session_id}", question=f"Push {branch!r} and open PR for task {task_id}?", choices=["Approve", "Reject"])
     except ImportError:
         # Not on VPS — clarify_gateway not available; raise to propagate
         raise RuntimeError("clarify_gateway not available — pr_open.py must run inside Hermes skill body on VPS")
  3. Decision loop: Hermes clarify blocks until resolved; entry["answer"] is "Approve" or "Reject"
     decision = entry.get("answer", "Reject")
     if decision != "Approve":
         append_progress(task_id, "pr_rejected", {"decision": decision})
         return "REJECTED"
  4. On Approve — git push then gh pr create:
     subprocess.run(["git", "push", "origin", branch], cwd=diff.branch, check=False, shell=False,
                    env={**os.environ, "GH_TOKEN": os.environ.get("GITHUB_PAT", "")})
     (Note: use diff.branch as the workspace_dir fallback or pass workspace_dir as parameter — add workspace_dir: str = "" param)
     pr_result = subprocess.run(
         ["gh", "pr", "create", "--repo", REPO, "--base", "main", "--head", branch,
          "--title", f"[workshop/{task_id}] {plan.goal[:60]}",
          "--body", f"Auto-generated by ultra-workshop.\n\nTask: {task_id}\n\n{diff.summary[:400]}"],
         capture_output=True, text=True, shell=False,
         env={**os.environ, "GH_TOKEN": os.environ.get("GITHUB_PAT", "")},
     )
     if pr_result.returncode != 0:
         raise RuntimeError(f"gh pr create failed: {pr_result.stderr}")
     pr_url = pr_result.stdout.strip()
     append_progress(task_id, "pr_opened", {"pr_url": pr_url})
  5. ADR write-back (non-blocking try/except per PATTERNS.md):
     try:
         adr_md = f"---\ntask_id: {task_id}\npr_url: {pr_url}\ngoal: {plan.goal}\nbranch: {branch}\n---\n\n# ADR: {task_id}\n\n{diff.summary}"
         _brain_http.call_agent("ingest", f"vault/_system/workshop-adrs/{task_id}.md\n\n{adr_md}")
         append_progress(task_id, "adr_written", {"path": f"vault/_system/workshop-adrs/{task_id}.md"})
     except Exception as exc:
         print(f"[pr_open] WARNING: ADR write-back failed (non-blocking): {exc}", file=sys.stderr, flush=True)
  6. return pr_url

Note on run_pr_opener signature: add workspace_dir: str = "" as a parameter for the git push cwd.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-workshop && python -c "from workshop.nodes.coder import run_coder; from workshop.nodes.pr_open import run_pr_opener; print('ok')"</automated>
  </verify>
  <verify>
    <automated>grep -c "shell=False" /Users/caiobellizzi/Documents/Projects/ultra-workshop/workshop/nodes/coder.py /Users/caiobellizzi/Documents/Projects/ultra-workshop/workshop/nodes/pr_open.py</automated>
  </verify>
  <done>
    - coder.py and pr_open.py importable
    - grep "shell=True" workshop/nodes/coder.py workshop/nodes/pr_open.py → no matches
    - grep "record_hitl_pause" workshop/nodes/pr_open.py → match (HITL write happens before clarify)
    - grep "non-blocking\|try:" workshop/nodes/pr_open.py → ADR write-back wrapped in try/except
    - grep "GH_TOKEN.*GITHUB_PAT\|GITHUB_PAT.*GH_TOKEN" workshop/nodes/pr_open.py → match (PAT injected via env)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coder.py→git clone | Network call to GitHub; workspace is a tmpdir under /tmp |
| pr_open.py→gh pr create | Injects GH_TOKEN from env; subprocess with shell=False |
| pr_open.py→clarify_gateway | HITL approval before PR push; durable write before clarify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | coder.py tmpdir workspace | mitigate | tempfile.mkdtemp creates 0700 owned by uws; tmpdir cleaned after task |
| T-04-02-02 | Spoofing | gh pr create GH_TOKEN | mitigate | shell=False; GH_TOKEN injected from env (never hardcoded); fine-grained PAT scoped to test-workshop-sandbox only |
| T-04-02-03 | Elevation of Privilege | clarify_gateway bypass | mitigate | HITL durable-first: row written before clarify call; restart safety via pending_hitl.db |
| T-04-02-04 | Information Disclosure | aider_runner.py stdout in diff.summary | mitigate | summary truncated at 500 chars; no secrets expected in aider output |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installations in this plan |
</threat_model>

<verification>
1. python -c "from workshop.nodes import triage, planner, coder, reviewer, pr_open; print('all nodes ok')" exits 0
2. grep -r "shell=True" workshop/nodes/ → no matches
3. grep "record_hitl_pause" workshop/nodes/pr_open.py → at least one match (before clarify)
4. grep "shell=False" workshop/nodes/pr_open.py → at least 2 matches (git push + gh pr create)
5. grep -A3 "adr_md\|write-back" workshop/nodes/pr_open.py | grep "except" → match (non-blocking try/except present)
</verification>

<success_criteria>
- All five node modules importable without VPS connectivity
- coder.py uses shell=False for all subprocess calls
- pr_open.py writes pending_hitl.db row before calling clarify_gateway.register
- pr_open.py ADR write-back wrapped in non-blocking try/except
- gh pr create uses GH_TOKEN env injection with shell=False
</success_criteria>

<output>
Create `.planning/phases/04-build-fix-pipeline/04-02-SUMMARY.md` when done
</output>
