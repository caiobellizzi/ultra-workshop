---
phase: 04-build-fix-pipeline
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/04-build-fix-pipeline/04-00-probe-results.md
autonomous: false
requirements:
  - REQ-ws-007
  - REQ-ws-008
  - REQ-ws-010

must_haves:
  truths:
    - "GITHUB_PAT is stored in /etc/uws/env and gh auth login as uws succeeds"
    - "gh CLI is callable as root on VPS"
    - "caiobellizzi/test-workshop-sandbox repo exists on GitHub"
    - "Hermes Python body skill API is probed (result recorded so Wave 1 can decide invocation style)"
  artifacts:
    - path: ".planning/phases/04-build-fix-pipeline/04-00-probe-results.md"
      provides: "Probe answers for Q1/Q2/Q3 from RESEARCH.md Open Questions; informs Wave 1 architecture"
      min_lines: 10
  key_links:
    - from: "GITHUB_PAT in /etc/uws/env"
      to: "gh auth login --with-token"
      via: "environment injection"
      pattern: "GITHUB_PAT=ghp_"
---

<objective>
Provision all hard prerequisites that block the Phase 4 pipeline: GITHUB_PAT, gh CLI, test-workshop-sandbox repo, and Hermes Python-body-skill probe. None of these can be automated fully — GITHUB_PAT requires a human to visit GitHub settings and paste the token.

Purpose: Wave 1 (types/orchestrator/nodes) is pure Python with no external dependencies and can proceed in parallel after the probe result is recorded. But end-to-end tests and the HITL gate require the PAT and gh CLI.
Output: /etc/uws/env updated, gh CLI installed, test-workshop-sandbox exists, probe results written to 04-00-probe-results.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-build-fix-pipeline/04-RESEARCH.md
@.planning/phases/03-skill-toolkit/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install gh CLI and verify/create test-workshop-sandbox</name>
  <files>.planning/phases/04-build-fix-pipeline/04-00-probe-results.md</files>
  <action>
SSH to the VPS as root (31.97.130.253). Execute the following steps in order, capturing each result:

STEP 1 — Check if gh CLI is installed:
  ssh root@31.97.130.253 "which gh && gh --version 2>&1 || echo 'GH_NOT_FOUND'"

STEP 2 — Install gh CLI if absent (no-op if already present):
  ssh root@31.97.130.253 "apt-get install -y gh 2>&1 || curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg && echo 'deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main' > /etc/apt/sources.list.d/github-cli.list && apt-get update && apt-get install gh -y"

STEP 3 — Probe Hermes Python body skill (answers RESEARCH.md Open Question 1). Create a minimal one-off probe skill at /tmp/probe-delegate.skill.md with this exact body:
```
---
name: probe-delegate
description: "Probe: does delegate_task work from Python skill body?"
version: 0.0.1
author: probe
license: MIT
platforms: [linux]
---
## Body
result = delegate_task(goal="return the single word: PROBE_OK", toolsets=[])
print(result)
```
Then invoke: ssh root@31.97.130.253 "sudo -u uws bash /opt/ultra-workshop/scripts/hermes-skill-run.sh probe-delegate 2>&1 | tail -20"
Record whether the output contains "PROBE_OK" or an error.

STEP 4 — Check if test-workshop-sandbox exists:
  Run: ssh root@31.97.130.253 "sudo -u uws bash -c 'GH_TOKEN=\$GITHUB_PAT gh repo view caiobellizzi/test-workshop-sandbox 2>&1 || echo REPO_NOT_FOUND'"
  If REPO_NOT_FOUND (or GITHUB_PAT not yet set — the human gate handles PAT), skip repo creation for now; it will be created after the PAT is injected in Task 2.

STEP 5 — Write probe results to .planning/phases/04-build-fix-pipeline/04-00-probe-results.md with this structure:
```markdown
# Phase 4 Wave 0 Probe Results
Date: <ISO date>

## Q1: Hermes Python body delegate_task support
- Status: SUPPORTED | NOT_SUPPORTED | TIMEOUT
- Evidence: <raw output snippet>
- Architecture impact: if SUPPORTED → workshop_build.py calls delegate_typed() directly; if NOT_SUPPORTED → each specialist uses `terminal hermes chat --skills <role>` subprocess

## Q3: gh CLI on VPS
- Status: PRE_INSTALLED | INSTALLED_NOW | FAILED
- Version: <gh --version output>

## Test-workshop-sandbox
- Status: EXISTS | PENDING_PAT (create after PAT injected) | CREATED
- URL: https://github.com/caiobellizzi/test-workshop-sandbox
```
  </action>
  <verify>
    <automated>grep -c "Q1:\|Q3:\|gh CLI" .planning/phases/04-build-fix-pipeline/04-00-probe-results.md</automated>
  </verify>
  <done>
    - 04-00-probe-results.md exists with Q1 and Q3 answers
    - gh CLI is callable: ssh root@31.97.130.253 "gh --version" exits 0
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>gh CLI installed on VPS, probe results written. GITHUB_PAT cannot be automated — it requires a personal access token from github.com/settings/tokens.</what-built>
  <how-to-verify>
    1. Go to https://github.com/settings/tokens?type=beta (Fine-grained personal access tokens)
    2. Click "Generate new token"
    3. Token name: ultra-workshop-pr
    4. Repository access: select only "caiobellizzi/test-workshop-sandbox"
    5. Permissions required:
       - Contents: Read and write
       - Pull requests: Read and write
       - Metadata: Read-only (required)
    6. Click "Generate token" — copy the ghp_... value immediately (shown once)
    7. On VPS, inject the token:
       ssh root@31.97.130.253 "echo 'GITHUB_PAT=ghp_YOUR_TOKEN_HERE' >> /etc/uws/env"
    8. Log in gh CLI as uws:
       ssh root@31.97.130.253 "sudo -u uws bash -c 'echo \$GITHUB_PAT | gh auth login --with-token'"
    9. Create the sandbox repo if it doesn't exist:
       ssh root@31.97.130.253 "sudo -u uws bash -c 'gh repo view caiobellizzi/test-workshop-sandbox 2>/dev/null || gh repo create caiobellizzi/test-workshop-sandbox --public --description \"ultra-workshop test sandbox\" --gitignore Python'"
    10. Verify: ssh root@31.97.130.253 "sudo -u uws gh auth status 2>&1"
        Expected: "Logged in to github.com account caiobellizzi"
  </how-to-verify>
  <resume-signal>Type "PAT injected" once step 10 shows "Logged in to github.com account caiobellizzi" (or describe any issues)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify PAT end-to-end and finalize probe results</name>
  <files>.planning/phases/04-build-fix-pipeline/04-00-probe-results.md</files>
  <action>
Run the following verification commands and append results to 04-00-probe-results.md:

VERIFY 1 — gh auth status as uws:
  ssh root@31.97.130.253 "sudo -u uws gh auth status 2>&1"
  Expected: contains "Logged in to github.com" and "caiobellizzi"

VERIFY 2 — GITHUB_PAT in /etc/uws/env:
  ssh root@31.97.130.253 "grep -c 'GITHUB_PAT=ghp_' /etc/uws/env"
  Expected: 1 (exactly one line)

VERIFY 3 — test-workshop-sandbox repo accessible:
  ssh root@31.97.130.253 "sudo -u uws gh repo view caiobellizzi/test-workshop-sandbox --json name,defaultBranchRef 2>&1"
  Expected: JSON with "name":"test-workshop-sandbox"

Append a ## Final Status section to 04-00-probe-results.md:
```markdown
## Final Status
- GITHUB_PAT injected: YES/NO
- gh auth login as uws: PASS/FAIL
- test-workshop-sandbox: EXISTS
- All Wave 0 prerequisites: READY/BLOCKED
```

If any check fails, do NOT mark as READY. Record what failed for the human to address.
  </action>
  <verify>
    <automated>grep -c "READY\|BLOCKED" .planning/phases/04-build-fix-pipeline/04-00-probe-results.md</automated>
  </verify>
  <verify>
    <human-check>Confirm "gh auth status" on VPS shows caiobellizzi logged in, and test-workshop-sandbox is visible at https://github.com/caiobellizzi/test-workshop-sandbox</human-check>
  </verify>
  <done>
    - 04-00-probe-results.md has Final Status section marked READY
    - ssh root@31.97.130.253 "sudo -u uws gh auth status" exits 0 with "caiobellizzi"
    - ssh root@31.97.130.253 "sudo -u uws gh repo view caiobellizzi/test-workshop-sandbox" exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human→VPS /etc/uws/env | GITHUB_PAT entered by human; stored as plaintext in root-owned env file |
| gh CLI→GitHub API | Fine-grained PAT scoped to test-workshop-sandbox only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Information Disclosure | /etc/uws/env (GITHUB_PAT) | mitigate | File is root-owned 0600; uws user reads via sudo env injection; never echoed to logs |
| T-04-00-02 | Elevation of Privilege | Fine-grained PAT scope | mitigate | PAT scoped to test-workshop-sandbox only — no other repos accessible |
| T-04-00-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan; gh CLI via apt (system package manager) |
</threat_model>

<verification>
After all tasks complete:
1. ssh root@31.97.130.253 "sudo -u uws gh auth status 2>&1" → contains "caiobellizzi"
2. ssh root@31.97.130.253 "grep -c 'GITHUB_PAT=ghp_' /etc/uws/env" → 1
3. cat .planning/phases/04-build-fix-pipeline/04-00-probe-results.md | grep "All Wave 0 prerequisites: READY"
4. ssh root@31.97.130.253 "gh --version" exits 0
</verification>

<success_criteria>
- gh CLI installed and callable on VPS as root and uws
- GITHUB_PAT in /etc/uws/env; gh auth login as uws succeeds
- caiobellizzi/test-workshop-sandbox repo exists
- Hermes Python-body probe result recorded (SUPPORTED or NOT_SUPPORTED) in 04-00-probe-results.md
- All Wave 0 prerequisites marked READY in probe results
</success_criteria>

<output>
Create `.planning/phases/04-build-fix-pipeline/04-00-SUMMARY.md` when done
</output>
