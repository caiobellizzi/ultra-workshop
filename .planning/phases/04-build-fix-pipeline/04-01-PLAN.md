---
phase: 04-build-fix-pipeline
plan: "01"
type: execute
wave: 1
depends_on: ["04-00"]
files_modified:
  - workshop/__init__.py
  - workshop/types.py
  - workshop/orchestrator.py
  - workshop/ledger.py
  - workshop/cost.py
autonomous: true
requirements:
  - REQ-ws-028
  - REQ-ws-009
  - REQ-ws-012

must_haves:
  truths:
    - "workshop.types can be imported from the Hermes venv without error"
    - "delegate_typed() retries on ValidationError up to 2 times before raising"
    - "append_progress() writes a JSONL line to ~/.ultra-workshop/tasks/<id>/progress_log.jsonl"
    - "check_circuit_breaker() raises BudgetExhausted at $20 daily spend and BudgetWarning at $18 in cron mode"
    - "task_ledger.md is created by write_task_ledger() in the task directory"
  artifacts:
    - path: "workshop/__init__.py"
      provides: "Package marker"
    - path: "workshop/types.py"
      provides: "Pydantic v2 schemas: Plan, PlanStep, Diff, FileChange, Review, Issue, IngestResult"
      contains: "class Plan(BaseModel)"
    - path: "workshop/orchestrator.py"
      provides: "delegate_typed() with max 2 parse retries; _extract_json(); brain_http importlib loader"
      contains: "def delegate_typed"
    - path: "workshop/ledger.py"
      provides: "task_dir(), append_progress(), write_task_ledger()"
      contains: "def append_progress"
    - path: "workshop/cost.py"
      provides: "get_daily_spend(), record_cost(), check_circuit_breaker(), BudgetExhausted, BudgetWarning"
      contains: "class BudgetExhausted"
  key_links:
    - from: "workshop/orchestrator.py"
      to: "hermes-skills/brain_http.py"
      via: "importlib.util.spec_from_file_location"
      pattern: "spec_from_file_location.*brain_http"
    - from: "workshop/cost.py"
      to: "/srv/second-brain/_system/cost-ledger.md"
      via: "Path.read_text()"
      pattern: "cost-ledger\\.md"
---

<objective>
Create the `workshop/` Python package: Pydantic v2 schemas (types.py), the delegate_typed() orchestration helper (orchestrator.py), the two-ledger task audit writer (ledger.py), and the circuit-breaker cost module (cost.py).

Purpose: These four files are the pure-Python foundation that all pipeline nodes (Plan 02) depend on. No Hermes, no VPS connectivity required — pure local unit-testable code.
Output: workshop/ package importable from the Hermes venv with all schemas and helpers exercised by unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-build-fix-pipeline/04-RESEARCH.md
@.planning/phases/04-build-fix-pipeline/04-PATTERNS.md
@.planning/phases/04-build-fix-pipeline/04-00-probe-results.md
@hermes-skills/aider_runner.py
@hermes-skills/brain_http.py
</context>

<interfaces>
<!-- Contracts established in this plan. Downstream plans (04-02, 04-03) import these. -->

From workshop/types.py (to be created):
```python
class PlanStep(BaseModel):
    id: str
    description: str
    files: list[str] = Field(default_factory=list)

class Plan(BaseModel):
    goal: str
    steps: list[PlanStep]
    affected_files: list[str] = Field(default_factory=list)

class FileChange(BaseModel):
    path: str
    diff: str

class Diff(BaseModel):
    summary: str
    changes: list[FileChange]
    branch: str  # workshop/<id>-<slug>

class Review(BaseModel):
    passed: bool
    feedback: str
    blocking_issues: list[str] = Field(default_factory=list)

class Issue(BaseModel):
    url: str
    title: str
    body: str
    number: int

class IngestResult(BaseModel):
    run_id: str
    status: str
    adr_path: str
```

From workshop/orchestrator.py (to be created):
```python
T = TypeVar("T", bound=BaseModel)

def delegate_typed(
    goal: str,
    context: str,
    output_schema: Type[T],
    toolsets: list[str],
    max_retries: int = 2,
) -> T: ...
```

From workshop/ledger.py (to be created):
```python
LEDGER_BASE = Path("/home/uws/.ultra-workshop/tasks")

def task_dir(task_id: str) -> Path: ...
def append_progress(task_id: str, event: str, data: dict) -> None: ...
def write_task_ledger(task_id: str, goal: str, status: str, pr_url: str = "") -> None: ...
```

From workshop/cost.py (to be created):
```python
WARN_THRESHOLD = 18.0
HARD_THRESHOLD = 20.0

class BudgetExhausted(Exception): ...
class BudgetWarning(Exception): ...

def get_daily_spend() -> float: ...
def record_cost(task_id: str, amount: float, model: str) -> None: ...
def check_circuit_breaker(mode: str = "interactive") -> None: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: workshop/types.py and workshop/orchestrator.py</name>
  <files>workshop/__init__.py, workshop/types.py, workshop/orchestrator.py, tests/phase-04/test_orchestrator.py</files>
  <behavior>
    - Test 1: Plan.model_validate_json('{"goal":"g","steps":[{"id":"1","description":"d"}]}') succeeds
    - Test 2: delegate_typed() retries exactly 2 times when output_schema.model_validate_json raises ValidationError, then raises on the 3rd failure
    - Test 3: delegate_typed() returns parsed model on first valid response
    - Test 4: _extract_json('{"key": "value"}') returns '{"key": "value"}'
    - Test 5: _extract_json('prefix {"a":1} suffix') returns '{"a":1}'
    - Test 6: _extract_json('no json here') raises ValueError
    - Test 7: All seven types (Plan, PlanStep, Diff, FileChange, Review, Issue, IngestResult) export model_json_schema() without error
  </behavior>
  <action>
Create workshop/__init__.py as an empty file with the deploy location comment.

Create workshop/types.py following PATTERNS.md §workshop/types.py exactly:
- Header comment: "# Deploy location: /opt/ultra-workshop/workshop/types.py"
- from __future__ import annotations
- Imports: pydantic BaseModel, Field; typing Optional
- Seven schema classes in this order: PlanStep, Plan, FileChange, Diff, Review, Issue, IngestResult
- Use Pydantic v2 API only — never .schema(), .parse_raw(), .json()
- All list fields use Field(default_factory=list)

Create workshop/orchestrator.py following PATTERNS.md §workshop/orchestrator.py:
- Header comment: "# Deploy location: /opt/ultra-workshop/workshop/orchestrator.py"
- Imports: importlib pattern to load brain_http from /opt/ultra-workshop/hermes-skills/brain_http.py at module level (use a try/except so unit tests that lack the VPS path still import cleanly — store loader result in _brain_http, set to None if import fails)
- TypeVar T bound to BaseModel
- _extract_json(text: str) -> str: find first '{' and last '}' in text; raise ValueError if not found
- delegate_typed(goal, context, output_schema, toolsets, max_retries=2) -> T:
  - Build schema_str = json.dumps(output_schema.model_json_schema(), indent=2)
  - Loop attempt in range(max_retries + 1):
    - On attempt > 0, append "\n\nIMPORTANT: Your previous response could not be parsed. You MUST return ONLY valid JSON matching this schema:\n{schema_str}" to prompt
    - Call _call_delegate_task(goal=prompt, context=context, toolsets=toolsets) — this is the Hermes tool; in tests it is monkeypatched
    - Try: return output_schema.model_validate_json(_extract_json(raw))
    - Except (ValidationError, ValueError): if attempt == max_retries: raise; else continue
  - raise RuntimeError("unreachable") after loop
- _call_delegate_task is a module-level stub: def _call_delegate_task(goal, context, toolsets): raise NotImplementedError("must be bound to Hermes delegate_task at runtime")

Create tests/phase-04/test_orchestrator.py with pytest tests for all 7 behaviors above. Mock _call_delegate_task via monkeypatch to simulate valid JSON, invalid JSON, and third-failure scenarios.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-workshop && python -m pytest tests/phase-04/test_orchestrator.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - All 7 pytest tests pass
    - python -c "from workshop.types import Plan, Diff, Review, Issue, IngestResult; print('ok')" exits 0
    - python -c "from workshop.orchestrator import delegate_typed, _extract_json; print('ok')" exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: workshop/ledger.py and workshop/cost.py</name>
  <files>workshop/ledger.py, workshop/cost.py, tests/phase-04/test_ledger.py, tests/phase-04/test_cost.py</files>
  <behavior>
    Ledger behaviors:
    - Test L1: append_progress("abc123", "triage_complete", {"role": "triage"}) creates progress_log.jsonl in a temp task dir with a valid JSON line containing ts, event, and role keys
    - Test L2: multiple append_progress() calls append separate lines; file has N lines for N calls
    - Test L3: write_task_ledger("abc123", "add hello endpoint", "completed", "https://github.com/.../pull/1") writes task_ledger.md with goal and pr_url visible in content
    - Test L4: task_dir() creates the directory with parents=True

    Cost behaviors:
    - Test C1: get_daily_spend() returns 0.0 when cost-ledger.md does not exist
    - Test C2: check_circuit_breaker() raises BudgetExhausted when get_daily_spend() returns 20.0
    - Test C3: check_circuit_breaker(mode="cron") raises BudgetWarning when get_daily_spend() returns 18.0
    - Test C4: check_circuit_breaker() does NOT raise when daily spend is 5.0
    - Test C5: check_circuit_breaker(mode="cron") does NOT raise when daily spend is 17.99
  </behavior>
  <action>
Create workshop/ledger.py following PATTERNS.md §workshop/ledger.py:
- Header: "# Deploy location: /opt/ultra-workshop/workshop/ledger.py"
- from __future__ import annotations
- Imports: json, os, datetime (datetime, timezone), pathlib Path
- LEDGER_BASE = Path("/home/uws/.ultra-workshop/tasks")
- task_dir(task_id: str) -> Path: creates and returns LEDGER_BASE / task_id with parents=True, exist_ok=True
- append_progress(task_id: str, event: str, data: dict) -> None:
  - p = task_dir(task_id) / "progress_log.jsonl"
  - entry = {"ts": datetime.now(timezone.utc).isoformat(), "event": event, **data}
  - Open in append mode, write json.dumps(entry) + "\n"
- write_task_ledger(task_id: str, goal: str, status: str, pr_url: str = "") -> None:
  - p = task_dir(task_id) / "task_ledger.md"
  - Write a markdown file with frontmatter: task_id, goal, status, pr_url (if non-empty), created timestamp
  - Minimum content: # Task {task_id}\n\n**Goal:** {goal}\n**Status:** {status}\n**PR:** {pr_url or "N/A"}

Create workshop/cost.py following PATTERNS.md §workshop/cost.py:
- Header: "# Deploy location: /opt/ultra-workshop/workshop/cost.py"
- from __future__ import annotations
- Imports: re, datetime.date, pathlib Path
- importlib to load brain_http (same try/except pattern as orchestrator.py)
- LEDGER_PATH = Path("/srv/second-brain/_system/cost-ledger.md")
- WARN_THRESHOLD = 18.0
- HARD_THRESHOLD = 20.0
- class BudgetExhausted(Exception): pass
- class BudgetWarning(Exception): pass
- get_daily_spend() -> float:
  - If LEDGER_PATH not exists: return 0.0
  - today = date.today().isoformat()
  - Parse lines containing today; match "amount:\s*([\d.]+)" with re.search; sum matching values
  - return total
- record_cost(task_id: str, amount: float, model: str) -> None:
  - Non-blocking try/except: call _brain_http.call_agent("curator", f"record-cost&amount={amount:.6f}&task={task_id}&source=workshop&model={model}")
  - Print [cost-ledger] run_id on success, WARNING on exception; never re-raise
- check_circuit_breaker(mode: str = "interactive") -> None:
  - spend = get_daily_spend()
  - if spend >= HARD_THRESHOLD: raise BudgetExhausted(...)
  - if mode == "cron" and spend >= WARN_THRESHOLD: raise BudgetWarning(...)

For tests: use tmp_path fixture (pytest built-in) to override LEDGER_BASE and LEDGER_PATH via monkeypatch.setattr.
Create tests/phase-04/__init__.py if it doesn't exist.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-workshop && python -m pytest tests/phase-04/test_ledger.py tests/phase-04/test_cost.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - All 9 pytest tests (L1–L4, C1–C5) pass
    - python -c "from workshop.ledger import append_progress, write_task_ledger; print('ok')" exits 0
    - python -c "from workshop.cost import BudgetExhausted, BudgetWarning, check_circuit_breaker; print('ok')" exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workshop/cost.py→/srv/second-brain | Reads vault file for spend calculation; potential TOCTOU |
| workshop/orchestrator.py→brain_http.py | importlib load at runtime from fixed path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | /srv/second-brain/_system/cost-ledger.md | accept | File is vault-managed; workshop only reads, never writes directly |
| T-04-01-02 | Denial of Service | delegate_typed() retry loop | mitigate | max_retries=2 hard cap; raises after 3rd failure rather than looping indefinitely |
| T-04-01-03 | Information Disclosure | progress_log.jsonl | mitigate | Written to /home/uws/ (uws-owned, 0700 dir); os.chmod 0o600 on the task dir |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | No new package installations; pydantic 2.12.5 already in venv |
</threat_model>

<verification>
1. python -m pytest tests/phase-04/test_orchestrator.py tests/phase-04/test_ledger.py tests/phase-04/test_cost.py -q → all pass
2. python -c "import workshop.types, workshop.orchestrator, workshop.ledger, workshop.cost; print('all imports ok')" exits 0
3. grep -r "\.schema()\|\.parse_raw()\|\.json()" workshop/ → no matches (Pydantic v1 API absent)
4. grep -r "shell=True" workshop/ → no matches
</verification>

<success_criteria>
- 16 unit tests (7 orchestrator + 4 ledger + 5 cost) all pass
- All workshop/ submodules importable without VPS connectivity
- No Pydantic v1 API calls in workshop/
- BudgetExhausted raised at $20; BudgetWarning raised at $18 in cron mode
</success_criteria>

<output>
Create `.planning/phases/04-build-fix-pipeline/04-01-SUMMARY.md` when done
</output>
