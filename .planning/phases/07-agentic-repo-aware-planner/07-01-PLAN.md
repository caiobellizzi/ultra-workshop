---
phase: 07-agentic-repo-aware-planner
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-07/__init__.py
  - tests/phase-07/test_doc_resolver.py
  - tests/phase-07/test_workspace.py
  - tests/phase-07/test_planner_llm.py
  - tests/phase-07/planner-smoke.bats
  - tests/phase-07/hermes-tool-notes.txt
autonomous: false
requirements:
  - REQ-ws-034
  - REQ-ws-031

must_haves:
  truths:
    - "tests/phase-07/ directory exists and pytest discovers all test stubs (exits 0)"
    - "Exact Hermes tool identifiers for read_file/list_files/grep_files are confirmed from the running VPS binary"
    - "specialist-home-orchestrator HERMES_HOME allowlist state is known before any SKILL.md changes"
  artifacts:
    - path: "tests/phase-07/__init__.py"
      provides: "pytest discovery for phase-07 test package"
    - path: "tests/phase-07/test_doc_resolver.py"
      provides: "stub test module for REQ-ws-032 doc resolution coverage"
    - path: "tests/phase-07/test_workspace.py"
      provides: "stub test module for REQ-ws-030 workspace/state coverage"
    - path: "tests/phase-07/test_planner_llm.py"
      provides: "stub test module for REQ-ws-033 planner schema coverage"
    - path: "tests/phase-07/planner-smoke.bats"
      provides: "bats smoke for REQ-ws-031 dry-run routing assertion"
    - path: "tests/phase-07/hermes-tool-notes.txt"
      provides: "confirmed Hermes tool IDs and specialist-home-orchestrator allowlist state, consumed by Plans 07-02 and 07-03"
  key_links:
    - from: "tests/phase-07/"
      to: "pytest discovery"
      via: "__init__.py"
      pattern: "tests/phase-07/__init__.py"
---

<objective>
Wave 0 prerequisite: create the test scaffold and verify the unconfirmed Hermes tool
identifiers on the live VPS before any implementation tasks run.

Purpose: Two medium-risk assumptions (A2, A3 from RESEARCH.md) must be resolved before
the planner SKILL.md rewrite in Plan 03. If `read_file`/`list_files`/`grep_files` are
not the correct Hermes tool IDs in the installed binary, or if specialist-home-orchestrator
already restricts them, the SKILL.md changes in Wave 2 must use different identifiers.
This plan is `autonomous: false` because Task 2 requires running a command on the live VPS.

Output:
- `tests/phase-07/` package with stub test files (all pass)
- Confirmed Hermes tool names and specialist-home-orchestrator allowlist state documented
  in a short note in `tests/phase-07/hermes-tool-notes.txt`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agentic-repo-aware-planner/07-RESEARCH.md

# Key analogs (from RESEARCH.md):
# - tests/phase-06/ — mirrors directory structure to follow
# - tests/test_repo_registry.py — mirrors pytest style (from __future__, no class, tmp_path)
# - tests/phase-04/model-matrix-smoke.bats — mirrors bats style (@test, run, [[ ]])
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/phase-07/ package with stub test files</name>
  <files>tests/phase-07/__init__.py, tests/phase-07/test_doc_resolver.py, tests/phase-07/test_workspace.py, tests/phase-07/test_planner_llm.py, tests/phase-07/planner-smoke.bats</files>

  <read_first>
    - tests/phase-06/ — read directory listing + test_workshop_coder.py for pytest style
    - tests/test_repo_registry.py — canonical pytest style (from __future__, tmp_path, no class)
    - tests/phase-04/model-matrix-smoke.bats — canonical bats style to mirror
    - .planning/phases/07-agentic-repo-aware-planner/07-VALIDATION.md — Wave 0 checklist
  </read_first>

  <action>
    Create the tests/phase-07/ package. Mirror the exact style of tests/test_repo_registry.py
    (from __future__ annotations, pytest, tmp_path, no test classes) and
    tests/phase-04/model-matrix-smoke.bats (#!/usr/bin/env bats, SCRIPT path relative to
    BATS_TEST_DIRNAME, @test, run, status/output assertions).

    tests/phase-07/__init__.py: empty file.

    tests/phase-07/test_doc_resolver.py: import stub with 3 test function signatures
    marked xfail pending implementation. Functions to stub:
      - test_tier1_repo_first: xfail, verifies resolve_doc finds doc in workspace_dir
      - test_tier2_vault_grep: xfail, verifies fallback to vault path
      - test_tier3_brain_degraded: xfail, verifies Brain error returns None gracefully
      - test_doc_name_traversal_blocked: xfail, verifies "../etc/passwd" doc_name is rejected
    Import: `from workshop.doc_resolver import resolve_doc` — mark with `pytest.importorskip`
    or wrap in try/except ImportError and xfail if module not yet present.

    tests/phase-07/test_workspace.py: import stub with 2 test function signatures marked xfail:
      - test_new_task_state_has_workspace_dir: xfail, asserts "workspace_dir" in new_task_state(...)
      - test_clone_saves_workspace_dir: xfail, patches subprocess.run to simulate clone success
        and asserts state["workspace_dir"] is populated after clone step
    Import: `from workshop.state import new_task_state`

    tests/phase-07/test_planner_llm.py: import stub with 1 test function marked xfail:
      - test_plan_schema_valid: xfail, validates that a minimal plan dict validates against
        Plan.model_validate({"goal": "test", "steps": [], "affected_files": []})
    Import: `from workshop.types import Plan`

    tests/phase-07/planner-smoke.bats: bats file asserting the NEW (post-Phase-7) planner
    dry-run output. Since hermes-skill-run.sh has NOT been updated yet, these tests must
    be written but will be SKIPPED or use a TODO marker. Use bats `skip` for now:
      - @test "planner-specialist routes through hermes chat after Phase 7": skip "TODO: update hermes-skill-run.sh first (Plan 03)"
      - @test "planner-specialist dry-run shows specialist-home-orchestrator HERMES_HOME": skip "TODO: update hermes-skill-run.sh first (Plan 03)"

    After creating all files, verify: `python -m pytest tests/phase-07/ -v --no-header`
    exits 0 (xfail counts as passed, skip counts as passed).
  </action>

  <verify>
    <automated>python -m pytest tests/phase-07/ -v --no-header 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/phase-07/ -v` exits 0 with all tests reported as xfail or skipped
    - `tests/phase-07/__init__.py` exists (can be empty)
    - `tests/phase-07/test_doc_resolver.py` contains 4 test functions (test_tier1*, test_tier2*, test_tier3*, test_doc_name_traversal*)
    - `tests/phase-07/test_workspace.py` contains test_new_task_state_has_workspace_dir and test_clone_saves_workspace_dir
    - `tests/phase-07/test_planner_llm.py` contains test_plan_schema_valid
    - `tests/phase-07/planner-smoke.bats` exists with 2 @test entries using `skip`
  </acceptance_criteria>

  <done>Phase-07 test scaffold exists; pytest reports 0 failures; bats file is valid syntax</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: VPS verification — confirm Hermes tool IDs and specialist-home-orchestrator allowlist</name>

  <read_first>
    - .planning/phases/07-agentic-repo-aware-planner/07-RESEARCH.md — Open Questions §1 and §2 (Assumptions A2, A3)
    - skills/planner-specialist/SKILL.md — current forbidden tool list to compare against confirmed names
  </read_first>

  <what-built>
    Task 1 created the test scaffold. This checkpoint requires the human to run two
    commands on the VPS and paste the output so the Hermes tool identifiers and
    specialist-home-orchestrator allowlist state can be documented before Plan 03
    rewrites the planner SKILL.md.
  </what-built>

  <how-to-verify>
    Run both commands on the VPS (SSH in first):

    1. Confirm exact tool names in the installed Hermes binary:
       ```
       HERMES_HOME=/opt/ultra-workshop/specialist-home-orchestrator \
         /opt/ultra-workshop/hermes/venv/bin/hermes tools list 2>&1
       ```
       Expected: a list of tool identifiers including file-reading tools.
       Look for: read_file, list_files, grep_files (or similar variants like
       read_files, list_directory, search_files).

    2. Check whether specialist-home-orchestrator already allows or restricts those tools:
       ```
       cat /opt/ultra-workshop/specialist-home-orchestrator/HERMES.md 2>/dev/null || echo "NO HERMES.MD"
       ls /opt/ultra-workshop/specialist-home-orchestrator/ 2>&1
       ```
       Expected: shows whether a HERMES.md or config restricts tools.

    After running, record findings in tests/phase-07/hermes-tool-notes.txt:
    - Exact tool IDs (e.g., "read_file confirmed" or "actual name is read_files")
    - Whether specialist-home-orchestrator blocks any file tools (yes/no + config excerpt)
  </how-to-verify>

  <resume-signal>
    Paste the `hermes tools list` output and `cat HERMES.md` output here, OR type
    "confirmed: read_file/list_files/grep_files are exact names, no restriction in orchestrator home"
    if that matches the actual output. Include any name differences found.
  </resume-signal>

  <acceptance_criteria>
    - Human has run `hermes tools list` on VPS and pasted output or confirmed exact tool names
    - tests/phase-07/hermes-tool-notes.txt created with confirmed tool IDs and allowlist state
    - If tool names differ from read_file/list_files/grep_files: corrected names documented
      so Plan 03 (SKILL.md rewrite) uses the correct identifiers
  </acceptance_criteria>

  <done>Hermes tool names confirmed; specialist-home-orchestrator allowlist state documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scaffold → workshop package | Test imports workshop.state, workshop.types; stubs only, no live execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; stdlib + existing pytest/bats only |
| T-07-01-01 | Information Disclosure | hermes tools list on VPS | accept | Command is read-only, runs as uws; no secrets exposed in tool list output |
</threat_model>

<verification>
- `python -m pytest tests/phase-07/ -v` exits 0 (all xfail/skip)
- `python -m pytest tests/phase-06/ tests/test_repo_registry.py -q` exits 0 (regression check)
- tests/phase-07/hermes-tool-notes.txt exists with confirmed tool names after Task 2
</verification>

<success_criteria>
- tests/phase-07/ package exists and is pytest-discoverable
- All 6 stub test functions and 2 bats @test entries are present
- Hermes tool IDs and specialist-home-orchestrator allowlist state confirmed and documented
- Phase 6 + test_repo_registry.py regression suite stays green
</success_criteria>

<output>
Create `.planning/phases/07-agentic-repo-aware-planner/07-01-SUMMARY.md` when done
</output>
