---
phase: 07-agentic-repo-aware-planner
plan: 05
type: execute
wave: 4
depends_on:
  - "07-03"
  - "07-04"
files_modified:
  - tests/phase-07/test_planner_llm.py
autonomous: true
requirements:
  - REQ-ws-033
  - REQ-ws-034

must_haves:
  truths:
    - "Plan.model_validate() accepts the LLM planner output schema (goal + steps + affected_files)"
    - "A plan with real workspace paths in affected_files validates without error"
    - "All phase-07 unit tests pass (none xfail)"
    - "Full phase regression suite green: pytest tests/ && bats phase-04 && bats phase-06"
  artifacts:
    - path: "tests/phase-07/test_planner_llm.py"
      provides: "activated test_plan_schema_valid and test_plan_affected_files_real_paths"
  key_links:
    - from: "tests/phase-07/test_planner_llm.py"
      to: "workshop/types.py Plan model"
      via: "Plan.model_validate()"
      pattern: "Plan.model_validate"
---

<objective>
Wave 4 — phase gate plan. Activate the remaining xfail test (test_plan_schema_valid) and
add a second test (test_plan_affected_files_real_paths) that validates Plans with realistic
workspace-style paths. Run the complete phase regression suite to confirm SC-5 and SC-6.

Purpose: This plan is the "green board" gate before `/gsd:verify-work`. It ensures all
Plan 07 work is integration-complete from a test perspective and the existing suites are
not broken.

Output: Fully activated tests/phase-07/test_planner_llm.py; clean regression run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-agentic-repo-aware-planner/07-RESEARCH.md

# Read first:
# workshop/types.py — Plan, PlanStep schema (lines 9-25)
# tests/phase-07/test_planner_llm.py — current stub to activate
# tests/test_repo_registry.py — canonical pytest style to mirror
# .planning/phases/07-agentic-repo-aware-planner/07-VALIDATION.md — full verification map

<interfaces>
From workshop/types.py (verified):
  class PlanStep(BaseModel):
      id: str
      description: str
      files: list[str] = Field(default_factory=list)

  class Plan(BaseModel):
      goal: str
      steps: list[PlanStep]
      affected_files: list[str] = Field(default_factory=list)
      # Plan.model_validate() accepts dict or JSON string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate test_plan_schema_valid and add test_plan_affected_files_real_paths; run full regression suite</name>
  <files>tests/phase-07/test_planner_llm.py</files>

  <read_first>
    - tests/phase-07/test_planner_llm.py — current stub content
    - workshop/types.py — Plan and PlanStep model definitions
    - tests/test_repo_registry.py — canonical pytest style
    - tests/phase-07/07-VALIDATION.md — Per-Task Verification Map to complete
  </read_first>

  <behavior>
    - test_plan_schema_valid: Plan.model_validate({"goal": "add hello world", "steps": [{"id": "1", "description": "create file", "files": ["hello.py"]}], "affected_files": ["hello.py"]}) does not raise; result.goal == "add hello world"; len(result.steps) == 1.

    - test_plan_affected_files_real_paths: A plan dict with workspace-style paths
      {"goal": "refactor auth", "steps": [{"id": "1", "description": "update auth module", "files": ["src/workshop/orchestrator.py", "tests/test_orchestrator.py"]}], "affected_files": ["src/workshop/orchestrator.py", "tests/test_orchestrator.py"]}
      validates successfully; result.affected_files contains both paths exactly.

    - test_plan_empty_affected_files: An LLM response with affected_files=[] is valid
      (empty is allowed); result.affected_files == [].

    - test_clarification_needed_is_not_a_plan: A dict {"clarification_needed": True, "question": "Which file?"}
      raises pydantic.ValidationError when passed to Plan.model_validate() — confirming that
      ClarificationNeeded is handled by the existing orchestrator detection (not passed as a Plan).
      (This tests that the separation is clean — workshop_build.py already handles this via
      ClarificationNeeded exception raised in run_specialist/orchestrator._extract_json.)
  </behavior>

  <action>
    In tests/phase-07/test_planner_llm.py: replace all xfail stubs with real tests.
    Add the 4 test functions (test_plan_schema_valid, test_plan_affected_files_real_paths,
    test_plan_empty_affected_files, test_clarification_needed_is_not_a_plan).

    For test_clarification_needed_is_not_a_plan: wrap in `pytest.raises(Exception)` — any
    ValidationError or similar exception is acceptable.

    After activating tests, run the full regression suite in sequence:
    1. `python -m pytest tests/phase-07/ -q` — all pass
    2. `python -m pytest tests/phase-06/ tests/test_repo_registry.py -q` — green
    3. `bats tests/phase-04/model-matrix-smoke.bats` — green
    4. `python -m pytest tests/ -q` — green (full suite)

    If any test in the full suite fails due to Phase 7 changes, diagnose and fix before
    marking this plan complete.
  </action>

  <verify>
    <automated>python -m pytest tests/phase-07/ -q && python -m pytest tests/phase-06/ tests/test_repo_registry.py -q && bats tests/phase-04/model-matrix-smoke.bats</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/phase-07/ -v` exits 0 with ALL tests PASSED (no xfail, no skip)
    - `python -m pytest tests/phase-06/ tests/test_repo_registry.py -q` exits 0
    - `bats tests/phase-04/model-matrix-smoke.bats` exits 0
    - `python -m pytest tests/ -q` exits 0 (full suite)
    - test_planner_llm.py contains 4 test functions: test_plan_schema_valid, test_plan_affected_files_real_paths, test_plan_empty_affected_files, test_clarification_needed_is_not_a_plan
    - No test in any suite is skipped or marked xfail that was previously passing
  </acceptance_criteria>

  <done>All phase-07 tests pass; no xfail remaining; full regression suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → production modules | Tests import workshop.types; read-only, no side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | Full test suite catches regressions from Phase 7 changes | mitigate | Running `python -m pytest tests/ -q` is the regression gate; any breakage is caught and fixed before this plan is closed |
| T-07-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages; pytest already installed |
</threat_model>

<verification>
- `python -m pytest tests/phase-07/ -v` exits 0, all PASSED
- `python -m pytest tests/ -q` exits 0 (full suite)
- `bats tests/phase-04/model-matrix-smoke.bats` exits 0
- `bats tests/phase-07/planner-smoke.bats` exits 0
</verification>

<success_criteria>
- All 4 test_planner_llm.py tests pass (not xfail)
- Complete regression suite green across phase-04, phase-06, phase-07 and full pytest tests/
- Phase 7 ready for /gsd:verify-work
</success_criteria>

<output>
Create `.planning/phases/07-agentic-repo-aware-planner/07-05-SUMMARY.md` when done
</output>
